# Spring 백엔드 베이스라인 + DDD 설계 가이드 (재사용 템플릿)

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1.0 |
| 목적 | 프로젝트 간 재사용 가능한 Spring 백엔드 표준 + 도메인 주도 설계(DDD) 적용 지침 |
| 범위 | 백엔드 한정 (프론트엔드·UI 비포함) |
| 배포 형태 | 온프레미스 / 클라우드 무관 (환경 비종속) |

> 이 문서는 특정 업무 도메인에 종속되지 않는다. 5~7장의 기능 예시(사용자/프로젝트/권한)는 **DDD 적용 방법을 보여주는 예제**일 뿐이며, 다른 프로젝트에서는 자신의 바운디드 컨텍스트로 대체한다.

---

## 1. 이 템플릿이 정의하는 것

새 프로젝트의 백엔드를 시작할 때 매번 다시 결정하지 않도록, 검증된 기본값을 고정한다:

- **기술 스택** (2장) — 런타임·프레임워크·영속성·인증·운영 도구
- **공통 아키텍처 결정** (3장) — BFF 인증, 영속성 이원화, 가상 스레드 등
- **DDD 설계 표준** (4장) — 계층/패키지 구조, 빌딩 블록, JPA·MyBatis와 DDD의 결합 방식 ← **핵심**
- **기능별 DDD 적용 패턴** (5장) — 인증·식별·권한 등 흔한 기능을 DDD로 풀어내는 방법
- **비기능 요구 베이스라인** (6장)
- **신규 프로젝트 적용 체크리스트** (7장)

각 프로젝트는 이 문서를 복사해 도메인 특화 내용만 채워 넣는다.

---

## 2. 기술 스택 (백엔드)

| 영역 | 선택 | 비고 |
|------|------|------|
| 언어/런타임 | **Java 21** (가상 스레드) | LTS, JPA 궁합, 생태계 |
| 프레임워크 | **Spring Boot + Spring MVC** | CRUD 중심이면 WebFlux보다 단순 |
| 영속성 (쓰기·기본 조회) | **Spring Data JPA / Hibernate** | 애그리거트 영속화 담당 |
| 영속성 (복잡 조회) | **MyBatis** (mybatis-spring) | 읽기 모델·리포트용 SQL 제어 |
| DB | **PostgreSQL** | 재귀 CTE 등 활용 |
| 세션/캐시 | **Redis** | BFF 세션 저장소 |
| 인증 | **Spring Security OAuth2/OIDC** | BFF 패턴, IdP 비종속(Entra ID·Keycloak·Okta 등) |
| 디렉터리/외부 연동 | (프로젝트별) | 위임 권한 범위 우선, 전체 동기화 지양 |
| 스키마 관리 | **Flyway** | 버전 관리 마이그레이션 |
| 보조 | **Lombok** | 보일러플레이트 절감 |
| 향후 | **Spring AI** | MVC에서 동작 |

**영속성 역할 분담 원칙**: 쓰기·기본 조회는 JPA(애그리거트 단위 정합성), 복잡 조회·목록·리포트는 MyBatis. **동일 DataSource로 단일 트랜잭션에 묶는다.** 이 분담은 4.4의 CQRS-lite로 자연스럽게 매핑된다.

---

## 3. 공통 아키텍처 결정 (재사용 가능한 패턴)

| # | 결정 | 근거 |
|---|------|------|
| A-1 | **MVC + 가상 스레드** (WebFlux 아님) | CRUD 중심에서 리액티브 이점 적음, 단순성·디버깅성 우선 |
| A-2 | **JPA + MyBatis 병행** | 쓰기=생산성/정합성, 복잡 조회=SQL 제어 |
| A-3 | **BFF 인증** | 액세스 토큰을 브라우저에 두지 않음. 토큰은 서버 세션(Redis)에만 보관, 프론트는 쿠키 세션으로 통신 |
| A-4 | **인증과 인가의 분리** | 신원 확인(IdP + Spring Security)과 리소스 권한 판단(인가 계층)을 별도 책임으로 |
| A-5 | **인가 엔진 = 포트/어댑터** | 앱 내부 구현(Postgres + 재귀 CTE) ↔ 외부 엔진(예: OpenFGA)을 어댑터 교체로 전환 가능하게 (4.5 참조) |
| A-6 | **불변 식별자 기반 매칭** | 외부 식별자(이메일/UPN)는 재할당 가능 → 최초 연결 후에는 불변 키(IdP oid 등)로 매칭 |
| A-7 | **확장성 트립와이어** | 특정 영역(예: 권한 그래프)의 복잡도가 앱 내부 처리 한계를 넘으면 전용 엔진 도입 재검토 |

> A-3·A-4·A-6은 신원/인증을 다루는 프로젝트에서 거의 항상 재사용 가능하다. 인증이 없는 내부 배치성 서비스라면 생략한다.

---

## 4. DDD 설계 표준 ★ (핵심)

> **원칙: 모든 기능은 도메인 주도 설계로 설계한다.** 데이터 모델(테이블)이 아니라 **도메인 모델(불변식을 지키는 객체)**에서 출발한다. 테이블은 도메인을 영속화하기 위한 부산물이다.

### 4.1 바운디드 컨텍스트부터 나눈다

기능 목록을 받으면 곧장 테이블을 그리지 말고, **의미가 응집되는 경계(바운디드 컨텍스트)**를 먼저 식별한다.

- 각 컨텍스트는 자신만의 모델·용어(유비쿼터스 언어)·트랜잭션 경계를 가진다.
- 컨텍스트 **간** 참조는 객체가 아니라 **식별자(ID)**로 한다. (예: 권한 컨텍스트가 사용자를 참조할 때 `User` 객체가 아니라 `userId`만 안다.)
- 컨텍스트 간 연동이 필요하면 **도메인 이벤트** 또는 명시적 포트로 한다. 직접 의존을 만들지 않는다.

> 다른 프로젝트에 적용할 때 가장 먼저 하는 일: "이 시스템의 바운디드 컨텍스트는 무엇인가?"를 적는 것.

### 4.2 패키지/계층 구조 (헥사고날 + DDD)

컨텍스트 우선, 그 안에서 계층으로 나눈다:

```
com.company.app
└── <context>                 ← 예: identity, authorization, project
    ├── domain                ← 순수 도메인 (프레임워크 의존 최소)
    │   ├── model             ← 애그리거트 루트, 엔티티, 값 객체
    │   ├── service           ← 도메인 서비스
    │   ├── event             ← 도메인 이벤트
    │   └── repository        ← 리포지토리 인터페이스(포트)
    ├── application           ← 유스케이스(애플리케이션 서비스), 커맨드/쿼리, 포트 정의
    ├── infrastructure        ← JPA 리포지토리 구현, MyBatis 매퍼, 외부 어댑터
    └── interfaces            ← REST 컨트롤러, 요청/응답 DTO
```

**의존 방향은 항상 안쪽으로**: `interfaces → application → domain`. `infrastructure`는 `domain`이 정의한 인터페이스(포트)를 **구현**한다. 도메인은 Spring·JPA·MyBatis를 모른다(또는 최소한만 안다 — 4.3 참조).

### 4.3 빌딩 블록

| 블록 | 책임 | 규칙 |
|------|------|------|
| **애그리거트 루트** | 정합성 경계, 트랜잭션 단위 | 외부는 루트를 통해서만 내부에 접근. 루트당 리포지토리 1개 |
| **엔티티** | 식별자로 구분되는 가변 객체 | 애그리거트 내부에 소속 |
| **값 객체(VO)** | 값으로 동등성 판단, 불변 | 도메인 개념을 타입으로 (예: `Email`, `DateRange`, `Percentage`, `ResourceCode`) |
| **도메인 서비스** | 단일 애그리거트에 속하지 않는 도메인 로직 | 상태 없음, 도메인 용어로 표현 |
| **애플리케이션 서비스** | 유스케이스 조율, 트랜잭션 경계 | **비즈니스 규칙 금지** — 규칙은 도메인에 |
| **리포지토리** | 애그리거트 컬렉션처럼 다루는 인터페이스 | 도메인에 인터페이스, infra에 구현 |
| **도메인 이벤트** | "도메인에서 일어난 일" | 컨텍스트 간/내 느슨한 결합 |
| **팩토리** | 복잡한 애그리거트 생성 | 생성 시점에 불변식 보장 |

**값 객체를 적극 쓴다.** 원시 타입(`String email`, `int rate`)을 그대로 흘리지 말고 검증·의미를 담은 VO로 감싼다. DDD 코드 품질의 절반은 여기서 갈린다.

### 4.4 JPA와 DDD — 두 가지 방식 (이 스택의 핵심 결정)

JPA 엔티티와 도메인 애그리거트의 관계를 어떻게 둘지 결정해야 한다.

**방식 A — 실용형(기본 권장):** JPA 엔티티가 곧 애그리거트다. 도메인 객체에 JPA 애너테이션을 붙이되 다음 규율로 도메인성을 지킨다.
- `setter` 공개 금지 → 의미 있는 메서드로 상태 변경 (`user.activate(oid)`)
- 정적 팩토리/생성자로만 생성, 불변식을 생성 시점에 보장
- 컬렉션은 캡슐화(외부에 가변 컬렉션 노출 금지)
- 무인자 생성자는 `protected`로 숨김 (JPA만 사용)
- *장점*: 매핑 보일러플레이트 없음, 빠름. *단점*: 도메인이 영속성에 약간 결합(지연 로딩 등).

**방식 B — 엄격 분리형:** 순수 도메인 모델과 JPA 영속성 모델을 분리하고 infrastructure에서 상호 변환한다.
- *장점*: 도메인이 완전히 순수. *단점*: 매핑 코드·이중 모델 부담이 큼.

> **권장**: 대부분의 사내/업무 시스템은 **방식 A**로 충분하다. 도메인 규칙이 매우 복잡하거나 영속성과 강하게 독립해야 하는 일부 컨텍스트만 선택적으로 방식 B를 적용한다. 한 프로젝트 안에서 컨텍스트별로 섞어도 된다.

### 4.5 MyBatis = 읽기 모델 (CQRS-lite)

쓰기와 읽기의 모델을 분리한다. 이것이 2장의 "JPA=쓰기 / MyBatis=복잡 조회" 분담의 DDD적 정당화다.

- **쓰기(커맨드)**: 항상 애그리거트 + JPA 리포지토리를 거친다. 불변식을 강제한다.
- **읽기(쿼리)**: 목록 화면·리포트·집계·"내가 접근 가능한 X 목록" 등은 **애그리거트를 거치지 않고** MyBatis로 직접 조회해 **읽기 전용 DTO/프로젝션**을 반환한다.
- 읽기 모델은 화면에 맞춰 자유롭게 조인·평탄화한다. 도메인 객체로 복원하지 않는다.

```
[Command]  Controller → AppService → Aggregate(JPA) → DB   (불변식 보장)
[Query]    Controller → QueryService → MyBatis Mapper → DTO (성능·표현 자유)
```

### 4.6 트랜잭션·정합성 경계

- **하나의 트랜잭션 = 하나의 애그리거트 수정**을 기본 원칙으로 한다.
- 여러 애그리거트/컨텍스트에 걸친 변경은 **도메인 이벤트 + 최종적 일관성**으로 처리한다. (예: 사용자 비활성화 → `UserDisabled` 이벤트 → 권한 컨텍스트가 구독해 정리)
- 외부 인가 엔진처럼 별도 저장소에 쓰기를 동기화해야 하면 dual-write 정합성 정책을 명시한다(이벤트 기반 보정, 재시도 등).

---

## 5. 기능별 DDD 적용 패턴 (예시 — 다른 도메인으로 대체 가능)

아래는 흔한 기능들을 DDD로 푸는 worked example이다. 자신의 프로젝트에서는 컨텍스트와 애그리거트 이름만 바꾼다.

### 5.1 인증·세션 — *과하게 DDD화하지 않는다*

- BFF 인증, Spring Security OIDC, Redis 세션은 **횡단 인프라**다. 풍부한 도메인 모델이 아니다.
- 도메인과 닿는 지점은 **최초 로그인 시 신원 연결**뿐 — 이건 식별 컨텍스트의 도메인 연산(`user.linkIdentity(oid)`)으로 다룬다.
- 교훈: 모든 것을 애그리거트로 만들지 말 것. 인프라는 인프라로.

### 5.2 식별/온보딩 컨텍스트 (Identity)

흔한 패턴: 외부에서 먼저 등록되고(미로그인) 나중에 로그인으로 신원이 연결되는 사용자.

- **애그리거트 루트**: `User`
- **값 객체**: `Email`(검증), `ExternalId`(IdP oid, 불변), `UserStatus`(INVITED/ACTIVE/DISABLED)
- **도메인 규칙(애그리거트 안에)**:
  - `User.invite(email, ...)` — 미로그인 등록, 상태 INVITED
  - `User.linkIdentity(externalId)` — INVITED→ACTIVE 전이, 두 번 연결 금지
  - `User.disable()` — DISABLED 전이
  - **필드 소유권**: IdP 출처 필드(이름 등)는 로그인 시 갱신, 관리자 입력 필드(내부 역할 등)는 보존 — 이 규칙을 애그리거트 메서드 안에 둔다.
- **불변식**: `email` unique, `externalId` nullable+unique, 연결 후 매칭은 불변 ID로(A-6).
- **도메인 이벤트**: `UserActivated`, `UserDisabled`.
- **핵심 설계 포인트**: 모든 업무 관계(배정·권한·그룹 등)는 외부 식별자가 아니라 **로컬 PK(`User.id`)**를 참조한다. 외부 ID는 인증 연결용 필드일 뿐.

### 5.3 업무 컨텍스트 예시 (Project / Assignment)

- **애그리거트 루트**: `Project`. **VO**: `ProjectCode`(unique), `ProjectStatus`(상태 머신), `DateRange`.
- 타 컨텍스트 참조(예: PM)는 `userId`로만.
- **설계 갈림길 — 배정(Assignment)을 어디에 둘 것인가?**
  - *Project 애그리거트 내부 엔티티로*: "동일 직원 중복 배정 금지", "투입률 합계 제약" 같은 불변식을 프로젝트 단위로 강제해야 하면 내부에 둔다.
  - *별도 애그리거트로*: 배정이 대량이고 독립적으로 조회/수정되면 분리하고 양쪽을 ID로 참조한다.
  - VO: `ProjectRole`, `AllocationRate`(0–100 검증), `DateRange`.
- 이력 보존이 필요하면 이력 테이블을 분리(읽기 모델 측)하고 도메인 이벤트로 적재한다.

### 5.4 인가 컨텍스트 (Authorization) — 가장 풍부한 도메인

3계층(전역 역할 / 메뉴 권한 / 리소스 권한) + 그룹 + 계층 상속을 다루는 패턴.

- **작은 애그리거트로 쪼갠다**: `MenuGrant`, `ResourceGrant`, `Group`, `ResourceHierarchy`. **전체 권한 그래프를 하나의 거대한 애그리거트로 만들지 않는다.**
- **권한 판정은 도메인 서비스**: `PermissionEvaluator.evaluate(userId, resource, action)` = (직접 부여) ∪ (소속 그룹) ∪ (상위 리소스 상속). 상속·역할 함의(예: EDITOR ⊇ VIEW) 규칙을 여기에 캡슐화.
- **상속/목록 조회는 읽기 모델(MyBatis)**: 계층 상속(재귀 CTE), "현재 사용자가 접근 가능한 리소스 목록(ListObjects)"은 읽기 측에서 처리.
- **인가 구현 = 포트/어댑터(A-5)**: 도메인/애플리케이션은 `AuthorizationPort` 인터페이스만 안다. 구현은
  - 어댑터 1: 앱 내부(Postgres 권한 테이블 + 재귀 CTE)
  - 어댑터 2: 외부 엔진(예: OpenFGA, 관계 튜플)
  를 교체 가능하게 둔다. 외부 엔진을 쓰면 **부패 방지 계층(ACL)**으로 감싸 도메인이 외부 모델에 오염되지 않게 한다.
- **미로그인 사용자 권한**: 권한은 ID(로컬 PK) 기준으로 저장되므로, 미로그인(INVITED) 상태에서 부여해도 저장되고 로그인 시 자동 효력 발생.

### 5.5 그룹 (Group)

- 외부 IdP 그룹 동기화가 아니라 **앱 자체 관리**가 기본(통제·단순성).
- **애그리거트**: `Group` + `GroupMember`(다대다, ID 참조). 메뉴/리소스 권한 부여 대상이 될 수 있다.

---

## 6. 비기능 요구 베이스라인

| ID | 요구사항 |
|------|----------|
| NFR-01 | **배포**: 환경 비종속(온프레미스/클라우드). 외부 의존 최소화 |
| NFR-02 | **보안**: BFF 패턴(토큰 비노출). 서버 간 호출은 별도 자격증명으로 보호 |
| NFR-03 | **외부 권한 최소화**: 전체 디렉터리 동기화 지양, 위임 권한 범위로 한정(관리자 동의 불필요) |
| NFR-04 | **동시성**: Java 21 가상 스레드로 처리량 확보 |
| NFR-05 | **데이터 정합성**: 쓰기 트랜잭션 정합성 보장. 외부 엔진 사용 시 동기화 정책 명시 |
| NFR-06 | **스키마 관리**: Flyway 버전 관리 |
| NFR-07 | **확장성 트립와이어**: 특정 영역 복잡도가 한계 초과 시 전용 엔진 도입 재검토 |

---

## 7. 신규 프로젝트 적용 체크리스트

1. **컨텍스트 식별** — 이 시스템의 바운디드 컨텍스트를 적는다. (4.1)
2. **컨텍스트별 유비쿼터스 언어** — 핵심 용어 사전을 만든다.
3. **각 컨텍스트의 애그리거트·VO·불변식**을 먼저 설계한다 (테이블 X). (4.3, 5장)
4. **패키지 구조**를 컨텍스트×계층으로 만든다. (4.2)
5. **JPA 방식 선택**(A/B)을 컨텍스트별로 결정한다. (4.4)
6. **읽기 모델 분리** — 복잡 조회는 MyBatis 쿼리 서비스로. (4.5)
7. **인증 필요 시** BFF + OIDC + Redis, 식별자 매칭 정책(A-6) 적용. (5.1, 5.2)
8. **인가 필요 시** 포트/어댑터로 두고 내부 구현부터 시작. (5.4)
9. **트랜잭션/이벤트 경계**를 명시한다. (4.6)
10. **Flyway** 마이그레이션으로 스키마 버전 관리.

---

## 부록: 미결정 시 자주 마주치는 갈림길

| 항목 | 선택지 | 영향 |
|------|--------|------|
| 인가 구현 | 앱 내부(서버 추가 없음, 재귀 CTE) ↔ 외부 엔진(선언적 모델·ListObjects, dual-write 부담) | A-5의 어댑터로 흡수 |
| 미등록 사용자 로그인 | 거부(allowlist) ↔ 자동 생성(권한 0) | 보안 게이트·온보딩 정책 |
| 리소스 역할 모델 | 역할이 액션 묶음 함의(EDITOR=VIEW+EDIT) ↔ 액션 직접 부여 | 권한 모델 구조 |
| 배정 위치 | Project 애그리거트 내부 ↔ 별도 애그리거트 | 정합성 경계 (5.3) |
| 이력 보존 | 현재 상태만 ↔ 이력 테이블 분리 | 데이터/읽기 모델 |
