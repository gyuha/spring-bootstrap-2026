---
phase: 04-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V3__create_authorization.sql
  - src/main/java/com/anchors/baseline/authorization/domain/model/RoleName.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/Action.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/ResourceId.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/MenuId.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/GlobalRoleGrant.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/MenuGrant.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/ResourceGrant.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/Group.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/GroupMember.java
  - src/main/java/com/anchors/baseline/authorization/domain/model/ResourceHierarchy.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/GlobalRoleGrantRepository.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/MenuGrantRepository.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/ResourceGrantRepository.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/GroupRepository.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/GroupMemberRepository.java
  - src/main/java/com/anchors/baseline/authorization/domain/repository/ResourceHierarchyRepository.java
  - src/main/java/com/anchors/baseline/authorization/application/EffectiveGrantDto.java
  - src/main/java/com/anchors/baseline/authorization/application/PermissionReadPort.java
  - src/main/java/com/anchors/baseline/authorization/application/AuthorizationPort.java
  - src/main/java/com/anchors/baseline/authorization/application/PermissionEvaluator.java
autonomous: true
requirements: [AUTHZ-01, AUTHZ-02, AUTHZ-03, AUTHZ-04, AUTHZ-05, AUTHZ-06, AUTHZ-08]

must_haves:
  truths:
    - "authorization 컨텍스트의 4계층 패키지가 존재하고 ArchitectureTest가 0 위반으로 통과한다"
    - "권한 부여/회수가 작은 JPA 애그리거트 6종으로 표현된다(거대 단일 그래프 아님)"
    - "PermissionEvaluator가 application 계층에 있고 PermissionReadPort를 주입받아 역할 함의를 적용한다"
    - "V3 Flyway 스키마가 도메인 모델과 일치하며 user_id로의 물리 FK가 없다(soft reference)"
  artifacts:
    - path: "src/main/resources/db/migration/V3__create_authorization.sql"
      provides: "3계층 권한 스키마(roles/menu/resource grants, group, group_members, resource_hierarchy)"
      contains: "CREATE TABLE"
    - path: "src/main/java/com/anchors/baseline/authorization/application/PermissionEvaluator.java"
      provides: "순수 도메인 판정 서비스(evaluate + 역할 함의), application 계층 배치(ArchUnit Q1)"
      contains: "evaluate"
    - path: "src/main/java/com/anchors/baseline/authorization/application/PermissionReadPort.java"
      provides: "재귀 CTE 읽기 포트(findEffectiveGrants/listAccessibleResourceIds)"
      contains: "interface PermissionReadPort"
    - path: "src/main/java/com/anchors/baseline/authorization/application/AuthorizationPort.java"
      provides: "evaluate/listObjects 추상 경계(SC#5)"
      contains: "interface AuthorizationPort"
    - path: "src/main/java/com/anchors/baseline/authorization/domain/model/GlobalRoleGrant.java"
      provides: "전역 역할 부여 애그리거트(AUTHZ-01)"
      contains: "@Entity"
  key_links:
    - from: "authorization/application/PermissionEvaluator.java"
      to: "authorization/application/PermissionReadPort.java"
      via: "생성자 주입"
      pattern: "PermissionReadPort"
    - from: "V3__create_authorization.sql"
      to: "authorization/domain/model"
      via: "ddl-auto: validate 정합(컬럼명·타입 일치)"
      pattern: "user_id"
---

<objective>
authorization 바운디드 컨텍스트의 **도메인 계층 + 계약(포트/DTO) + 판정 서비스 + Flyway V3 스키마**를 생성한다. 이 Plan은 타입을 먼저 정의해(types-first) Wave 2 인프라 어댑터와 Wave 3 테스트가 컴파일·구현할 계약을 제공한다.

Purpose: 정본 §5.4("가장 풍부한 도메인")의 worked example을 작은 애그리거트 6종 + 순수 판정 서비스로 구현하고, ArchUnit 게이트(lesson 01 P1)와 정합하는 계층 배치를 **신규 패키지 추가 시점부터 실증**한다.

Output: authorization.domain(애그리거트 6종·VO·쓰기 포트), authorization.application(PermissionEvaluator·PermissionReadPort·AuthorizationPort·DTO), V3 마이그레이션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-authorization/04-CONTEXT.md
@.planning/phases/04-authorization/04-RESEARCH.md
@spring-backend-ddd-baseline.md

<read_first>
- src/main/java/com/anchors/baseline/identity/domain/model/User.java — JPA 방식 A 엔티티 형판(@Entity, protected 무인자 생성자, @GeneratedValue(IDENTITY), 의미 메서드, public setter 금지). created_at 이중 소스 패턴(53-54행)은 **이번에 따르지 않는다** — 아래 D-09 결정 참조.
- src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java — 쓰기 포트 형판(domain, infrastructure import 금지).
- src/main/java/com/anchors/baseline/platform/application/SampleQuery.java — 읽기 포트 형판(application).
- src/main/java/com/anchors/baseline/platform/application/SampleDto.java — 읽기 DTO 형판(@Data @NoArgsConstructor @AllArgsConstructor 평탄 클래스, record 아님 — MyBatis 무인자 매핑).
- src/main/resources/db/migration/V2__create_identity_users.sql — Flyway DDL 형판(BIGSERIAL PK, TIMESTAMP WITH TIME ZONE, COMMENT). users 테이블은 이 컨텍스트가 user_id로 참조하나 FK는 걸지 않는다.
- src/test/java/com/anchors/baseline/architecture/ArchitectureTest.java — 결정적 게이트. Domain.mayNotAccessAnyLayer() / Application.mayOnlyAccessLayers("Domain"). 이 규칙이 신규 authorization 패키지에 ..domain.. 두 점 패턴으로 자동 적용된다.
- src/test/resources/application-test.yml — `ddl-auto: validate`. JPA 엔티티 컬럼이 V3 DDL과 다르면 ApplicationContext 부팅 실패(Phase 2 02-03-SUMMARY 실증). 엔티티 ↔ DDL 정합은 컴파일이 아니라 부팅이 강제한다.
</read_first>

<interfaces>
ArchUnit 게이트 규칙(커밋됨, 위반 시 빌드 RED):
- Domain → 어떤 레이어도 의존 불가 (mayNotAccessAnyLayer)
- Application → Domain 만 (mayOnlyAccessLayers "Domain")
- Infrastructure → Domain, Application
- Interfaces → Application, Domain

귀결(Open Q1 해소): PermissionEvaluator가 PermissionReadPort(application 포트)를 주입받아야 하므로 **PermissionEvaluator는 application 계층**에 둔다. domain에 두면 application 포트를 주입할 수 없어 빌드 RED. 정본은 "도메인 서비스"라 부르나, 게이트 정합상 application 배치가 유일한 해(Phase 1 SampleQuery가 application인 것과 동형).

소비할 Identity 계약(userId만, FK 없음):
- com.anchors.baseline.identity.domain.model.User.id : Long (불변 로컬 PK)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V3 권한 스키마 (도메인 모델에서 역산)</name>
  <files>src/main/resources/db/migration/V3__create_authorization.sql</files>
  <action>
정본 §5.4·§5.5 + AUTHZ-01~05를 도메인 모델로 먼저 설계한 뒤(테이블 우선 금지 — PROJECT Constraints) 그 모델을 영속화하는 V3 마이그레이션을 작성한다. V2 형판 규약 계승: 모든 PK는 BIGSERIAL, created_at은 TIMESTAMP WITH TIME ZONE NOT NULL(단 D-09에 따라 DDL DEFAULT now()는 생략 — 엔티티 Instant.now() 단일 소스), 각 테이블에 COMMENT.

테이블(주체 표현은 Open Q2 해소 = polymorphic 단일 테이블 + XOR CHECK):
- global_role_grants(id, user_id BIGINT NOT NULL, role VARCHAR NOT NULL, created_at) — AUTHZ-01. (user_id, role) UNIQUE로 중복 부여 방지.
- menu_grants(id, user_id BIGINT NULL, group_id BIGINT NULL, menu_id BIGINT NOT NULL, role VARCHAR NOT NULL, created_at) — AUTHZ-02. CHECK ((user_id IS NOT NULL) <> (group_id IS NOT NULL)) 로 주체 XOR 강제.
- resource_grants(id, user_id BIGINT NULL, group_id BIGINT NULL, resource_id BIGINT NOT NULL, role VARCHAR NOT NULL, created_at) — AUTHZ-03·06. 동일 XOR CHECK.
- groups(id, name VARCHAR NOT NULL UNIQUE, created_at) — AUTHZ-04·§5.5. (예약어 충돌 주의: 테이블명 groups는 PG에서 따옴표 없이 사용 가능하나 컬럼/별칭에서 주의.)
- group_members(id, group_id BIGINT NOT NULL, user_id BIGINT NOT NULL, created_at) — AUTHZ-04. (group_id, user_id) UNIQUE.
- resource_hierarchy(id, resource_id BIGINT NOT NULL, parent_resource_id BIGINT NULL, created_at) — AUTHZ-05. (resource_id) UNIQUE(한 리소스의 부모는 0~1개). 재귀 CTE 대상.

**user_id / group_id / resource_id / menu_id 어느 것도 다른 테이블로 물리 FK를 걸지 않는다**(D-07 soft reference — identity users 및 컨텍스트 내 결합 회피, §4.1). DDL 상단 주석에 "soft reference: no physical FK (§4.1 / D-07)"를 명시한다. evaluate/listObjects 조회 성능을 위해 user_id·group_id·resource_id에 인덱스를 추가한다.
  </action>
  <verify>
    <automated>./gradlew flywayInfo 2>/dev/null || grep -L '\${' src/main/resources/db/migration/V3__create_authorization.sql</automated>
  </verify>
  <done>V3 SQL 파일이 6개 테이블을 BIGSERIAL PK·TIMESTAMP WITH TIME ZONE·COMMENT로 정의하고, 어떤 컬럼에도 REFERENCES(물리 FK)가 없으며, grants 테이블에 XOR CHECK가 있다. created_at에 DDL DEFAULT now()가 없다(D-09 단일 소스).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 도메인 계층 — VO·애그리거트 6종·쓰기 포트 6종</name>
  <files>src/main/java/com/anchors/baseline/authorization/domain/model/RoleName.java, src/main/java/com/anchors/baseline/authorization/domain/model/Action.java, src/main/java/com/anchors/baseline/authorization/domain/model/ResourceId.java, src/main/java/com/anchors/baseline/authorization/domain/model/MenuId.java, src/main/java/com/anchors/baseline/authorization/domain/model/GlobalRoleGrant.java, src/main/java/com/anchors/baseline/authorization/domain/model/MenuGrant.java, src/main/java/com/anchors/baseline/authorization/domain/model/ResourceGrant.java, src/main/java/com/anchors/baseline/authorization/domain/model/Group.java, src/main/java/com/anchors/baseline/authorization/domain/model/GroupMember.java, src/main/java/com/anchors/baseline/authorization/domain/model/ResourceHierarchy.java, src/main/java/com/anchors/baseline/authorization/domain/repository/GlobalRoleGrantRepository.java, src/main/java/com/anchors/baseline/authorization/domain/repository/MenuGrantRepository.java, src/main/java/com/anchors/baseline/authorization/domain/repository/ResourceGrantRepository.java, src/main/java/com/anchors/baseline/authorization/domain/repository/GroupRepository.java, src/main/java/com/anchors/baseline/authorization/domain/repository/GroupMemberRepository.java, src/main/java/com/anchors/baseline/authorization/domain/repository/ResourceHierarchyRepository.java</files>
  <behavior>
    - GlobalRoleGrant.of(userId, RoleName) 으로 생성하면 INVITED/ACTIVE 무관하게 userId·role을 보유하고 created_at이 엔티티 소유(Instant.now())로 설정된다(D-09 단일 소스).
    - MenuGrant / ResourceGrant 는 주체가 user XOR group 임을 정적 팩토리(forUser / forGroup)로 구조적으로 강제한다(둘 다 받는 생성자 미노출).
    - VO(RoleName/Action/ResourceId/MenuId)는 null·빈 값 입력을 거부한다.
  </behavior>
  <action>
User.java 형판을 따라 JPA 방식 A 엔티티 6종을 작성한다: @Entity, @Table(name=...), @Id @GeneratedValue(IDENTITY), protected 무인자 생성자, public setter 금지, 정적 팩토리 + 의미 생성자. created_at은 **엔티티 소유 단일 소스**(`private Instant createdAt = Instant.now();`, @Column(updatable=false)) — DDL에는 DEFAULT를 두지 않는다(D-09: lesson 02 P2 이중 소스 패턴 회피, 신규 컨텍스트를 단일화 시작점으로). 컬럼명·nullable·타입을 Task 1 V3 DDL과 정확히 일치시킨다(ddl-auto: validate가 부팅 시 강제).

주체 polymorphic 표현: MenuGrant·ResourceGrant는 user_id·group_id를 nullable Long으로 두되, 정적 팩토리 `forUser(userId, ...)` / `forGroup(groupId, ...)`로만 생성해 XOR 불변식을 코드에서 강제한다(DDL CHECK는 DB 측 최종 방어). Group/GroupMember/ResourceHierarchy도 동형으로 작성한다.

VO(RoleName/Action/ResourceId/MenuId): RoleName·Action은 문자열 래퍼(또는 enum — 역할 함의를 enum에 두려면 RoleName을 enum으로). **역할 함의(EDITOR ⊇ VIEW)는 이 Plan에서는 표현만 두고 적용 로직은 Task 3 PermissionEvaluator에 둔다.** ResourceId/MenuId는 Long 래퍼 VO. 생성자에서 null·공백 검증.

쓰기 포트 6종(domain/repository): UserRepository 형판대로 인터페이스만 정의(infrastructure import 금지). 각 포트에 save·revoke(deleteBy...)·조회 시그니처를 둔다. 예: GlobalRoleGrantRepository.save(grant) / deleteByUserIdAndRole(userId, role) / findByUserId(userId). GroupMemberRepository.deleteByGroupIdAndUserId(...). ResourceHierarchyRepository.save(...).

역할 함의 모델(Open Q3 of D-05 = Claude 재량): RoleName을 enum {VIEWER, EDITOR, ADMIN}으로 두고 각 역할이 함의하는 Action 집합(EDITOR ⊇ VIEWER의 액션)을 enum 메서드 `implies(Action)`로 표현한다. 이는 domain VO에 캡슐화(Spring 타입 미참조).
  </action>
  <verify>
    <automated>./gradlew compileJava --tests none 2>&1 | tail -5; ./gradlew compileJava</automated>
  </verify>
  <done>6개 @Entity + 4개 VO + 6개 쓰기 포트가 컴파일된다. 각 엔티티의 컬럼 매핑이 V3 DDL과 일치하고 created_at이 엔티티 단일 소스다. domain 패키지의 어떤 파일도 infrastructure/application/Spring Security를 import하지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: application 계약 — DTO·읽기 포트·AuthorizationPort·PermissionEvaluator</name>
  <files>src/main/java/com/anchors/baseline/authorization/application/EffectiveGrantDto.java, src/main/java/com/anchors/baseline/authorization/application/PermissionReadPort.java, src/main/java/com/anchors/baseline/authorization/application/AuthorizationPort.java, src/main/java/com/anchors/baseline/authorization/application/PermissionEvaluator.java</files>
  <behavior>
    - PermissionEvaluator.evaluate(userId, ResourceId, Action) 는 readPort.findEffectiveGrants(...)가 빈 리스트를 반환하면 false(default-deny — ASVS V4 / T-04-04).
    - 반환된 raw grant 중 하나라도 역할 함의로 요청 Action을 함의하면 true(직접 ∪ 그룹 ∪ 상속은 SQL이 이미 합집합으로 모으고, 도메인은 역할 함의만 적용 — D-05).
    - EDITOR 역할 grant + VIEW action 요청 → true(EDITOR ⊇ VIEW). VIEWER grant + EDIT action → false.
  </behavior>
  <action>
EffectiveGrantDto: SampleDto 형판(@Data @NoArgsConstructor @AllArgsConstructor 평탄 클래스, record 아님 — MyBatis 무인자 매핑, Pitfall 5). 필드는 평탄 프로젝션 — `role`(String), `resourceId`(Long), `source`(String: DIRECT/GROUP/INHERITED 등 선택). map-underscore-to-camel-case가 전역 설정이라 resource_id→resourceId 자동.

PermissionReadPort(application 읽기 포트, SampleQuery 형판): `List<EffectiveGrantDto> findEffectiveGrants(@Param("userId") long userId, @Param("resourceId") long resourceId)` — 직접 ∪ 그룹 ∪ 조상 리소스 상속을 한 번에 모으는 재귀 CTE 결과(SQL은 Wave 2 @Mapper). `List<Long> listAccessibleResourceIds(@Param("userId") long userId, @Param("action") String action)` — ListObjects(AUTHZ-07).

AuthorizationPort(application, SC#5/AUTHZ-08): `boolean evaluate(long userId, ResourceId resource, Action action)` + `List<Long> listObjects(long userId, Action action)`. 부여/회수는 포트에 넣지 않는다(D-04 — 외부 엔진 교체 동기는 판정 엔진이지 애그리거트 쓰기가 아님).

PermissionEvaluator(application, Open Q1 해소 — domain 아님): 생성자로 PermissionReadPort 주입(@RequiredArgsConstructor 또는 명시 생성자). `boolean evaluate(long userId, ResourceId resource, Action action)`: readPort.findEffectiveGrants 호출 → 결과가 비면 false(default-deny) → 각 grant의 RoleName.implies(action) 적용해 anyMatch. **Spring Security org.springframework.security.access.PermissionEvaluator를 import하거나 implements하지 않는다**(D-03 최대 함정 — Pitfall 2; Spring 메서드는 hasPermission이지 evaluate가 아니며 도메인 오염 금지). @Service로 등록(application 빈).
  </action>
  <verify>
    <automated>./gradlew compileJava && ./gradlew test --tests "*ArchitectureTest*"</automated>
  </verify>
  <done>EffectiveGrantDto·PermissionReadPort·AuthorizationPort·PermissionEvaluator가 컴파일되고, ArchitectureTest가 0 위반으로 통과한다(신규 authorization 패키지 계층 정합 실증 — lesson 01 P1). PermissionEvaluator 어디에도 org.springframework.security import가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → PermissionEvaluator.evaluate | 신뢰되지 않은 userId/resource/action 입력이 판정 경계를 넘는다 |
| application 포트 → DB(MyBatis CTE) | 파라미터가 SQL로 전달되는 경계(Wave 2에서 실제 SQL) |
| authorization → identity(user_id) | 크로스 컨텍스트 soft reference(FK 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | PermissionEvaluator.evaluate | mitigate | grant 없으면 false 반환(default-deny). Wave 3 진리표 "없음" 케이스로 단언 |
| T-04-02 | Elevation of Privilege | 도메인 vs Spring PermissionEvaluator 혼동 | mitigate | 도메인 서비스가 Spring 인터페이스 미구현(D-03). ArchUnit이 domain→framework RED, import 0건 단언 |
| T-04-03 | Tampering | 주체 XOR 위반(user_id+group_id 동시) | mitigate | 정적 팩토리 forUser/forGroup만 노출(코드) + DDL CHECK XOR(DB 최종 방어) |
| T-04-04 | Elevation of Privilege | 권한 상승(상속 오버리치) | mitigate | resource_hierarchy는 조상 방향만 정의(parent_resource_id), 재귀 SQL은 Wave 2. 진리표로 Wave 3 단언 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 없음(D-11) — slopcheck 대상 없음 |
</threat_model>

<verification>
- `./gradlew compileJava` 성공 (신규 authorization 패키지 컴파일)
- `./gradlew test --tests "*ArchitectureTest*"` GREEN (계층 정합 — PermissionEvaluator application 배치 실증, lesson 01 P1)
- V3 SQL에 물리 FK(REFERENCES) 0건, `${` (MyBatis 동적 SQL 위험) 0건
</verification>

<success_criteria>
- authorization.{domain,application} 계층이 존재하고 ArchitectureTest 0 위반
- 작은 애그리거트 6종 + VO 4종 + 쓰기 포트 6종 + 읽기 포트 + AuthorizationPort + PermissionEvaluator 컴파일
- V3 마이그레이션이 도메인 모델과 정합(컬럼·타입·nullable), 물리 FK 없음, created_at 단일 소스
- PermissionEvaluator가 application에 있고 Spring Security 타입 미참조(D-03)
</success_criteria>

<output>
Create `.planning/phases/04-authorization/04-01-SUMMARY.md` when done
</output>
