---
phase: 03-bff-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle/libs.versions.toml
  - src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java
  - src/main/java/com/anchors/baseline/identity/infrastructure/jpa/UserJpaRepository.java
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "spring-boot-starter-oauth2-client·spring-session-data-redis가 클래스패스에 들어가 ./gradlew compileJava가 통과한다"
    - "spring-security-test·WireMock이 test 스코프에 추가되어 테스트 컴파일이 가능하다"
    - "identity.domain.repository.UserRepository에 findByEmail(Email)이 존재해 최초 로그인 매칭(D-02 2단계)이 가능해진다"
  artifacts:
    - path: "build.gradle.kts"
      provides: "oauth2-client·spring-session-data-redis(impl), spring-security-test·wiremock(test) 의존성"
      contains: "spring-boot-starter-oauth2-client"
    - path: "gradle/libs.versions.toml"
      provides: "wiremock 버전·좌표 카탈로그 항목"
      contains: "wiremock"
    - path: "src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java"
      provides: "findByEmail(Email) 포트 메서드"
      contains: "findByEmail"
  key_links:
    - from: "build.gradle.kts"
      to: "org.springframework.session:spring-session-data-redis"
      via: "implementation 의존성 (BOM 관리, version 생략)"
      pattern: "spring-session-data-redis"
    - from: "UserJpaRepository"
      to: "UserRepository.findByEmail(Email)"
      via: "Spring Data JPA 파생 쿼리 (email.value 컬럼)"
      pattern: "extends JpaRepository.*UserRepository"
---

<objective>
Phase 3 인증 구현의 토대를 깐다: (1) BFF 인증/세션에 필요한 Spring 공식 스타터 의존성을 추가하고, (2) 최초 로그인 매칭(D-02 2단계)에 필요한 `findByEmail(Email)` 포트 메서드를 Identity 컨텍스트에 최소 보강한다.

Purpose: Wave 2(auth 컨텍스트 구현)와 Wave 3(통합 테스트)이 컴파일되려면 의존성과 포트가 먼저 존재해야 한다. 선형 wave로 전방 참조 컴파일 위험을 구조적으로 제거한다(lesson 01/02 패턴 계승).
Output: 갱신된 build.gradle.kts·libs.versions.toml, findByEmail이 추가된 UserRepository 포트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bff-auth/03-CONTEXT.md
@.planning/phases/03-bff-auth/03-RESEARCH.md

<interfaces>
<!-- Wave 2/3가 의존할 계약. 코드베이스에서 추출. 탐색 불필요. -->

기존 UserRepository 포트 (src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java):
```
User save(User user);
Optional<User> findById(Long id);
boolean existsByEmail(Email email);
Optional<User> findByExternalId(String externalId);
```
→ 이 플랜이 `Optional<User> findByEmail(Email email)` 를 추가한다.

기존 UserJpaRepository 어댑터 (src/main/java/.../identity/infrastructure/jpa/UserJpaRepository.java):
```
public interface UserJpaRepository extends JpaRepository<User, Long>, UserRepository {}
```
→ existsByEmail(Email)이 이미 파생 쿼리로 정상 해석됨(Phase 2 lesson, Pitfall 6 미발현). findByEmail도 동형으로 동작 예상.

Email VO: @Embeddable, 필드 value(@Column name="email"), 생성자 형식 검증. getValue()로만 노출.

현재 build.gradle.kts dependencies: spring-boot-starter-web/data-jpa/data-redis/actuator, mybatis, springdoc, flyway, lombok, postgresql(runtime), test: spring-boot-starter-test/testcontainers/archunit. **security/oauth2/session 부재.**

현재 libs.versions.toml: spring-boot=3.5.3, testcontainers=1.21.3 등. BOM 3.5.3이 Security 6.5.1·Session 3.5.1 관리.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF 인증/세션 의존성 추가</name>
  <files>build.gradle.kts, gradle/libs.versions.toml</files>
  <read_first>
    - build.gradle.kts (dependencies 블록 현황)
    - gradle/libs.versions.toml ([versions]/[libraries] 패턴)
    - 03-RESEARCH.md §Standard Stack·Installation(좌표·BOM 관리 여부)
    - 03-RESEARCH.md §Package Legitimacy Audit (4개 Spring 공식 = Approved, WireMock만 [ASSUMED])
  </read_first>
  <action>
    build.gradle.kts dependencies 블록에 D-09 의존성을 추가한다. 메인: `implementation("org.springframework.boot:spring-boot-starter-oauth2-client")`, `implementation("org.springframework.session:spring-session-data-redis")` — 둘 다 Spring Boot BOM 3.5.3이 버전 관리하므로 version 생략. group이 `org.springframework.session`인 점 주의(`org.springframework.boot` 아님 — RESEARCH 실측). spring-boot-starter-security는 oauth2-client가 전이 포함하므로 명시 추가하지 않는다(NFR-01 최소화). 테스트: `testImplementation("org.springframework.security:spring-security-test")`(BOM 관리, version 생략).

    WireMock(D-08 (b) — SC#1·SC#4 토큰 교환 흐름 검증용, test 스코프)은 BOM 관리 밖이므로 libs.versions.toml 카탈로그로 추가한다: [versions]에 `wiremock = "<verify>"`, [libraries]에 `wiremock-standalone = { module = "org.wiremock:wiremock-standalone", version.ref = "wiremock" }`. build.gradle.kts에 `testImplementation(libs.wiremock.standalone)`. **[ASSUMED] 좌표·버전 — install 전 Maven Central에서 org.wiremock:wiremock-standalone 최신 안정 버전을 verify해 핀한다**(Task 1 verify에서 좌표·버전 실측 후 기록). 그 외 인증 관련 외부 의존(커스텀 토큰 스토어 등) 추가 금지(NFR-01).

    기존 의존성·tasks.withType<Test>(dockerApiVersion 옵트인 가드)는 건드리지 않는다(surgical).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew dependencies --configuration testRuntimeClasspath 2>&1 | grep -E "spring-security-test|spring-session-data-redis|spring-security-oauth2-client|wiremock" && ./gradlew compileJava compileTestJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    의존성 트리에 oauth2-client·spring-session-data-redis·spring-security-test·wiremock이 해석되고, compileJava·compileTestJava가 통과한다(아직 새 코드 없음 — resolve만 확인). WireMock 좌표·버전이 Maven Central에서 실측되어 libs.versions.toml에 핀된다.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserRepository에 findByEmail(Email) 추가</name>
  <files>src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java, src/main/java/com/anchors/baseline/identity/infrastructure/jpa/UserJpaRepository.java</files>
  <read_first>
    - src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java (기존 포트 시그니처)
    - src/main/java/com/anchors/baseline/identity/infrastructure/jpa/UserJpaRepository.java (어댑터 — 파생 쿼리 자동 구현 방식)
    - src/main/java/com/anchors/baseline/identity/domain/model/Email.java (@Embeddable, value 필드)
    - 03-RESEARCH.md §Pitfall 6 (findByEmail 파생 쿼리 해석 — existsByEmail과 동형, 깨지면 @Query fallback)
    - .planning/lessons/02-2026-05-31.md (existsByEmail @Embeddable 파생 쿼리가 런타임 정상 해석 — @Query override 불필요 실증)
  </read_first>
  <behavior>
    - findByEmail로 INVITED 사용자를 조회하면 Optional<User>가 반환된다(존재 시 present).
    - 없는 이메일은 Optional.empty()를 반환한다.
    - @Embeddable Email의 value가 email 컬럼으로 풀려 파생 쿼리가 부팅 시 PropertyReferenceException 없이 해석된다.
  </behavior>
  <action>
    `UserRepository` 포트에 `Optional<User> findByEmail(Email email);` 한 줄을 추가한다(D-03). `existsByEmail(Email)`과 동형 — Identity 컨텍스트 안에서 최소 보강(인증이 Identity 영속성을 직접 건드리지 않게 포트 경유). 도메인 순수 유지 — infrastructure import 금지.

    `UserJpaRepository`는 JpaRepository + UserRepository를 extends하므로 파생 쿼리로 자동 구현된다 — 별도 메서드 본문 불필요. existsByEmail이 이미 정상 해석되므로 findByEmail도 동작 예상(lesson 02 실증). 만약 부팅 시 PropertyReferenceException이 나면 `@Query("select u from User u where u.email.value = :#{#email.value}")` fallback을 UserJpaRepository에 명시(RESEARCH Pitfall 6). 파생 쿼리로 통과하면 fallback 불필요.

    IdentityApplicationService는 이 플랜에서 건드리지 않는다(브리지는 Wave 2 auth.application 소관 — Identity 서비스에 인증 흐름 침투 금지).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew test --tests "*UserLifecycleIT*" --tests "*ArchitectureTest*" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    findByEmail이 포트에 존재하고, 기존 Identity 통합 테스트(UserLifecycleIT)가 부팅·통과(파생 쿼리 해석 성공 = 컨텍스트 기동 = PropertyReferenceException 없음)하며, ArchitectureTest 계층 위반 0건이 유지된다.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 빌드 의존성 해석(Gradle → Maven Central) | 외부 아티팩트가 빌드에 진입하는 지점. 공급망 공격 표면 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | Maven 의존성(oauth2-client, session-data-redis, security-test, wiremock) | mitigate | Spring 공식 4종은 BOM 3.5.3 관리·Maven Central 실측 Approved(RESEARCH Audit). WireMock은 [ASSUMED] → Task 1 verify에서 좌표·버전 Maven Central 실측 후 핀. SLOP 없음 |
| T-03-01 | Information Disclosure | findByEmail 노출 | accept | 포트 내부 조회 메서드. 인증 브리지(Wave 2)만 호출, 외부 노출 경로 없음. 저위험 |
</threat_model>

<verification>
- `./gradlew compileJava compileTestJava` 통과 (의존성 해석 + 기존 코드 컴파일)
- `./gradlew test --tests "*UserLifecycleIT*" --tests "*ArchitectureTest*"` GREEN (findByEmail 파생 쿼리 해석 + 계층 위반 0)
- WireMock 좌표·버전이 libs.versions.toml에 실측 핀됨
</verification>

<success_criteria>
- D-09 의존성 4종이 클래스패스에 해석된다
- UserRepository.findByEmail(Email)이 존재하고 파생 쿼리로 정상 동작한다(Pitfall 6 미발현 또는 @Query fallback)
- ArchUnit 계층 게이트 위반 0건 유지(lesson 01 P1)
</success_criteria>

<output>
Create `.planning/phases/03-bff-auth/03-01-SUMMARY.md` when done
</output>
