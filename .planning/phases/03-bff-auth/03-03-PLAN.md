---
phase: 03-bff-auth
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/test/java/com/anchors/baseline/auth/BffAuthIT.java
  - src/test/java/com/anchors/baseline/auth/support/MockOidcServer.java
  - src/test/java/com/anchors/baseline/auth/BaselineOidcUserSerializationTest.java
  - src/test/resources/application-test.yml
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "SC#1: 로그인 응답 Set-Cookie에 SESSION만 있고 토큰 문자열이 없으며, Redis 세션 서버 측에 OAuth2AuthorizedClient(토큰)가 존재한다(WireMock 토큰 교환 경로)"
    - "SC#2: 쿠키 세션만으로 /api/me가 200을 반환한다(oidcLogin)"
    - "SC#3: 최초 로그인 시 linkIdentity가 호출되어 INVITED→ACTIVE 전이가 일어난다(Postgres Testcontainer)"
    - "SC#3 멱등: 재로그인(findByExternalId 매칭) 시 linkIdentity가 호출되지 않고 ACTIVE 유지된다"
    - "SC#4: 로그아웃 후 동일 세션 쿠키 재요청이 401/redirect로 인증 실패한다"
    - "BaselineOidcUser가 JDK 직렬화 라운드트립을 통과한다(Pitfall 2 회귀 가드)"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/auth/BffAuthIT.java"
      provides: "SC#1~4·AUTH-01~05 행위 단언 통합 테스트. AbstractIntegrationTest 상속"
      contains: "extends AbstractIntegrationTest"
    - path: "src/test/java/com/anchors/baseline/auth/support/MockOidcServer.java"
      provides: "WireMock OIDC IdP 스텁(discovery/token/jwks) — SC#1·SC#4 토큰 교환"
      contains: "WireMock"
    - path: "src/test/java/com/anchors/baseline/auth/BaselineOidcUserSerializationTest.java"
      provides: "BaselineOidcUser JDK 직렬화 라운드트립 가드(Pitfall 2)"
      contains: "Serializable"
    - path: "src/test/resources/application-test.yml"
      provides: "test 프로파일 OIDC registration/provider → WireMock issuer-uri 지정"
      contains: "oauth2"
  key_links:
    - from: "BffAuthIT"
      to: "MockOidcServer (WireMock)"
      via: "discovery/token/jwks 스텁으로 authorization-code 토큰 교환 검증"
      pattern: "MockOidcServer|wireMock|stubFor"
    - from: "BffAuthIT.tokenOnlyInRedisNotInCookie"
      to: "Set-Cookie SESSION만 + Redis 세션 토큰 존재"
      via: "응답 쿠키 단언 + OAuth2AuthorizedClientService/세션 조회"
      pattern: "SESSION|OAuth2AuthorizedClient"
    - from: "BffAuthIT.firstLoginLinksIdentity"
      to: "identity User INVITED→ACTIVE"
      via: "WireMock 로그인 후 DB 상태 단언 (Postgres TC)"
      pattern: "ACTIVE|linkIdentity"
---

<objective>
Phase 3의 모든 Success Criteria를 **행위로 증명**하는 통합 테스트를 작성한다(lesson 02 P1 — 말로만 주장 금지, 테스트로 단언). WireMock OIDC 스텁으로 토큰 교환 경로까지 실증(SC#1·SC#4)하고, oidcLogin()으로 인증 후 상태(SC#2·SC#3)를 검증한다. BaselineOidcUser 직렬화 라운드트립으로 Pitfall 2 회귀를 가드한다.

Purpose: SC#1(토큰 Redis-only)이 baseline 보안의 핵심(NFR-02). "토큰이 쿠키에 없고 Redis에만"을 WireMock 실제 로그인 흐름으로 증명하는 것이 이 Phase의 가장 중요한 검증. 선형 wave 마지막 — 모든 auth 타입이 이미 존재하므로 전방 참조 컴파일 위험 없음(lesson 01/02).
Output: BffAuthIT(SC#1~4), MockOidcServer(WireMock fixture), BaselineOidcUserSerializationTest, test 프로파일 OIDC 설정.

검증 충실도(RESEARCH Open Q1 결정 적용):
- SC#1(토큰 비노출)·SC#4(로그아웃)·AUTH-01(OIDC 로그인 성공): WireMock 토큰 교환 경로 — D-08 (b).
- SC#2(쿠키 인증 API)·SC#3(linkIdentity 호출·멱등): oidcLogin() — D-08 (a).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bff-auth/03-CONTEXT.md
@.planning/phases/03-bff-auth/03-RESEARCH.md
@.planning/phases/03-bff-auth/03-VALIDATION.md

<interfaces>
<!-- 테스트가 소비할 계약. 탐색 불필요. -->

테스트 베이스 (src/test/java/com/anchors/baseline/AbstractIntegrationTest.java):
```
@SpringBootTest(webEnvironment = RANDOM_PORT) @ActiveProfiles("test")
abstract class AbstractIntegrationTest {
  // 싱글턴 Testcontainers: PostgreSQLContainer postgres("postgres:16") @ServiceConnection
  //                       GenericContainer redis("redis:7") @ServiceConnection(name="redis")
}
```

spring-security-test(03-01 추가) — oidcLogin/logout/csrf:
```
import static org.springframework.security.test.web.servlet.request.SecurityMockMvcRequestPostProcessors.*;
mvc.perform(get("/api/me").with(oidcLogin().idToken(t -> t.subject("oid-123").claim("email","invited@x.com")))).andExpect(status().isOk());
mvc.perform(post("/logout").with(oidcLogin()).with(csrf()));
```

03-02 산출물:
- /api/me (보호) → {"userId": <로컬 User.id>}
- BaselineOidcUser implements OidcUser, Serializable; getLocalUserId():Long
- SecurityFilterChain: oauth2Login + logout(invalidateHttpSession,deleteCookies SESSION) + csrf + /api/** 401 entrypoint
- IdentityLinkService.resolveAndLink — 2단계 매칭(findByExternalId→findByEmail), 미초대 OAuth2AuthenticationException

identity (검증 대상): IdentityApplicationService.invite(emailValue):Long (INVITED 사용자 생성용). User.getStatus() → UserStatus.{INVITED,ACTIVE}. UserRepository.findByExternalId/findByEmail.

WireMock(03-01 추가, test 스코프): org.wiremock:wiremock-standalone. OIDC IdP 스텁 = discovery(/.well-known/openid-configuration) + token(/token) + jwks(/jwks). issuer-uri를 WireMock baseUrl로 application-test.yml에서 지정.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WireMock OIDC 스텁 + test 프로파일 OIDC 설정</name>
  <files>src/test/java/com/anchors/baseline/auth/support/MockOidcServer.java, src/test/resources/application-test.yml</files>
  <read_first>
    - 03-RESEARCH.md §Standard Stack(WireMock 용도), §Architecture Diagram(discovery/token/jwks 흐름), Open Q1(SC#1 (b) 수준)
    - 03-CONTEXT.md D-08(mock OIDC 전략)
    - src/test/resources/application-test.yml (기존 test 프로파일 — jpa/flyway/management)
    - src/test/java/com/anchors/baseline/AbstractIntegrationTest.java (RANDOM_PORT, test 프로파일)
    - Context7로 WireMock(현행 org.wiremock) OIDC 스텁·JWT 서명 jwks 구성 최신 API 확인
  </read_first>
  <action>
    `auth/support/MockOidcServer.java`: WireMock 서버를 띄워 OIDC IdP를 모킹한다. 스텁 3종 — (1) discovery `/.well-known/openid-configuration`(issuer·authorization_endpoint·token_endpoint·jwks_uri 반환), (2) token `/token`(authorization-code→id_token+access_token+refresh_token JSON 반환, id_token은 jwks 키로 서명한 JWT — subject(oid)·email claim 포함), (3) jwks `/jwks`(서명 검증용 공개키). RSA 키쌍 생성→jwks 노출→id_token 서명이 핵심(Spring Security가 jwks로 id_token 검증). WireMock baseUrl을 Spring에 주입할 수 있게 정적 접근자 또는 @DynamicPropertySource 연동 훅 제공.

    `application-test.yml`: `spring.security.oauth2.client.registration.test-idp`(client-id/secret 더미, redirect-uri, scope openid·email) + `provider.test-idp.issuer-uri`를 WireMock baseUrl로 지정한다. RANDOM_PORT라 issuer-uri는 @DynamicPropertySource로 런타임 주입(WireMock 포트가 동적)하거나 고정 포트 WireMock 사용 — 동적 주입 권장. `spring.session.store-type: redis` 유지. 기존 test 프로파일 jpa/flyway 블록 surgical 보존.

    test 전용 fixture이므로 src/test 아래에만 둔다(메인 코드 오염 금지, NFR-01).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew compileTestJava 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    MockOidcServer가 컴파일되고 discovery/token/jwks 스텁과 id_token 서명(jwks) 구성을 제공한다. application-test.yml에 test-idp registration/provider(issuer-uri→WireMock)가 설정된다. 컴파일 통과.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BffAuthIT — SC#1~4·AUTH-01~05 행위 단언</name>
  <files>src/test/java/com/anchors/baseline/auth/BffAuthIT.java</files>
  <read_first>
    - 03-VALIDATION.md §Per-Task Verification Map (6개 테스트명·단언 내용)
    - 03-RESEARCH.md §Code Examples(oidcLogin 테스트), §Pitfall 4(로그아웃 행위 단언), Open Q1(WireMock vs oidcLogin 매핑)
    - .planning/lessons/02-2026-05-31.md (SC를 행위로 단언, UNIQUE 동시성 같은 누락 경로 주의)
    - Task 1 산출물 MockOidcServer
    - src/test/java/com/anchors/baseline/AbstractIntegrationTest.java (상속)
    - src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java (기존 통합 테스트 패턴 — invite 호출·DB 단언 스타일)
  </read_first>
  <action>
    `auth/BffAuthIT.java`: `extends AbstractIntegrationTest`. MockMvc 또는 TestRestTemplate(RANDOM_PORT) + WireMock(MockOidcServer) + 주입된 UserRepository/IdentityApplicationService/OAuth2AuthorizedClientService. 03-VALIDATION 6개 단언을 1:1 구현 —

    - `oidcLoginFlowSucceeds`(AUTH-01): WireMock IdP로 authorization-code→callback 흐름을 태워 인증 성공으로 귀결(세션 생성)함을 단언.
    - `tokenOnlyInRedisNotInCookie`(AUTH-02/SC#1 — 핵심): WireMock 실제 로그인 흐름 수행 → 응답 `Set-Cookie`에 `SESSION`만 있고 access/refresh 토큰 문자열이 없음을 단언 + OAuth2AuthorizedClientService(또는 Redis 세션 attribute) 조회로 토큰이 **서버 측 세션에 존재**함을 단언. "쿠키엔 세션ID만, 토큰은 Redis에만"을 행위로 증명.
    - `cookieSessionAuthenticatesApi`(AUTH-03/SC#2): oidcLogin()으로 인증된 세션 쿠키만으로 `/api/me`가 200을 반환함을 단언.
    - `logoutInvalidatesSession`(AUTH-04/SC#4): 로그인(WireMock 또는 oidcLogin)→/api/me 200→`POST /logout`(+csrf)→**동일 세션 쿠키 재요청 시 401/redirect** 단언. Redis 세션 삭제 확인(선택: OAuth2AuthorizedClientService 부재 단언). Pitfall 4 — 로그아웃 후 /api/me가 200이면 실패.
    - `firstLoginLinksIdentity`(AUTH-05/SC#3): invite로 INVITED 사용자 선생성 → WireMock 로그인(해당 email·oid) → DB에서 해당 User가 ACTIVE이고 externalId=oid임을 단언(INVITED→ACTIVE, linkIdentity 호출됨).
    - `reLoginIsIdempotent`(AUTH-05 멱등): 이미 ACTIVE(externalId 연결)인 사용자가 재로그인 → linkIdentity 미호출·ACTIVE 유지·예외 없음 단언(findByExternalId 매칭 분기, D-02 1단계).

    추가 가드(lesson 02 누락 경로 주의): 미초대 사용자 로그인 거부(D-04) 단언을 1건 포함 권장 — INVITED 없는 email로 WireMock 로그인 시 인증 실패. WireMock 흐름이 jwks 서명 검증·nonce/state로 까다로우면, SC#1/SC#4/AUTH-01만 WireMock으로 하고 나머지는 oidcLogin()으로 분리(Open Q1 매핑). 단 SC#1의 "토큰 Redis-only"는 반드시 WireMock 경로(oidcLogin은 토큰 교환 우회라 SC#1 증명 불충분).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew test --tests "*BffAuthIT*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    BffAuthIT 6개(+미초대 거부 1) 테스트가 모두 GREEN. SC#1은 WireMock 경로로 쿠키-토큰-부재 + Redis-토큰-존재를 단언, SC#4는 로그아웃 후 401, SC#3은 INVITED→ACTIVE DB 단언, 멱등 분기 단언. 실 브라우저·실 IdP·실 포트 미사용(Testcontainers+WireMock만).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BaselineOidcUser 직렬화 라운드트립 가드 + 전체 스위트 GREEN</name>
  <files>src/test/java/com/anchors/baseline/auth/BaselineOidcUserSerializationTest.java</files>
  <read_first>
    - 03-RESEARCH.md §Pitfall 1(JDK 직렬화 유지), §Pitfall 2(BaselineOidcUser Serializable 필수)
    - src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUser.java (03-02 산출물)
  </read_first>
  <action>
    `auth/BaselineOidcUserSerializationTest.java`: BaselineOidcUser 인스턴스를 JDK `ObjectOutputStream`/`ObjectInputStream`로 직렬화→역직렬화 라운드트립하고 getLocalUserId() 등 핵심 필드가 보존됨을 단언(Pitfall 2 회귀 가드 — Redis 세션 JDK 직렬화 경로 모사). 단위 테스트(컨텍스트 불필요)면 빠르게. delegate OidcUser가 Serializable 호환인지도 라운드트립이 증명. NotSerializableException이 나면 03-02 BaselineOidcUser 필드 점검 필요(피드백).

    완료 후 전체 스위트 GREEN을 확인한다(Phase gate).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    BaselineOidcUser JDK 직렬화 라운드트립이 통과(localUserId 보존, NotSerializableException 없음). `./gradlew test` 전체 스위트 GREEN(Phase 1·2 기존 테스트 + auth 신규 + ArchitectureTest 계층 위반 0).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 ↔ WireMock OIDC IdP | mock IdP가 발급한 id_token(jwks 서명)을 백엔드가 검증하는 경계. 실 IdP 대체 |
| Redis 세션 직렬화 경계 | OidcUser/OAuth2AuthorizedClient/BaselineOidcUser가 JDK 직렬화로 Redis 왕복 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information Disclosure | SC#1 토큰 비노출 검증 부실(주장만, 미검증) | mitigate | WireMock 실제 토큰 교환 경로로 쿠키-토큰-부재 + Redis-토큰-존재를 행위 단언(lesson 02 P1). oidcLogin 우회로는 SC#1 증명 안 함 |
| T-03-10 | Spoofing | 로그아웃 후 세션 잔존(SC#4 미검증) | mitigate | 로그아웃 후 동일 쿠키 재요청 401 행위 단언(Pitfall 4). 200이면 RED |
| T-03-11 | Tampering | 직렬화 회귀(BaselineOidcUser non-serializable로 세션 깨짐) | mitigate | JDK 직렬화 라운드트립 가드 테스트(Pitfall 2). JSON 전환 금지(Pitfall 1) |
| T-03-SC | Tampering | WireMock(test 스코프 외부 의존) | mitigate | 03-01에서 Maven Central 좌표 실측 핀. test 스코프 한정(프로덕션 미진입) |
</threat_model>

<verification>
- `./gradlew test --tests "*BffAuthIT*"` GREEN — SC#1~4·AUTH-01~05 6+1 단언
- `./gradlew test` 전체 스위트 GREEN — Phase 1·2 회귀 없음 + ArchitectureTest 위반 0
- 03-VALIDATION.md Per-Task Verification Map 6개 행이 모두 실제 테스트로 구현됨(lesson 02 P1 — VALIDATION 주장↔테스트 존재 일치)
</verification>

<success_criteria>
- SC#1/AUTH-02: 응답 쿠키 SESSION만 + Redis 세션 토큰 존재(WireMock 행위 증명)
- SC#2/AUTH-03: 쿠키 세션으로 /api/me 200
- SC#3/AUTH-05: 최초 로그인 INVITED→ACTIVE, 재로그인 멱등
- SC#4/AUTH-04: 로그아웃 후 401/redirect
- AUTH-01: WireMock OIDC 로그인 흐름 성공
- D-04: 미초대 사용자 거부
- Pitfall 2: BaselineOidcUser 직렬화 라운드트립 가드
- 전체 스위트 GREEN, 계층 위반 0(lesson 01 P1)
</success_criteria>

<output>
Create `.planning/phases/03-bff-auth/03-03-SUMMARY.md` when done
</output>
