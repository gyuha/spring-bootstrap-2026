---
phase: 03-bff-auth
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main/java/com/anchors/baseline/auth/application/IdentityLinkService.java
  - src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java
  - src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUserService.java
  - src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUser.java
  - src/main/java/com/anchors/baseline/auth/interfaces/MeController.java
  - src/main/resources/application.yml
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "auth 컨텍스트(application/infrastructure/interfaces)가 ArchUnit 4계층 게이트를 위반 없이 통과한다"
    - "SecurityFilterChain이 oauth2Login·logout(invalidateHttpSession+deleteCookies)·CSRF·/api 401 엔트리포인트를 선언한다"
    - "최초 로그인 시 IdentityLinkService가 2단계 매칭(findByExternalId→findByEmail) 후 linkIdentity를 호출하고, 재로그인은 멱등(미호출)이다"
    - "principal이 로컬 User.id를 attribute(user_id)와 getLocalUserId() 양쪽으로 싣는 BaselineOidcUser(Serializable)로 교체되고 /api/me가 표준 OidcUser.getAttribute(\"user_id\")로 User.id를 반환한다"
    - "OAuth2AuthorizedClient(토큰)가 HttpSessionOAuth2AuthorizedClientRepository(프레임워크 기본)로 세션에만 저장되어 Redis 서버 측에만 들어간다(커스텀 토큰 스토어 없음)"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/auth/application/IdentityLinkService.java"
      provides: "oid/email→User.id 해소 + linkIdentity 조율 브리지(D-02). 도메인과 닿는 유일 지점"
      contains: "resolveAndLink"
    - path: "src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java"
      provides: "SecurityFilterChain 빈 (oauth2Login+logout+CSRF+401 entrypoint)"
      contains: "SecurityFilterChain"
    - path: "src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUserService.java"
      provides: "OidcUserService 확장 — 브리지 호출 + principal에 User.id"
      contains: "extends OidcUserService"
    - path: "src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUser.java"
      provides: "로컬 User.id를 싣는 Serializable OidcUser(A-6, Pitfall 2)"
      contains: "implements"
    - path: "src/main/java/com/anchors/baseline/auth/interfaces/MeController.java"
      provides: "SC#2 검증용 보호 엔드포인트 /api/me"
      contains: "/api/me"
    - path: "src/main/resources/application.yml"
      provides: "세션 store-type redis (OIDC client registration은 main이 아니라 test/배포 프로파일에서만)"
      contains: "store-type"
  key_links:
    - from: "BaselineOidcUserService"
      to: "IdentityLinkService.resolveAndLink"
      via: "loadUser() 안에서 브리지 호출"
      pattern: "linkService\\.resolveAndLink|resolveAndLink"
    - from: "IdentityLinkService"
      to: "identity.application.IdentityApplicationService.linkIdentity"
      via: "Application→Application 호출(ArchUnit ownLayer 허용 — 실증됨)"
      pattern: "linkIdentity"
    - from: "IdentityLinkService"
      to: "identity.domain.repository.UserRepository.findByEmail"
      via: "포트 주입 — 2단계 매칭"
      pattern: "findByEmail|findByExternalId"
    - from: "SecurityConfig.oauth2Login"
      to: "BaselineOidcUserService"
      via: "userInfoEndpoint.oidcUserService(...)"
      pattern: "oidcUserService"
    - from: "MeController"
      to: "OidcUser.getAttribute(\"user_id\")"
      via: "표준 OidcUser principal attribute 읽기 (infrastructure 캐스팅 없음 — 계층 위반 회피)"
      pattern: "getAttribute\\(\"user_id\"\\)"
---

<objective>
auth 바운디드 컨텍스트를 신설해 BFF 인증 와이어링을 조립한다(정본 §5.1 — 풍부한 도메인이 아니라 "프레임워크 와이어링 + 얇은 브리지"가 산출물의 본질). 다섯 조각: (1) SecurityFilterChain(oauth2Login+logout+CSRF+401), (2) BaselineOidcUserService(OIDC 성공 시점에 브리지 호출), (3) IdentityLinkService(auth.application 브리지 — 2단계 매칭+linkIdentity), (4) BaselineOidcUser(로컬 User.id 캐리어, Serializable), (5) MeController(SC#2 검증용 /api/me) + application.yml 세션 외부화.

Purpose: AUTH-01~05·SC#1~4를 충족하는 인증 코드의 본체. 토큰 Redis-only(SC#1)는 프레임워크 기본 동작(HttpSessionOAuth2AuthorizedClientRepository)이므로 "토큰을 쿠키/헤더/바디로 내보내는 코드를 만들지 않는 것"이 곧 구현(NFR-01).
Output: auth/{application,infrastructure/security,interfaces} 클래스 5종 + application.yml 세션 설정.

RESEARCH Open Questions 결정(이 플랜에서 확정):
- Q1(SC#1/#4 충실도): SC#1·SC#4의 "토큰 교환 경로"는 Wave 3에서 WireMock으로, SC#2·SC#3은 oidcLogin()으로 검증. 이 플랜은 양쪽이 가능한 구조를 만든다.
- Q2(인증 실패 응답): `/api/**`는 `HttpStatusEntryPoint(401)`, 그 외 브라우저 경로는 기본 302 IdP 리다이렉트.
- Q3(principal 캐리어): 커스텀 `BaselineOidcUser implements OidcUser, Serializable`로 로컬 User.id를 운반하되, **로컬 User.id를 OidcUser attribute map의 `user_id` 키로도 주입**한다(Pitfall 2 — Serializable 필수). 이로써 interfaces(MeController)는 infrastructure 타입을 import/cast하지 않고 표준 `OidcUser.getAttribute("user_id")`로 읽어 ArchUnit interfaces→infrastructure 위반을 구조적으로 제거한다. 단일 메커니즘(attribute 경로)으로 end-to-end 고정 — 런타임 분기·역와이어링 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bff-auth/03-CONTEXT.md
@.planning/phases/03-bff-auth/03-RESEARCH.md

<interfaces>
<!-- 이 플랜이 소비/생성하는 계약. 탐색 불필요. -->

소비 — identity 컨텍스트 (Phase 2 + 03-01 보강):
```
// identity.application.IdentityApplicationService
void linkIdentity(Long userId, String externalId, String idpDisplayName)  // INVITED→ACTIVE, 멱등 가드는 User 내부

// identity.domain.repository.UserRepository (포트)
Optional<User> findByExternalId(String externalId)   // 재로그인 매칭 (D-02 1단계)
Optional<User> findByEmail(Email email)              // 최초 로그인 매칭 (D-02 2단계, 03-01 추가)

// identity.domain.model.User: getId():Long, getStatus():UserStatus
// identity.domain.model.Email: new Email(String) — 형식 검증
```

ArchUnit 게이트(src/test/.../architecture/ArchitectureTest.java — 신규 auth 패키지에 자동 적용):
```
Domain: mayNotAccessAnyLayer
Application: mayOnlyAccessLayers("Domain")     ← ownLayer(Application) 자동 허용 → auth.application→identity.application 통과(실증됨)
Interfaces: mayOnlyAccessLayers("Application","Domain")   ← interfaces→infrastructure 금지(MeController가 BaselineOidcUser cast하면 위반)
Infrastructure: mayOnlyAccessLayers("Domain","Application")
```
consideringOnlyDependenciesInLayers() → Spring Security 타입(com.anchors.baseline 밖)은 게이트 무관.

RESEARCH Code Examples 위치/시그니처(구현 형판 — §Pattern 1·5, §Code Examples 참조):
- SecurityConfig.filterChain(HttpSecurity, OidcUserService): authorizeHttpRequests(permitAll: /actuator/health,/login/**,/oauth2/**; anyRequest authenticated) + oauth2Login(userInfoEndpoint.oidcUserService) + logout(logoutUrl /logout, invalidateHttpSession true, clearAuthentication true, deleteCookies SESSION) + csrf(CookieCsrfTokenRepository.withHttpOnlyFalse) + exceptionHandling(/api/** → HttpStatusEntryPoint(401))
- BaselineOidcUserService extends OidcUserService: loadUser(req) → super.loadUser → oid=getSubject, email=getEmail, displayName=getFullName → linkService.resolveAndLink(oid,email,displayName) → new BaselineOidcUser(oidc, userId)
- IdentityLinkService.resolveAndLink(oid,email,displayName): findByExternalId(oid) present → return id(멱등); else findByEmail(new Email(email)) orElseThrow(미초대 거부 D-04) → identityAppService.linkIdentity(id,oid,displayName) → return id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: auth.application 브리지 + principal 캐리어(user_id attribute 주입)</name>
  <files>src/main/java/com/anchors/baseline/auth/application/IdentityLinkService.java, src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUser.java</files>
  <read_first>
    - 03-RESEARCH.md §Pattern 5 (IdentityLinkService 형판·2단계 매칭), §Pattern 3 (principal=User.id), §Pitfall 2 (Serializable 필수)
    - 03-CONTEXT.md D-02(2단계 매칭), D-04(미초대 거부)
    - src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java (linkIdentity 시그니처)
    - src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java (findByExternalId/findByEmail)
    - src/main/java/com/anchors/baseline/identity/domain/model/{User,Email}.java
    - org.springframework.security.oauth2.core.oidc.user.OidcUser·DefaultOidcUser 시그니처(getAttributes 위임·생성자가 받는 attributes 맵 형태 — Context7 Spring Security 6.5.1 필요 시)
  </read_first>
  <behavior>
    - resolveAndLink(oid, email, name): findByExternalId(oid)가 present면 그 User.id를 반환하고 linkIdentity를 호출하지 않는다(재로그인 멱등 — D-02 1단계).
    - findByExternalId 부재 + findByEmail(email) present면 linkIdentity(id,oid,name) 호출 후 그 id 반환(최초 로그인 — D-02 2단계, INVITED→ACTIVE).
    - findByExternalId·findByEmail 모두 부재면 인증 거부 예외를 던진다(D-04 invite-first, 미초대 거부).
    - BaselineOidcUser는 Serializable이고 getLocalUserId()로 로컬 User.id를 노출하며, 동시에 getAttributes()/getAttribute("user_id")가 로컬 User.id를 담은 attribute 맵을 반환한다(위임 OidcUser attribute + user_id 병합). 나머지 OidcUser 위임을 보존한다.
  </behavior>
  <action>
    `auth/application/IdentityLinkService.java`: `@Service @RequiredArgsConstructor @Transactional`. 주입 = `UserRepository`(identity.domain.repository 포트), `IdentityApplicationService`(identity.application — 같은 Application 레이어, ArchUnit ownLayer 허용 실증됨). `Long resolveAndLink(String oid, String email, String displayName)` 구현 — D-02 2단계 매칭: (1) findByExternalId(oid) present → getId() 반환(멱등, linkIdentity 미호출), (2) else findByEmail(new Email(email)) → orElseThrow로 미초대 거부(D-04). 거부 예외는 Spring Security가 인증 실패로 처리하도록 `OAuth2AuthenticationException`(또는 그 하위) 계열을 던진다 — RuntimeException 단순 throw 금지(OidcUserService.loadUser 계약상 OAuth2 예외여야 401/실패로 매핑). 비즈니스 규칙 없음 — 조율만(전이·멱등 가드는 User 애그리거트 내부).

    `auth/infrastructure/security/BaselineOidcUser.java`: `OidcUser`를 구현(또는 `DefaultOidcUser` 위임 래퍼)하고 `java.io.Serializable`을 명시 구현(Pitfall 2 — Redis JDK 직렬화 라운드트립 필수). 생성자 `(OidcUser delegate, Long localUserId)`. **핵심 — 단일 메커니즘 고정:** 위임 OidcUser의 attribute 맵에 `user_id` 키로 localUserId를 병합한 새 attribute 맵을 만들어 `getAttributes()`가 그 병합 맵을 반환하도록 한다(표준 `getAttribute("user_id")`가 로컬 User.id를 돌려주게 됨 — MeController가 infrastructure 타입 없이 읽는 단일 경로). 추가로 `getLocalUserId():Long` 편의 접근자도 노출(infrastructure 내부·직렬화 가드 테스트용). 나머지 OidcUser 메서드(getClaims/getUserInfo/getIdToken/getName/getAuthorities 등)는 delegate로 위임. 위치는 infrastructure(프레임워크 타입 OidcUser 결합). localUserId(Long)·병합 attribute 맵의 값들이 모두 Serializable이어야 라운드트립이 깨지지 않음(03-03 Task 1 가드가 검증).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew compileJava test --tests "*ArchitectureTest*" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    IdentityLinkService·BaselineOidcUser가 컴파일되고, ArchitectureTest 계층 위반 0건(특히 auth.application→identity.application이 통과 = D-01 [가정] 실증). BaselineOidcUser가 Serializable을 구현하고, getAttribute("user_id")가 localUserId를, getLocalUserId()가 동일 값을 반환한다(병합 attribute 맵).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SecurityFilterChain + OidcUserService + application.yml 세션</name>
  <files>src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java, src/main/java/com/anchors/baseline/auth/infrastructure/security/BaselineOidcUserService.java, src/main/resources/application.yml</files>
  <read_first>
    - 03-RESEARCH.md §Pattern 1(SecurityFilterChain), §Pattern 5(OidcUserService), §Pattern 2(application.yml 세션), §Pitfall 1(JDK 직렬화 유지), §Pitfall 3(SameSite=Lax+code 흐름), Open Q2(401 entrypoint)
    - 03-CONTEXT.md D-05(세션·토큰), D-06(쿠키/CSRF), D-08(IdP 외부화)
    - src/main/resources/application.yml (기존 spring.* 블록 — 세션·OIDC 미설정)
    - Task 1 산출물 BaselineOidcUser(user_id attribute 병합)·IdentityLinkService (이 Task가 주입/생성)
    - Context7로 Spring Security 6.5.1 oauth2Login/HttpSecurity DSL 최신 시그니처 확인(필요 시)
  </read_first>
  <action>
    `auth/infrastructure/security/BaselineOidcUserService.java`: `@Component @RequiredArgsConstructor`, `extends OidcUserService`. `IdentityLinkService` 주입. `loadUser(OidcUserRequest)` override → `super.loadUser(req)`로 표준 OIDC 처리 → `getSubject()`(oid, 불변 식별자 A-6)·`getEmail()`(표준 email claim, D-03 [가정])·`getFullName()`(displayName) 추출 → `linkService.resolveAndLink(...)` 호출 → `new BaselineOidcUser(oidc, userId)` 반환(BaselineOidcUser가 user_id attribute 주입을 담당). infrastructure 배치(프레임워크 타입 결합, D-01).

    `auth/infrastructure/security/SecurityConfig.java`: `@Configuration @EnableWebSecurity`. `SecurityFilterChain filterChain(HttpSecurity, OidcUserService baselineOidcUserService)` 빈. RESEARCH §Pattern 1 형판대로 — authorizeHttpRequests(permitAll: /actuator/health,/login/**,/oauth2/**,/v3/api-docs/**,/swagger-ui/**; anyRequest authenticated), oauth2Login(userInfoEndpoint.oidcUserService(baselineOidcUserService)), logout(logoutUrl("/logout"), invalidateHttpSession(true), clearAuthentication(true), deleteCookies("SESSION")) — SC#4, csrf(CookieCsrfTokenRepository.withHttpOnlyFalse()) — D-06, exceptionHandling으로 `/api/**` 경로에 `HttpStatusEntryPoint(HttpStatus.UNAUTHORIZED)`(401) — Open Q2 결정(SPA/HTTP 클라이언트 단언 용이). 세션 고정 방어는 Spring Security 기본(migrateSession) 유지 — 명시 불필요. **토큰을 쿠키/헤더/바디로 내보내는 코드·커스텀 OAuth2AuthorizedClientService 금지(NFR-01) — HttpSessionOAuth2AuthorizedClientRepository 기본 동작에 의존(SC#1). authorized client는 HttpSession attribute로 저장됨(비-bean 경로).**

    `application.yml`: `spring.session.store-type: redis` + `spring.session.redis.namespace: "spring:session:baseline"`(명시) 추가. **결정(WARNING 4) — main application.yml에는 `spring.security.oauth2.client.registration`/`provider` 블록을 두지 않는다.** 빈 `${OIDC_*:}` placeholder를 main에 두면 부팅 시 `ClientRegistrationRepository` auto-config가 빈/유효하지 않은 issuer-uri로 eager issuer discovery를 시도해 부팅이 깨지거나 무의미한 등록이 생긴다. 따라서 registration/provider는 (a) test 프로파일 application-test.yml(WireMock issuer-uri, 03-03 소관)과 (b) 배포용 프로파일(application-prod.yml 또는 환경변수/시크릿 매니저 주입, 본 플랜 범위 밖·문서화만)에서만 정의한다. main application.yml은 OIDC client 블록 없이 부팅 가능해야 한다(oauth2Login DSL은 ClientRegistrationRepository 빈이 없으면 자동 비활성/조건부 — 부팅 실패 안 함). 세션 직렬화는 기본 JDK 유지(JSON 전환 금지 — Pitfall 1, OAuth2/OIDC 역직렬화 깨짐). 기존 datasource/jpa/flyway/redis/mybatis/springdoc/management 블록은 surgical하게 보존. swagger/actuator permitAll 규칙이 기존 springdoc 노출과 정합하는지 확인.

    부팅 검증: main 프로파일(OIDC registration 없음)에서 `./gradlew bootJar` 또는 기존 컨텍스트-로드 테스트가 깨지지 않아야 한다. 실 OIDC 흐름 부팅·동작은 application-test.yml의 test-idp registration이 있는 03-03 통합 테스트가 검증한다(다음 wave).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew compileJava test --tests "*ArchitectureTest*" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    SecurityConfig·BaselineOidcUserService가 컴파일되고 ArchitectureTest 계층 위반 0건. SecurityFilterChain이 oauth2Login(oidcUserService 연결)·logout(invalidateHttpSession+deleteCookies)·CSRF·/api 401 entrypoint를 선언한다. main application.yml에 세션 store-type redis가 추가되고 OIDC registration/provider 블록은 main에 없다(test/배포 프로파일에서만). 커스텀 토큰 스토어 코드 없음.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SC#2 검증용 /api/me 보호 엔드포인트(표준 attribute 경로)</name>
  <files>src/main/java/com/anchors/baseline/auth/interfaces/MeController.java</files>
  <read_first>
    - 03-RESEARCH.md §Code Examples(MeController), §Pattern 3(@AuthenticationPrincipal → User.id)
    - Task 1 산출물 BaselineOidcUser — getAttribute("user_id")가 로컬 User.id를 반환(병합 attribute 맵)
    - src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java (anyRequest authenticated — /api/me 보호됨)
  </read_first>
  <action>
    `auth/interfaces/MeController.java`: `@RestController`. `GET /api/me` → `@AuthenticationPrincipal org.springframework.security.oauth2.core.oidc.user.OidcUser principal`를 받아 **`principal.getAttribute("user_id")`로 로컬 User.id를 읽어** `{"userId": ...}` JSON으로 반환한다(A-6 — IdP oid가 아니라 로컬 User.id). 단일 메커니즘 — Task 1에서 BaselineOidcUser가 user_id attribute를 병합하므로 런타임에 이 attribute가 항상 존재한다. **BaselineOidcUser를 import하거나 그 타입으로 cast하지 않는다**(NO `com.anchors.baseline.auth.infrastructure...` import, NO 캐스팅). 이로써 interfaces→infrastructure 의존이 발생하지 않아 ArchUnit `Interfaces.mayOnlyAccessLayers("Application","Domain")`를 통과한다. getAttribute가 반환하는 값 타입(Long/Number)을 그대로 직렬화하거나 명시 변환해 응답한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 && ./gradlew compileJava test --tests "*ArchitectureTest*" 2>&1 | tail -10 && grep -rn "auth.infrastructure" src/main/java/com/anchors/baseline/auth/interfaces/MeController.java; test $? -ne 0 && echo "OK: no infrastructure import in MeController"</automated>
  </verify>
  <acceptance_criteria>
    MeController가 /api/me에서 `OidcUser.getAttribute("user_id")`로 로컬 User.id를 반환하고, MeController에 `...auth.infrastructure...` import가 0건이며(grep 무매치), ArchitectureTest 계층 위반 0건(interfaces→infrastructure 0). 전체 컴파일 통과.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 ↔ 백엔드(쿠키 세션) | 신뢰 불가 클라이언트 입력. 쿠키엔 SESSION ID만, 토큰 비노출 |
| 백엔드 ↔ OIDC IdP(authorization-code/token) | 토큰 교환·검증은 서버에서만(BFF A-3). state/nonce/PKCE 경계 |
| auth 컨텍스트 ↔ identity 컨텍스트 | linkIdentity 호출 — 도메인과 닿는 유일 지점(§5.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | 액세스/리프레시 토큰 브라우저 노출(XSS 탈취) | mitigate | HttpSessionOAuth2AuthorizedClientRepository(기본)로 토큰을 세션→Redis 서버 측에만. 쿠키 HttpOnly. 토큰을 쿠키/헤더/바디로 내보내는 코드 안 만듦(SC#1/NFR-02). 03-03 행위 단언 |
| T-03-03 | Tampering | CSRF(쿠키 세션 상태 변경) | mitigate | CookieCsrfTokenRepository.withHttpOnlyFalse + SameSite=Lax(D-06). /logout은 CSRF 토큰 요구 |
| T-03-04 | Spoofing | 세션 고정(session fixation) | mitigate | Spring Security 기본 sessionManagement migrateSession(로그인 시 세션 ID 교체) |
| T-03-05 | Elevation of Privilege | 미초대 사용자 무단 가입 | mitigate | IdentityLinkService가 findByEmail 부재 시 OAuth2AuthenticationException으로 거부(D-04 invite-first allowlist) |
| T-03-06 | Tampering/Spoofing | OAuth2 state/nonce 위조 | mitigate | oauth2Login 내장 state·nonce·PKCE 검증(hand-roll 금지 — RESEARCH Don't Hand-Roll) |
| T-03-07 | Spoofing | principal에 가변 식별자(oid/email) 사용 | mitigate | principal attribute user_id는 불변 로컬 User.id(A-6/IDEN-05). 연결 후 매칭은 oid로만 |
| T-03-08 | Information Disclosure | OIDC client-secret 커밋 노출 | mitigate | main application.yml에 OIDC registration 없음. 시크릿은 배포 프로파일/시크릿 매니저 주입, 미커밋 |
</threat_model>

<verification>
- `./gradlew compileJava` 통과 (auth 컨텍스트 5종)
- `./gradlew test --tests "*ArchitectureTest*"` GREEN — auth 4계층 위반 0(application→identity.application 통과, interfaces→infrastructure 미발생: MeController가 표준 OidcUser attribute만 읽음)
- MeController에 `...auth.infrastructure...` import 0건(grep)
- SecurityFilterChain에 oauth2Login·logout(invalidateHttpSession+deleteCookies)·CSRF·401 entrypoint 선언 확인
- main application.yml에 세션 redis store-type, OIDC registration 블록 없음
</verification>

<success_criteria>
- AUTH-01: oauth2Login으로 OIDC 로그인 흐름 구성(IdP 비종속 — registration/provider는 test/배포 프로파일 외부화)
- AUTH-02/SC#1: 커스텀 토큰 스토어 없음 — 프레임워크 기본으로 토큰 세션(HttpSession attribute)→Redis (03-03이 행위 단언)
- AUTH-03/SC#2: principal attribute user_id=로컬 User.id, /api/me 보호 엔드포인트(표준 OidcUser attribute 경로 — 계층 위반 없음)
- AUTH-04/SC#4: logout(invalidateHttpSession+deleteCookies) (03-03이 행위 단언)
- AUTH-05/SC#3: BaselineOidcUserService→IdentityLinkService→linkIdentity 브리지(2단계 매칭, 멱등)
- ArchUnit 계층 위반 0건 유지(lesson 01 P1)
</success_criteria>

<output>
Create `.planning/phases/03-bff-auth/03-02-SUMMARY.md` when done
</output>
</content>
</invoke>
