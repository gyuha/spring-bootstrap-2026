---
phase: "06-me-aggregation"
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java
  - src/main/java/com/anchors/baseline/auth/interfaces/MeResponse.java
autonomous: true
requirements:
  - AUTH-08
  - AUTH-09
  - AUTH-11

must_haves:
  truths:
    - "GET /api/auth/me 엔드포인트가 AuthController에 존재한다"
    - "me()는 OidcUser.getAttribute('user_id')로 userId를 읽는다(infrastructure 타입 미참조)"
    - "me()는 IdentityApplicationService.findUser + AuthorizationApplicationService.findPermissions를 호출해 MeResponse로 조립한다"
    - "MeResponse는 userId/email/status + roles/menus/resources를 단일 record로 표현한다"
    - "D-03: me() 응답은 Phase 5 record + @JsonInclude 패턴을 계승한 MeResponse 단일 record다"
    - "D-06: ArchUnit 무변경으로 GREEN — 교차 컨텍스트 application 호출이 이미 허용됨을 테스트로 실증한다"
    - "ArchUnit hexagonalLayerDependencies 테스트가 신규 import 추가 후에도 GREEN이다"
    - "SecurityConfig 무변경 — /api/auth/me는 /api/** 보호 매처에 걸려 비인증 401이 자연 충족된다"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java"
      provides: "me() 메서드 추가 + 두 application 서비스 생성자 주입"
      contains: "GetMapping.*me"
    - path: "src/main/java/com/anchors/baseline/auth/interfaces/MeResponse.java"
      provides: "신원+권한 결합 응답 계약 (auth/interfaces 소유)"
      contains: "record MeResponse"
  key_links:
    - from: "AuthController.me()"
      to: "IdentityApplicationService.findUser"
      via: "생성자 주입 필드 호출"
      pattern: "identityService\\.findUser"
    - from: "AuthController.me()"
      to: "AuthorizationApplicationService.findPermissions"
      via: "생성자 주입 필드 호출"
      pattern: "authorizationService\\.findPermissions"
    - from: "MeResponse"
      to: "UserView / PermissionView"
      via: "정적 팩토리 MeResponse.of()"
      pattern: "MeResponse\\.of"
---

<objective>
AuthController에 GET /api/auth/me 핸들러(me() 메서드)를 추가하고, 반환 타입으로 MeResponse record를 신규 생성한다. me()는 OidcUser.getAttribute("user_id")로 로컬 userId를 읽고, Wave 1에서 신설된 두 application read 메서드를 교차 호출해 신원+권한을 MeResponse 하나로 결합한다.

Purpose: SPA가 bootstrap 시 단일 요청으로 신원과 권한을 모두 획득하는 표준 표면을 제공한다(AUTH-08/09 SC#1~2). ArchUnit 계층 규칙이 이 교차 컨텍스트 호출을 무변경으로 허용함을 Wave 3 이전에 컴파일·테스트로 실증한다(AUTH-11 SC#3 사전 확인).
Output: MeResponse.java(신규), AuthController.java 수정(me() 메서드 추가 + 두 서비스 주입).
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-me-aggregation/06-CONTEXT.md
@.planning/phases/06-me-aggregation/06-PATTERNS.md
@.planning/phases/06-me-aggregation/06-RESEARCH.md
@.planning/phases/06-me-aggregation/06-01-SUMMARY.md

<interfaces>
<!-- Wave 1 산출물 계약 (06-01-PLAN.md에서 정의) -->

identity/application/UserView.java (Wave 1 신규):
  public record UserView(Long id, String email, String status)

authorization/application/PermissionView.java (Wave 1 신규):
  public record PermissionView(List<String> roles, List<MenuEntry> menus, List<ResourceEntry> resources)
  public record MenuEntry(long menuId, String role)
  public record ResourceEntry(long resourceId, String role)

IdentityApplicationService.findUser(long userId) -> UserView  (@Transactional readOnly)
AuthorizationApplicationService.findPermissions(long userId) -> PermissionView  (@Transactional readOnly)

<!-- 기존 AuthController 계약 (수정 대상) -->

src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java:
- @RestController @RequestMapping("/api/auth")
- 생성자: ObjectProvider<ClientRegistrationRepository> + @Value oidcRegistrationId
- session() 메서드: @GetMapping("/session"), @AuthenticationPrincipal OidcUser, SessionResponse 반환
- login() 메서드: @GetMapping("/login"), returnTo 처리
- SessionResponse record: (boolean authenticated, @JsonInclude(NON_NULL) Object userId)
- OidcUser.getAttribute("user_id") 패턴 — infrastructure 타입 미참조 (ArchUnit 통과 선례)

ArchUnit 규칙 (D-06 실측):
- layeredArchitecture 단일 규칙, 접미 글로브 ..interfaces.., ..application..
- .whereLayer("Interfaces").mayOnlyAccessLayers("Application", "Domain")
- auth/interfaces → identity/application + authorization/application 호출은 이미 허용됨
- 무변경으로 GREEN (IdentityLinkService 선례: auth.application → identity.application)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeResponse record 신규 생성 (auth/interfaces 소유, 응답 계약)</name>
  <files>
    src/main/java/com/anchors/baseline/auth/interfaces/MeResponse.java
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java — SessionResponse record 패턴 (line 95-99): record + @JsonInclude(NON_NULL) 계승 여부 확인
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — "MeResponse 적용 패턴" 섹션
    - .planning/phases/06-me-aggregation/06-RESEARCH.md — Pattern 3: read DTO — record + @JsonInclude
  </read_first>
  <action>
    src/main/java/com/anchors/baseline/auth/interfaces/MeResponse.java 신규 생성:
    패키지 com.anchors.baseline.auth.interfaces.

    public record MeResponse(Long userId, String email, String status, List<String> roles, List<PermissionView.MenuEntry> menus, List<PermissionView.ResourceEntry> resources) 로 선언.

    정적 팩토리 메서드 추가: public static MeResponse of(UserView identity, PermissionView permissions)
      — identity.id(), identity.email(), identity.status(), permissions.roles(), permissions.menus(), permissions.resources() 를 조합해 new MeResponse(...) 반환.

    import: com.anchors.baseline.identity.application.UserView, com.anchors.baseline.authorization.application.PermissionView, java.util.List.

    Javadoc:
    "GET /api/auth/me 응답 계약. 신원(userId/email/status)과 직접 부여 권한(roles/menus/resources)을 단일 record로 결합한다.
     권한 필드(roles/menus/resources)는 권한이 없어도 null이 아닌 빈 배열로 직렬화된다.
     D-02 한계 주석: 권한은 직접 부여(direct grant)만 포함하며 그룹·계층 상속은 포함하지 않는다.
     향후 그룹/계층 상속 추가 시 이 record의 필드 구조(3버킷)는 그대로 유지하고 집계 메서드만 확장한다."

    — @JsonInclude(NON_NULL)은 nullable 스칼라가 없으므로 불필요. 빈 컬렉션은 [] 그대로 직렬화.
    — infrastructure 타입 import 금지.
  </action>
  <verify>
    <automated>./gradlew compileJava -x test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MeResponse.java 파일 존재, record 타입, userId/email/status/roles/menus/resources 필드.
    - MeResponse.of(UserView, PermissionView) 정적 팩토리 존재.
    - ./gradlew compileJava 컴파일 오류 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: AuthController에 me() 메서드 추가 + 두 application 서비스 생성자 주입</name>
  <files>
    src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java — 전체(현재 생성자 시그니처, session() 패턴, import 목록, line 1-100)
    - src/main/java/com/anchors/baseline/auth/interfaces/MeController.java — OidcUser.getAttribute("user_id") 표준 읽기 + infrastructure 미참조 패턴 실측
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — "me() 추가 패턴" + "infrastructure 타입 미참조" + "Number 상위 캐스팅" 섹션
    - .planning/phases/06-me-aggregation/06-RESEARCH.md — Pitfall 4(SecurityConfig permitAll 추가 금지), Pitfall 5(Number 캐스팅)
  </read_first>
  <action>
    src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java 수정 (수술적 — 기존 코드 최소 변경):

    1. import 추가 (기존 import 블록 마지막에):
       import com.anchors.baseline.identity.application.IdentityApplicationService;
       import com.anchors.baseline.identity.application.UserView;
       import com.anchors.baseline.authorization.application.AuthorizationApplicationService;
       import com.anchors.baseline.authorization.application.PermissionView;

    2. 클래스 필드 추가 (기존 두 필드 아래):
       private final IdentityApplicationService identityService;
       private final AuthorizationApplicationService authorizationService;

    3. 생성자 시그니처 확장 (기존 2 파라미터 → 4 파라미터로):
       ObjectProvider<ClientRegistrationRepository> clientRegistrationRepository,
       @Value("${app.auth.oidc-registration-id:test-idp}") String oidcRegistrationId,
       IdentityApplicationService identityService,
       AuthorizationApplicationService authorizationService
       — 생성자 바디에 this.identityService = identityService; this.authorizationService = authorizationService; 추가.

    4. me() 메서드 추가 (session() 메서드 아래):
       Javadoc: "신원·권한 집계 엔드포인트(AUTH-08/09). 인증 사용자의 신원(userId/email/status)과 직접 부여 권한(roles/menus/resources)을 단일 응답으로 반환한다. 비인증 시 SecurityConfig /api/** 보호 매처가 401을 먼저 응답하므로 principal null 분기 없음(D-05). 권한은 직접 부여(direct grant)만 포함 — 그룹·계층 상속 미포함(D-02)."
       @GetMapping("/me")
       public MeResponse me(@AuthenticationPrincipal OidcUser principal) {
           Object rawId = principal.getAttribute("user_id");
           long userId = ((Number) rawId).longValue();  // Number 상위 캐스팅(Pitfall 5: Integer/Long 모두 안전)
           UserView identity = identityService.findUser(userId);
           PermissionView permissions = authorizationService.findPermissions(userId);
           return MeResponse.of(identity, permissions);
       }

    금지 사항:
    - SecurityConfig 파일 수정 금지 — D-05, /api/auth/me는 permitAll 제외.
    - infrastructure 타입(BaselineOidcUser 등) import/cast 금지 — D-04.
    - 기존 session()/login()/isSafeRelativePath()/SessionResponse 수정 금지 — 수술적 변경만.
    - me()에 principal == null 분기 추가 금지 — SecurityConfig가 401 처리(불필요한 방어 코드).
  </action>
  <verify>
    <automated>./gradlew test --tests "*ArchitectureTest*" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - AuthController에 @GetMapping("/me") me() 메서드 존재.
    - IdentityApplicationService, AuthorizationApplicationService 생성자 주입 존재.
    - ./gradlew test --tests "*ArchitectureTest*" → GREEN (신규 교차 컨텍스트 import 후에도 무변경 통과 실증 — AUTH-11 SC#3 사전 확인).
    - 기존 session()/login() 메서드 및 SessionResponse record 무변경.
  </done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| client → GET /api/auth/me | SecurityConfig /api/** 보호 매처. 비인증 요청은 Spring Security가 401로 차단 — me() 본문 진입 불가. |
| OidcUser.getAttribute("user_id") → long userId | 서버 세션에서 읽는 신뢰된 값. URL/쿼리파라미터로 외부에서 주입 불가. |
| AuthController.me() → application 서비스 | auth/interfaces → identity/application, authorization/application 교차 컨텍스트 호출 — ArchUnit 전역 레이어 글로브로 이미 허용(D-06). |

## STRIDE 위협 등록부

| Threat ID | 범주 | 구성요소 | 대응 | 완화 계획 |
|-----------|------|----------|------|----------|
| T-06-03 | Spoofing | AuthController.me() userId 출처 | mitigate | userId는 URL/body 파라미터가 아닌 서버 세션 principal.getAttribute("user_id")에서만 읽음. 타인의 userId로 요청 조작 불가(서버 세션 무결성은 Spring Security + Redis). |
| T-06-04 | Information Disclosure | GET /api/auth/me 응답 (PII: email, 권한 목록) | mitigate | SecurityConfig 무변경 — /api/** 보호 매처로 비인증 시 401. D-05 permitAll 제외 설계로 선제 차단. |
| T-06-05 | Elevation of Privilege | me() 응답에 타인 권한 포함 | mitigate | findUser(userId), findPermissions(userId) 모두 principal의 userId로만 조회 — 요청 파라미터로 타 userId 지정 불가 (API 설계상 차단). |
| T-06-SC | Tampering | npm/pip/cargo 설치 | accept | 신규 외부 패키지 없음 (NFR-01). slopcheck 대상 없음. |
</threat_model>

<verification>
Wave 2 완료 후 ArchUnit GREEN 확인(AUTH-11 SC#3 사전 실증) + 기존 회귀:

```
./gradlew test --tests "*ArchitectureTest*" && ./gradlew test --rerun-tasks
```

ArchUnit GREEN + 전체 테스트 58개 이상 GREEN 필수.
</verification>

<success_criteria>
1. MeResponse.java 신규 존재, userId/email/status/roles/menus/resources 필드, MeResponse.of() 정적 팩토리.
2. AuthController에 @GetMapping("/me") me() 메서드 존재, Number.longValue() 캐스팅 사용.
3. IdentityApplicationService, AuthorizationApplicationService 생성자 주입 추가.
4. SecurityConfig 파일 무변경 — /api/auth/me가 permitAll에 추가되지 않음.
5. ./gradlew test --tests "*ArchitectureTest*" → GREEN (신규 교차 컨텍스트 import 후 무변경 통과 실증).
6. ./gradlew test --rerun-tasks → 기존 58 tests 이상 GREEN, 회귀 없음.
</success_criteria>

<output>
완료 후 .planning/phases/06-me-aggregation/06-02-SUMMARY.md 생성
</output>
