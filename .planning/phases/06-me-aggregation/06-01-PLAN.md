---
phase: "06-me-aggregation"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/anchors/baseline/identity/application/UserView.java
  - src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java
  - src/main/java/com/anchors/baseline/authorization/application/PermissionView.java
  - src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java
autonomous: true
requirements:
  - AUTH-08
  - AUTH-09

must_haves:
  truths:
    - "IdentityApplicationService.findUser(long)가 UserView(id, email, status)를 반환한다"
    - "AuthorizationApplicationService.findPermissions(long)가 PermissionView(roles, menus, resources)를 반환한다"
    - "두 read 메서드 모두 @Transactional(readOnly=true)로 선언되어 클래스 레벨 쓰기 트랜잭션을 오버라이드한다"
    - "UserView·PermissionView에 JPA/MyBatis 애너테이션(@Entity·@Param 등) 없음 — 순수 DTO"
    - "PermissionView.findPermissions는 findByUserId 3종만 호출한다(findByGroupId 미사용)"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/identity/application/UserView.java"
      provides: "Identity 신원 읽기 DTO (CQRS-lite)"
      contains: "record UserView"
    - path: "src/main/java/com/anchors/baseline/authorization/application/PermissionView.java"
      provides: "Authorization 직접 부여 권한 읽기 DTO (CQRS-lite, D-02 direct 해석)"
      contains: "record PermissionView"
    - path: "src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java"
      provides: "findUser(long) 메서드 추가"
      contains: "@Transactional(readOnly = true)"
    - path: "src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java"
      provides: "findPermissions(long) 메서드 추가"
      contains: "@Transactional(readOnly = true)"
  key_links:
    - from: "IdentityApplicationService.findUser"
      to: "UserRepository.findById"
      via: "도메인 포트 위임"
      pattern: "userRepository\\.findById"
    - from: "AuthorizationApplicationService.findPermissions"
      to: "GlobalRoleGrantRepository.findByUserId / MenuGrantRepository.findByUserId / ResourceGrantRepository.findByUserId"
      via: "도메인 포트 3종 위임"
      pattern: "findByUserId"
---

<objective>
Identity application에 신원 읽기 메서드(findUser)와 Authorization application에 직접 부여 권한 읽기 메서드(findPermissions)를 신설하고, 각각의 반환 타입으로 순수 read DTO(UserView, PermissionView)를 신규 생성한다.

Purpose: Wave 2에서 AuthController.me()가 호출할 두 application read 메서드의 계약(타입·시그니처)을 먼저 확정함으로써 전방참조 컴파일 위험을 제거한다(types-first wave, lesson 03/04/05).
Output: UserView.java, PermissionView.java(신규), IdentityApplicationService.findUser 메서드, AuthorizationApplicationService.findPermissions 메서드.
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-me-aggregation/06-CONTEXT.md
@.planning/phases/06-me-aggregation/06-PATTERNS.md
@.planning/phases/06-me-aggregation/06-RESEARCH.md

<interfaces>
<!-- 수정 대상 파일에서 발췌한 기존 계약 — 실측 확인 완료 -->

From src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java:
- 클래스 레벨 @Transactional (쓰기 기본값)
- @RequiredArgsConstructor + private final UserRepository userRepository
- 기존 패턴: userRepository.findById(userId).orElseThrow(() -> new IllegalArgumentException("사용자를 찾을 수 없습니다: " + userId))
- 기존 메서드: invite(String), linkIdentity(String, String), disable(Long) — 수정 금지

From src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java:
- 클래스 레벨 @Transactional (쓰기 기본값)
- @RequiredArgsConstructor + 6개 포트 주입(globalRoleGrantRepository, menuGrantRepository, resourceGrantRepository, groupRepository, groupMemberRepository, resourceHierarchyRepository)
- 도메인 포트 시그니처(실측):
  GlobalRoleGrantRepository.findByUserId(long) -> List<GlobalRoleGrant>
  MenuGrantRepository.findByUserId(long) -> List<MenuGrant>
  ResourceGrantRepository.findByUserId(long) -> List<ResourceGrant>
- 기존 메서드들(grantGlobalRole, revokeGlobalRole 등) — 수정 금지

From src/main/java/com/anchors/baseline/identity/domain/model/User.java (실측):
- user.getId() -> Long
- user.getEmail().getValue() -> String  (Email VO 평탄화)
- user.getStatus().name() -> String     (UserStatus enum: INVITED/ACTIVE/DISABLED)

From src/main/java/com/anchors/baseline/authorization/domain/model/ (실측):
- GlobalRoleGrant.getRole().name() -> String  (RoleName enum)
- MenuGrant.getMenuId() -> Long, MenuGrant.getRole().name() -> String
- ResourceGrant.getResourceId() -> Long, ResourceGrant.getRole().name() -> String

Analog — src/main/java/com/anchors/baseline/authorization/application/EffectiveGrantDto.java:
- 순수 평탄 DTO, @Entity/@Param 등 영속 기술 애너테이션 없음
- MyBatis 무인자 생성자 필요 시 class + @Data/@NoArgsConstructor/@AllArgsConstructor
- JPA 전용 경로(findByUserId)는 record 사용 가능 — 무인자 생성자 불필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserView record 신규 생성 + IdentityApplicationService.findUser() 메서드 추가</name>
  <files>
    src/main/java/com/anchors/baseline/identity/application/UserView.java,
    src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java — 전체(클래스 레벨 @Transactional 위치, userRepository.findById 기존 패턴, orElseThrow 패턴, line 38-43)
    - src/main/java/com/anchors/baseline/authorization/application/EffectiveGrantDto.java — 순수 DTO 패턴(영속 기술 애너테이션 없는 read DTO 형판)
    - src/main/java/com/anchors/baseline/identity/domain/model/User.java — getId()/getEmail().getValue()/getStatus().name() getter 실측
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — "UserView 적용 패턴" 섹션
  </read_first>
  <behavior>
    - UserView record: 패키지 com.anchors.baseline.identity.application, 필드 Long id / String email / String status — JPA/Lombok 애너테이션 없음, 순수 record
    - findUser(long userId): @Transactional(readOnly=true) 메서드 레벨 오버라이드, userRepository.findById(userId).orElseThrow(IllegalArgumentException) 후 UserView 생성 반환
    - User 엔티티는 반환 타입에서 완전히 숨겨짐 — interfaces 계층에 User/Email/UserStatus 노출 없음
    - status = UserStatus.name() (문자열 "INVITED"/"ACTIVE"/"DISABLED")
    - email = user.getEmail().getValue() (Email VO 평탄화)
  </behavior>
  <action>
    1. src/main/java/com/anchors/baseline/identity/application/UserView.java 신규 생성:
       패키지 com.anchors.baseline.identity.application. public record UserView(Long id, String email, String status). Javadoc: "Identity 컨텍스트 신원 읽기 DTO (CQRS-lite). User 엔티티를 interfaces 계층에 노출하지 않는다. D-01(auth-08)."
       — @Entity/@Param/@Data/@NoArgsConstructor 등 영속 기술 애너테이션 없음.

    2. src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java 에 메서드 추가:
       기존 클래스 레벨 @Transactional 아래, 기존 쓰기 메서드들(invite/linkIdentity/disable) 뒤에 추가.
       시그니처: @Transactional(readOnly = true) / public UserView findUser(long userId)
       구현: userRepository.findById(userId).orElseThrow(() -> new IllegalArgumentException("사용자를 찾을 수 없습니다: " + userId)) 로 User를 얻고,
             new UserView(user.getId(), user.getEmail().getValue(), user.getStatus().name()) 반환.
       — 기존 메서드(invite/linkIdentity/disable), import, 클래스 구조, 주석 일체 수정 금지.
       — infrastructure 타입 import 추가 금지.
  </action>
  <verify>
    <automated>./gradlew compileJava -x test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - UserView.java 파일 존재, record 타입, 필드 id/email/status 선언.
    - IdentityApplicationService에 findUser(long) 메서드 존재, @Transactional(readOnly=true) 표시.
    - ./gradlew compileJava 컴파일 오류 없음.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PermissionView record 신규 생성 + AuthorizationApplicationService.findPermissions() 메서드 추가</name>
  <files>
    src/main/java/com/anchors/baseline/authorization/application/PermissionView.java,
    src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java — 전체(클래스 레벨 @Transactional 위치, 6개 포트 필드 목록, grantGlobalRole 패턴, line 29-36)
    - src/main/java/com/anchors/baseline/authorization/domain/repository/GlobalRoleGrantRepository.java — findByUserId 시그니처 실측
    - src/main/java/com/anchors/baseline/authorization/domain/repository/MenuGrantRepository.java — findByUserId 시그니처, findByGroupId 별도 메서드 확인
    - src/main/java/com/anchors/baseline/authorization/domain/repository/ResourceGrantRepository.java — findByUserId 시그니처, findByGroupId 별도 메서드 확인
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — "PermissionView 적용 패턴", "findPermissions() 적용 패턴" 섹션
  </read_first>
  <behavior>
    - PermissionView record: 패키지 com.anchors.baseline.authorization.application, 필드 List<String> roles / List<MenuEntry> menus / List<ResourceEntry> resources
    - 중첩 record: MenuEntry(long menuId, String role), ResourceEntry(long resourceId, String role) — 모두 동일 파일 내 public static record
    - findPermissions(long userId): @Transactional(readOnly=true) 오버라이드, findByUserId 3종 호출, Stream→toList() 변환, PermissionView 반환
    - findByGroupId 절대 미사용 — D-02 direct 해석
    - 빈 결과 시 빈 List 반환 (null 아님) — 권한 없는 사용자
    - RoleName enum → .name() String 평탄화
  </behavior>
  <action>
    1. src/main/java/com/anchors/baseline/authorization/application/PermissionView.java 신규 생성:
       패키지 com.anchors.baseline.authorization.application.
       public record PermissionView(List<String> roles, List<MenuEntry> menus, List<ResourceEntry> resources).
       중첩: public record MenuEntry(long menuId, String role), public record ResourceEntry(long resourceId, String role).
       import java.util.List. Javadoc: "Authorization 컨텍스트 직접 부여 권한 읽기 DTO (CQRS-lite). D-02 direct 해석 — 그룹·계층 상속 미포함. 향후 그룹/계층 상속 추가 시 roles/menus/resources 버킷 구조는 그대로 유지하고 집계 메서드만 확장한다."
       — @Entity/@Param/@Data 등 영속 기술 애너테이션 없음.

    2. src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java 에 메서드 추가:
       기존 쓰기 메서드들 뒤에 추가.
       시그니처: @Transactional(readOnly = true) / public PermissionView findPermissions(long userId)
       구현:
         List<String> roles = globalRoleGrantRepository.findByUserId(userId).stream().map(g -> g.getRole().name()).toList();
         List<PermissionView.MenuEntry> menus = menuGrantRepository.findByUserId(userId).stream().map(g -> new PermissionView.MenuEntry(g.getMenuId(), g.getRole().name())).toList();
         List<PermissionView.ResourceEntry> resources = resourceGrantRepository.findByUserId(userId).stream().map(g -> new PermissionView.ResourceEntry(g.getResourceId(), g.getRole().name())).toList();
         return new PermissionView(roles, menus, resources);
       — findByGroupId 호출 금지(D-02 주석 추가: "// direct 해석 — findByGroupId 미사용(D-02)").
       — 기존 메서드, import, 클래스 구조 일체 수정 금지.
  </action>
  <verify>
    <automated>./gradlew compileJava -x test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PermissionView.java 파일 존재, record 타입, MenuEntry/ResourceEntry 중첩 record 포함.
    - AuthorizationApplicationService에 findPermissions(long) 메서드 존재, @Transactional(readOnly=true) 표시.
    - ./gradlew compileJava 컴파일 오류 없음.
    - 기존 테스트(v1.0 51개 + Phase 5 7개) 회귀 없음: ./gradlew test --rerun-tasks exits 0.
  </done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| application → domain repository | application read 메서드가 도메인 포트를 통해 DB 조회. userId는 서버 세션 principal에서 온 신뢰된 값 — 외부 입력 없음. |

## STRIDE 위협 등록부

| Threat ID | 범주 | 구성요소 | 대응 | 완화 계획 |
|-----------|------|----------|------|----------|
| T-06-01 | Information Disclosure | PermissionView / UserView | mitigate | application read DTO가 도메인 엔티티를 평탄화해 반환 — interfaces 계층에 JPA 엔티티(@Entity) 노출 없음. Wave 1 자체는 메서드 신설만이라 외부 노출 없음(Wave 2 AuthController가 실제 노출 표면). |
| T-06-02 | Elevation of Privilege | AuthorizationApplicationService.findPermissions | mitigate | findByUserId(userId)만 호출 — findByGroupId 미사용. groupId를 userId로 오인하는 버그 차단. 메서드 주석에 direct 해석 명시. |
| T-06-SC | Tampering | npm/pip/cargo 설치 | accept | 신규 외부 패키지 없음 (NFR-01). slopcheck 대상 없음. |
</threat_model>

<verification>
Wave 1 완료 후 전체 회귀:

```
./gradlew test --rerun-tasks
```

58개 기존 테스트(v1.0 51 + Phase 5 7) GREEN 유지 필수.
</verification>

<success_criteria>
1. UserView.java, PermissionView.java 두 파일 신규 존재, 순수 record, 영속 기술 애너테이션 없음.
2. IdentityApplicationService.findUser(long), AuthorizationApplicationService.findPermissions(long) 두 메서드 @Transactional(readOnly=true) 오버라이드로 존재.
3. findPermissions는 findByUserId 3종만 호출, findByGroupId 미사용(D-02 direct 해석).
4. ./gradlew test --rerun-tasks → 기존 58 tests 이상 GREEN, 회귀 없음.
</success_criteria>

<output>
완료 후 .planning/phases/06-me-aggregation/06-01-SUMMARY.md 생성
</output>
