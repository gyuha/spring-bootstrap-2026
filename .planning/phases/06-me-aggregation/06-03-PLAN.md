---
phase: "06-me-aggregation"
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java
autonomous: true
requirements:
  - AUTH-08
  - AUTH-09
  - AUTH-11

must_haves:
  truths:
    - "BffAuthMeIT.meReturnsIdentity() 통과 — 인증 상태 GET /api/auth/me → 200 + userId/email/status"
    - "BffAuthMeIT.meUnauthenticatedReturns401() 통과 — 비인증 GET /api/auth/me → 401"
    - "BffAuthMeIT.meReturnsGrantedPermissions() 통과 — grant 시드 후 roles/menus/resources 응답 포함"
    - "BffAuthMeIT.meReturnsEmptyPermissionsForNewUser() 통과 — 권한 없는 사용자 빈 배열 []"
    - "BffAuthMeIT.meIsNotPermitAll() 통과 — /api/auth/me 401, /api/auth/session은 여전히 200"
    - "./gradlew test --tests '*ArchitectureTest*' GREEN (AUTH-11 SC#3 최종 실증)"
    - "principal user_id ↔ seededUser.id ↔ grant.userId 3자 일치 — oidcLogin claim이 실 User.id와 동일"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java"
      provides: "AUTH-08/09/11 SC#1~3 통합 단언 (Testcontainers + WireMock + MockMvc)"
      contains: "class BffAuthMeIT extends AbstractIntegrationTest"
  key_links:
    - from: "BffAuthMeIT.meReturnsIdentity"
      to: "GET /api/auth/me"
      via: "MockMvc + oidcLogin(user_id=seededUserId)"
      pattern: "oidcLogin.*claim.*user_id.*seededUserId"
    - from: "BffAuthMeIT.meReturnsGrantedPermissions"
      to: "authorizationService.grantGlobalRole / grantMenuToUser / grantResourceToUser"
      via: "실 Postgres 권한 시드 후 me() 단언"
      pattern: "grantGlobalRole|grantMenuToUser|grantResourceToUser"
---

<objective>
BffAuthMeIT 통합 테스트 클래스를 신규 생성하여 Phase 6 SC#1~3 전체를 자동화 단언한다: SC#1(신원 200/비인증 401), SC#2(권한 단일 응답 결합), SC#3(ArchUnit GREEN). 실 Postgres + WireMock + MockMvc + Testcontainers 인프라 위에서 principal user_id ↔ 시드 User.id ↔ grant.userId 3자 일치 전략으로 집계를 실증한다.

Purpose: Wave 1~2가 구현한 모든 코드 경로(identity read, authorization read, me() 조립, 보안 매처)를 단일 IT에서 행위 단언한다. oidcLogin 우회 금지(lesson 05) — seededUserId를 claim과 일치시켜 집계 거짓 통과를 원천 차단한다.
Output: BffAuthMeIT.java (신규, 6개 이상 테스트).
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-me-aggregation/06-CONTEXT.md
@.planning/phases/06-me-aggregation/06-PATTERNS.md
@.planning/phases/06-me-aggregation/06-RESEARCH.md
@.planning/phases/06-me-aggregation/06-02-SUMMARY.md

<interfaces>
<!-- 테스트 형판 — BffAuthSessionIT 실측 패턴 -->

BffAuthSessionIT 셋업 패턴 (06-PATTERNS.md 발췌):
- @AutoConfigureMockMvc, extends AbstractIntegrationTest
- private static final MockOidcServer OIDC = new MockOidcServer(); static { OIDC.start(); }
- @DynamicPropertySource static void oidcRegistration(DynamicPropertyRegistry registry) — spring.security.oauth2.client.registration.test-idp.* + spring.security.oauth2.client.provider.test-idp.*
- @Autowired MockMvc mvc, IdentityApplicationService identityService
- @LocalServerPort int port, RestTemplate rest = noRedirectRestTemplate()

3자 일치 시드 전략 (Option B — D-07):
1. String email = uniqueEmail("tag"); Long seededUserId = identityService.invite(email);
2. (SC#2) authorizationService.grantGlobalRole/grantMenuToUser/grantResourceToUser(seededUserId, ...)
3. mvc.perform(get("/api/auth/me").with(oidcLogin().userInfoToken(u -> u.claim("user_id", seededUserId))))
4. .andExpect(status().isOk()).andExpect(jsonPath("$.userId").value(seededUserId)) 등

uniqueEmail 패턴 (lesson 05 — 환경 종속 하드코딩 금지):
private static String uniqueEmail(String tag) { return tag + "-" + System.nanoTime() + "@example.com"; }

Wave 1~2 산출물 응답 계약:
- MeResponse: { userId: Long, email: String, status: String, roles: List<String>, menus: [{menuId, role}], resources: [{resourceId, role}] }
- 비인증: 401 (SecurityConfig /api/** 보호 매처)
- 권한 없는 사용자: roles=[], menus=[], resources=[] (null 아닌 빈 배열)

AuthorizationApplicationService 권한 시드 메서드 (실측):
- grantGlobalRole(long userId, RoleName role)
- grantMenuToUser(long userId, Long menuId, RoleName role) — 시그니처 실측 필요 시 소스 확인
- grantResourceToUser(long userId, Long resourceId, RoleName role) — 시그니처 실측 필요 시 소스 확인
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BffAuthMeIT 신규 생성 — SC#1 신원 단언 + SC#3 permitAll 회귀</name>
  <files>
    src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java
  </files>
  <read_first>
    - src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java — 전체(AbstractIntegrationTest 상속, MockOidcServer 셋업, @DynamicPropertySource 패턴, oidcLogin().userInfoToken 사용, uniqueEmail, noRedirectRestTemplate, identityService.invite, MockMvc 셋업, 세션 쿠키 유틸, import 목록)
    - src/test/java/com/anchors/baseline/AbstractIntegrationTest.java — 상속 구조 확인
    - src/test/java/com/anchors/baseline/auth/support/MockOidcServer.java — baseUrl(), stubToken() 시그니처 확인
    - src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java — grantGlobalRole/grantMenuToUser/grantResourceToUser 시그니처 실측
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — BffAuthMeIT 섹션 전체(meReturnsIdentity, meUnauthenticatedReturns401, meIsNotPermitAll 패턴)
    - .planning/phases/06-me-aggregation/06-RESEARCH.md — Validation Architecture, 3자 일치 전략, Pitfall 1(합성 claim 거짓통과)
  </read_first>
  <behavior>
    - meReturnsIdentity(): identityService.invite(uniqueEmail) → seededUserId, oidcLogin claim user_id=seededUserId → GET /api/auth/me → 200 + userId=seededUserId, email=시드email, status="INVITED"
    - meUnauthenticatedReturns401(): 인증 없이 GET /api/auth/me → 401
    - meIsNotPermitAll(): 인증 없이 GET /api/auth/me → 401 AND GET /api/auth/session → 200 (permitAll 회귀)
  </behavior>
  <action>
    src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java 신규 생성.
    클래스 선언: @AutoConfigureMockMvc + extends AbstractIntegrationTest.

    셋업:
    - private static final MockOidcServer OIDC = new MockOidcServer(); static { OIDC.start(); }
    - @DynamicPropertySource: BffAuthSessionIT와 동일한 spring.security.oauth2.client.registration.test-idp.* + spring.security.oauth2.client.provider.test-idp.* 속성 등록 (OIDC.baseUrl() 사용).
    - @Autowired MockMvc mvc, IdentityApplicationService identityService, AuthorizationApplicationService authorizationService, SessionRepository<? extends Session> sessionRepository (필요 시).
    - @LocalServerPort int port; private final RestTemplate rest = noRedirectRestTemplate(); (BffAuthSessionIT 형판 복사).

    테스트 메서드 3개:

    [1] meReturnsIdentity():
      String email = uniqueEmail("sc1");
      Long seededUserId = identityService.invite(email);
      mvc.perform(get("/api/auth/me").with(oidcLogin().userInfoToken(u -> u.claim("user_id", seededUserId))))
         .andExpect(status().isOk())
         .andExpect(jsonPath("$.userId").value(seededUserId))
         .andExpect(jsonPath("$.email").value(email))
         .andExpect(jsonPath("$.status").value("INVITED"));

    [2] meUnauthenticatedReturns401():
      mvc.perform(get("/api/auth/me")).andExpect(status().isUnauthorized());

    [3] meIsNotPermitAll():
      mvc.perform(get("/api/auth/me")).andExpect(status().isUnauthorized());
      mvc.perform(get("/api/auth/session")).andExpect(status().isOk());

    uniqueEmail 헬퍼: private static String uniqueEmail(String tag) { return tag + "-" + System.nanoTime() + "@example.com"; }
    noRedirectRestTemplate: BffAuthSessionIT에서 복사(인스턴스 메서드 또는 static 유틸).

    — 합성 userId(예: 42L 하드코딩) 절대 사용 금지(lesson 05 Pitfall 1).
    — SecurityConfig import/수정 금지.
  </action>
  <verify>
    <automated>./gradlew test --tests "*BffAuthMeIT.meReturnsIdentity" --tests "*BffAuthMeIT.meUnauthenticatedReturns401" --tests "*BffAuthMeIT.meIsNotPermitAll" 2>&1 | tail -15</automated>
  </verify>
  <done>
    - BffAuthMeIT.java 파일 존재, extends AbstractIntegrationTest.
    - meReturnsIdentity/meUnauthenticatedReturns401/meIsNotPermitAll 3개 테스트 GREEN.
    - userId claim = seededUserId 3자 일치 전략 적용됨(하드코딩 userId 없음).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#2 권한 집계 단언 + SC#3 ArchUnit GREEN 최종 실증</name>
  <files>
    src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java
  </files>
  <read_first>
    - src/test/java/com/anchors/baseline/auth/BffAuthMeIT.java — Task 1에서 생성된 현재 상태 확인
    - src/main/java/com/anchors/baseline/authorization/application/AuthorizationApplicationService.java — grantGlobalRole/grantMenuToUser/grantResourceToUser 시그니처 재확인 (menuId/resourceId 타입 Long vs long)
    - src/main/java/com/anchors/baseline/authorization/domain/model/RoleName.java — ADMIN/EDITOR/VIEWER 등 사용 가능한 값 확인
    - .planning/phases/06-me-aggregation/06-PATTERNS.md — meReturnsGrantedPermissions, meReturnsEmptyPermissionsForNewUser 패턴
  </read_first>
  <behavior>
    - meReturnsGrantedPermissions(): invite → seededUserId, grantGlobalRole(ADMIN) + grantMenuToUser(menuId=1L, EDITOR) + grantResourceToUser(resourceId=10L, VIEWER) → me() → roles 포함 ADMIN, menus[0].menuId=1, resources[0].resourceId=10
    - meReturnsEmptyPermissionsForNewUser(): invite → seededUserId, 권한 시드 없음 → me() → roles=[] menus=[] resources=[] (빈 배열)
    - ArchUnit GREEN: ./gradlew test --tests "*ArchitectureTest*" exits 0 (AUTH-11 SC#3 최종 확인)
  </behavior>
  <action>
    BffAuthMeIT.java에 테스트 메서드 2개 추가 (기존 3개 유지, 수술적 추가):

    [4] meReturnsGrantedPermissions():
      String email = uniqueEmail("sc2");
      Long seededUserId = identityService.invite(email);
      authorizationService.grantGlobalRole(seededUserId, RoleName.ADMIN);
      authorizationService.grantMenuToUser(seededUserId, 1L, RoleName.EDITOR);   // 시그니처 실측 후 조정
      authorizationService.grantResourceToUser(seededUserId, 10L, RoleName.VIEWER);  // 시그니처 실측 후 조정
      mvc.perform(get("/api/auth/me").with(oidcLogin().userInfoToken(u -> u.claim("user_id", seededUserId))))
         .andExpect(status().isOk())
         .andExpect(jsonPath("$.roles[0]").value("ADMIN"))
         .andExpect(jsonPath("$.menus[0].menuId").value(1))
         .andExpect(jsonPath("$.menus[0].role").value("EDITOR"))
         .andExpect(jsonPath("$.resources[0].resourceId").value(10))
         .andExpect(jsonPath("$.resources[0].role").value("VIEWER"));

    [5] meReturnsEmptyPermissionsForNewUser():
      String email = uniqueEmail("sc2-empty");
      Long seededUserId = identityService.invite(email);
      mvc.perform(get("/api/auth/me").with(oidcLogin().userInfoToken(u -> u.claim("user_id", seededUserId))))
         .andExpect(status().isOk())
         .andExpect(jsonPath("$.roles").isArray())
         .andExpect(jsonPath("$.roles").isEmpty())
         .andExpect(jsonPath("$.menus").isArray())
         .andExpect(jsonPath("$.menus").isEmpty())
         .andExpect(jsonPath("$.resources").isArray())
         .andExpect(jsonPath("$.resources").isEmpty());

    주의:
    - authorizationService.grantMenuToUser, grantResourceToUser 정확한 시그니처는 실측 후 맞춤(파라미터 순서 Long menuId vs long menuId, RoleName vs String).
    - RoleName import: com.anchors.baseline.authorization.domain.model.RoleName (도메인 enum — test 코드에서는 허용).
    - 합성 userId 하드코딩 금지 — seededUserId 사용.
  </action>
  <verify>
    <automated>./gradlew test --tests "*BffAuthMeIT*" --tests "*ArchitectureTest*" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BffAuthMeIT 5개 테스트 모두 GREEN (meReturnsIdentity, meUnauthenticatedReturns401, meIsNotPermitAll, meReturnsGrantedPermissions, meReturnsEmptyPermissionsForNewUser).
    - ./gradlew test --tests "*ArchitectureTest*" → GREEN (AUTH-11 SC#3 최종 실증).
    - ./gradlew test --rerun-tasks → 전체 63개 이상 테스트 GREEN (기존 58 + 신규 5 이상).
  </done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| 테스트 코드 → Testcontainers postgres:16 | 실 PostgreSQL에서 grant 시드·조회 — H2 사용 금지(lesson 04). |
| oidcLogin().userInfoToken claim | Spring Security Test 유틸 — seededUserId와 일치시켜야 집계 단언이 실질적(D-07 3자 일치). |

## STRIDE 위협 등록부

| Threat ID | 범주 | 구성요소 | 대응 | 완화 계획 |
|-----------|------|----------|------|----------|
| T-06-06 | Information Disclosure | BffAuthMeIT 응답 단언 | mitigate | 응답 필드(userId/email/권한)가 시드 데이터와 정확히 일치하는지 단언 — 타인 데이터 포함 시 테스트 실패로 감지. |
| T-06-07 | Tampering | oidcLogin claim userId 합성값 | mitigate | uniqueEmail + identityService.invite 반환 seededUserId를 claim에 주입 — 3자 일치(D-07). 하드코딩 42L 등 합성 claim 사용 시 테스트가 거짓 통과할 수 있음 — 코드 리뷰에서 하드코딩 userId 금지. |
| T-06-SC | Tampering | npm/pip/cargo 설치 | accept | 신규 외부 패키지 없음 (NFR-01). slopcheck 대상 없음. |
</threat_model>

<verification>
Wave 3 완료 후 최종 Phase gate:

```
./gradlew test --rerun-tasks
```

- 전체 테스트 63개 이상 GREEN (기존 58 + BffAuthMeIT 5개 이상)
- BffAuthMeIT: 5개 테스트, 0 failures, 0 errors
- ArchitectureTest: GREEN (AUTH-11 SC#3 최종 실증)

Phase gate 통과 후 /gsd:verify-work 실행.
</verification>

<success_criteria>
1. BffAuthMeIT.java 신규 존재, AbstractIntegrationTest 상속, @AutoConfigureMockMvc 적용.
2. meReturnsIdentity(): 인증 + 시드 User → 200 + userId/email/status="INVITED" 단언 GREEN.
3. meUnauthenticatedReturns401(): 비인증 → 401 단언 GREEN (SC#1 후단).
4. meIsNotPermitAll(): me → 401, session → 200 회귀 단언 GREEN (D-05 검증).
5. meReturnsGrantedPermissions(): 실 grant 시드 → 200 + roles/menus/resources 포함 단언 GREEN (SC#2).
6. meReturnsEmptyPermissionsForNewUser(): 신규 User → roles=[] menus=[] resources=[] 빈 배열 단언 GREEN (SC#2).
7. ./gradlew test --tests "*ArchitectureTest*" GREEN (AUTH-11 SC#3 최종 실증).
8. ./gradlew test --rerun-tasks → 전체 테스트 GREEN, 회귀 없음.
</success_criteria>

<output>
완료 후 .planning/phases/06-me-aggregation/06-03-SUMMARY.md 생성
</output>
