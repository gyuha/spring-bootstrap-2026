---
phase: 02-identity-context
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/test/java/com/anchors/baseline/identity/domain/model/UserTest.java
  - src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java
autonomous: true
requirements:
  - IDEN-01
  - IDEN-02
  - IDEN-03
  - IDEN-04
  - IDEN-05
  - IDEN-06

must_haves:
  truths:
    - "UserTest(순수 단위)가 linkIdentity 전이/멱등성(IDEN-02/03)·필드 소유권(IDEN-06)·disable 멱등성을 단언한다"
    - "UserLifecycleIT(AbstractIntegrationTest 상속)가 invite→INVITED 저장(IDEN-01)·email UNIQUE 위반(IDEN-05)·disable 시 UserDisabled 발행(IDEN-04)을 단언한다"
    - "UserDisabled 이벤트 단언은 disable() 직후가 아니라 repository.save() 후 @RecordApplicationEvents/ApplicationEvents 로 수집한다 (RESEARCH Pitfall 3 타이밍)"
    - "로그인 후 매칭이 User.id(로컬 PK)로 이루어짐을 통합 테스트가 문서화한다 (IDEN-05, A-6)"
    - "전체 스위트(ArchUnit 포함)가 GREEN 이고 identity 패키지 계층 의존 위반이 0건이다"
    - "Hibernate 매핑이 V2 스키마와 정합해 통합 테스트 컨텍스트가 부팅된다 (ddl-auto: validate, A4/A5 검증)"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/identity/domain/model/UserTest.java"
      provides: "도메인 순수 단위 테스트 — 전이/멱등성/소유권 (Docker 불필요)"
      contains: "linkIdentity"
    - path: "src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java"
      provides: "통합 테스트 — invite/UNIQUE/이벤트 발행/PK 매칭 (Testcontainers)"
      contains: "@RecordApplicationEvents"
  key_links:
    - from: "src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java"
      to: "ApplicationEvents (UserDisabled 수집)"
      via: "repository.save() 후 단언"
      pattern: "ApplicationEvents"
    - from: "src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java"
      to: "AbstractIntegrationTest"
      via: "싱글턴 Testcontainers 상속"
      pattern: "extends AbstractIntegrationTest"
---

<objective>
Phase 2 의 SC#1~5 를 증명하는 **Wave 0 테스트 2종**을 작성하고 전체 스위트를 GREEN 으로 만든다. `UserTest`(순수 단위 — 전이/멱등성/필드 소유권), `UserLifecycleIT`(통합 — invite 저장/email UNIQUE/UserDisabled 발행/로컬 PK 매칭)가 산출물이다.

Purpose: 도메인 계약(Plan 01)과 영속화·유스케이스(Plan 02)가 SC#1~5 와 IDEN-01~06 을 관찰 가능한 행위로 만족함을 자동 검증한다. RESEARCH 의 검증 함정(이벤트 발행 타이밍 Pitfall 3, AbstractAggregateRoot @Transient 스키마 정합 A4, ddl-auto validate 정합 A5, @Embeddable 파생 쿼리 Pitfall 6)을 통합 테스트가 실제로 검증한다. 전방 참조 위험 없음 — Plan 01·02 가 모든 타입을 이미 생성했으므로 @Disabled 우회 불필요.

Output: `UserTest.java`(단위), `UserLifecycleIT.java`(통합), 전체 스위트 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-identity-context/02-CONTEXT.md
@.planning/phases/02-identity-context/02-RESEARCH.md
@.planning/phases/02-identity-context/02-VALIDATION.md

<interfaces>
<!-- Plan 01·02 산출물 + Phase 1 테스트 형판. 재탐색 불필요. -->

From src/test/java/com/anchors/baseline/AbstractIntegrationTest.java (통합 베이스 — 상속):
- @SpringBootTest(webEnvironment=RANDOM_PORT) @ActiveProfiles("test"), 싱글턴 PostgreSQL+Redis Testcontainers. UserLifecycleIT 가 그대로 extends.

From Plan 02-01 (도메인):
- User.invite(Email) → INVITED. User.linkIdentity(String, String). User.disable(). User.getId()/getStatus()/getExternalId()/getDisplayName()/getInternalNote(). 관리자 필드 설정용 의미 메서드(예: updateInternalNote).
- Email VO(@Embeddable, getValue()), UserStatus(INVITED/ACTIVE/DISABLED), UserDisabled(record, Long userId), InvalidStateTransition.

From Plan 02-02 (인프라·서비스):
- UserRepository 포트(save/findById/existsByEmail/findByExternalId), UserJpaRepository 어댑터, IdentityApplicationService(invite/linkIdentity/disable), EmailAlreadyExists.

이벤트 단언(RESEARCH §"이벤트 발행 단언 방법", Pitfall 3):
- @org.springframework.test.context.event.RecordApplicationEvents 를 IT 클래스에 부착, ApplicationEvents 주입.
- UserDisabled 는 disable() 직후가 아니라 **repository.save()(또는 service.disable()이 내부에서 save) 후** 발행됨 → save 후 단언.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserTest 도메인 순수 단위 테스트 작성</name>
  <files>
    src/test/java/com/anchors/baseline/identity/domain/model/UserTest.java
  </files>
  <read_first>
    - Plan 02-01 산출 User.java / Email.java / UserStatus.java / InvalidStateTransition.java
    - .planning/phases/02-identity-context/02-VALIDATION.md §"Per-Task Verification Map"(IDEN-02/03/06 unit)
    - .planning/phases/02-identity-context/02-RESEARCH.md §SC#2·SC#4 단언 방법
  </read_first>
  <behavior>
    - IDEN-02: INVITED User.linkIdentity("oid-1","홍길동") → getStatus()==ACTIVE, getExternalId()=="oid-1", getDisplayName()=="홍길동"
    - IDEN-03 멱등성: linkIdentity 1회 후 재호출 → assertThatThrownBy(...).isInstanceOf(InvalidStateTransition.class)
    - IDEN-06 필드 소유권: 관리자 필드(internalNote)를 도메인 메서드로 설정 후 linkIdentity 호출 → getInternalNote() 불변, getDisplayName() 만 갱신
    - disable 정상: User.disable() → getStatus()==DISABLED
    - disable 멱등성(Open Q1): disable() 두 번 → 예외 없음, status==DISABLED 유지(이벤트 단언은 IT 에서)
    - Email 검증(D-02): Email 부정 형식 입력 → 예외(또는 정상 형식 통과)
  </behavior>
  <action>
    `src/test/java/com/anchors/baseline/identity/domain/model/UserTest.java` 작성. JUnit5 + AssertJ. Testcontainers/Spring 컨텍스트 **불필요**(순수 단위 — 새 객체로 도메인 메서드 직접 호출, Docker 미사용). 위 behavior 6개를 @Test 로 작성. id 가 null 인 신규 객체로도 전이·소유권 단언 가능(영속화 불필요). 관리자 필드 보존 단언은 Plan 01 이 제공한 의미 메서드(예: updateInternalNote)로 internalNote 세팅 후 linkIdentity → 불변 확인.

    이벤트 발행 단언은 이 단위 테스트에 두지 말 것(발행은 save() 시점 — IT 소관, Pitfall 3). 여기선 status 전이·예외·필드 보존만.
  </action>
  <verify>
    <automated>./gradlew test --tests "*UserTest"</automated>
  </verify>
  <acceptance_criteria>
    - UserTest.java 존재, 위 6개 behavior 전부 @Test 로 단언, Spring/Testcontainers 어노테이션 없음
    - `./gradlew test --tests "*UserTest"` GREEN (Docker 없이 ~수초)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserLifecycleIT 통합 테스트 작성 + 전체 스위트 GREEN</name>
  <files>
    src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java
  </files>
  <read_first>
    - src/test/java/com/anchors/baseline/AbstractIntegrationTest.java (상속 베이스)
    - src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java (Phase 1 통합 테스트 형판 — 컨텍스트 주입·트랜잭션 패턴)
    - .planning/phases/02-identity-context/02-RESEARCH.md §"이벤트 발행 단언 방법", §"Common Pitfalls" Pitfall 2(A4)·3·6, §SC#1·3·5
  </read_first>
  <behavior>
    - IDEN-01(SC#1): service.invite("a@x.com") 후 findById → status==INVITED, email 일치
    - IDEN-05(SC#1 UNIQUE): 동일 email 로 invite 재호출 → EmailAlreadyExists(선검사) 또는 DataIntegrityViolationException(DB UNIQUE). 둘 중 하나로 거부됨을 단언
    - IDEN-04(SC#3): invite→linkIdentity→disable 후 ApplicationEvents 에서 UserDisabled 1건, payload userId == 그 User.id. status==DISABLED
    - IDEN-05(SC#5): linkIdentity 후에도 findById(User.id) 로 동일 사용자 조회 가능(불변 PK 매칭) — email/externalId 가 아닌 id 가 업무 참조 키임을 문서화
    - 스키마 정합(A4/A5): 통합 테스트 컨텍스트가 부팅됨 = Hibernate 매핑(AbstractAggregateRoot domainEvents @Transient 포함)이 V2 스키마와 ddl-auto:validate 통과
  </behavior>
  <action>
    `src/test/java/com/anchors/baseline/identity/UserLifecycleIT.java` 작성. `extends AbstractIntegrationTest`, 클래스에 `@RecordApplicationEvents`(org.springframework.test.context.event) 부착. 주입: `IdentityApplicationService`(또는 UserRepository), `ApplicationEvents events`.

    위 behavior 를 @Test 로 작성:
    - invite 후 findById 로 INVITED 단언(IDEN-01)
    - 중복 email invite → 예외 단언(IDEN-05). 선검사가 먼저 잡으면 EmailAlreadyExists, 우회 시 DB 제약. 테스트는 "중복이 거부된다"를 단언(구체 예외 타입은 구현 정합에 맞춰).
    - disable 시나리오: invite→linkIdentity→disable 호출 후, **save 완료 시점 이후** events.stream(UserDisabled.class) 로 1건 수집, userId 단언(IDEN-04, Pitfall 3 타이밍 준수)
    - linkIdentity 후 findById(id) 재조회로 PK 매칭 불변 단언(IDEN-05/SC#5)
    - existsByEmail/findByExternalId 파생 쿼리가 @Embeddable 경로로 정상 동작함을 invite/조회 흐름이 간접 검증(Pitfall 6) — 매핑 오류 시 이 테스트가 RED.

    @Transactional 사용 시 이벤트 발행 타이밍·flush 에 유의(save 가 발행 트리거). 필요하면 명시적 save/flush 또는 서비스 경유로 트랜잭션 내 발행 보장.

    그 후 **전체 스위트** 실행: `./gradlew test`. ArchUnit(기존 게이트)이 신규 identity 패키지에 자동 적용되므로 계층 위반 0 확인. Phase 1 lesson 대로, 완료 단언 전 `--rerun-tasks` 로 독립 재검증(orchestrator 가 수행하나 이 task 도 권장).
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - UserLifecycleIT.java 존재, extends AbstractIntegrationTest, @RecordApplicationEvents 부착
    - IDEN-01/04/05 통합 단언 존재, UserDisabled 수집이 save 후 시점
    - `./gradlew test` 전체 GREEN — ArchUnit 포함, identity 계층 위반 0, 컨텍스트 부팅 성공(매핑↔V2 정합 검증)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| 테스트 → application/domain | SC#1~5 검증을 위해 유스케이스·도메인 불변식을 호출하는 경계 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-02-08 | Repudiation | UserDisabled 발행 누락(이벤트 미발행으로 Phase 4 권한 정리 불가) | mitigate | UserLifecycleIT 가 ApplicationEvents 로 발행을 단언 — 회귀 방어. Pitfall 3 타이밍(save 후) 준수. |
| T-02-09 | Tampering | Hibernate 매핑↔스키마 불일치로 잘못된 컬럼 영속화(A4 domainEvents 매핑 충돌) | mitigate | ddl-auto: validate + 통합 테스트 부팅이 매핑 정합 강제. 불일치 시 컨텍스트 부팅 실패로 즉시 RED. |
| T-02-SC | Tampering | 의존성(공급망) | accept | 신규 외부 패키지 0건 — JUnit5/AssertJ/Testcontainers 모두 Phase 1 존재. |
</threat_model>

<verification>
- `./gradlew test --tests "*UserTest"` GREEN (단위)
- `./gradlew test` 전체 GREEN — ArchUnit + 통합 포함
- 통합 테스트 컨텍스트 부팅 성공 = A4(AbstractAggregateRoot @Transient)·A5(ddl-auto validate↔V2) 검증됨
- grep: UserLifecycleIT 에 `@RecordApplicationEvents`, `ApplicationEvents`, `UserDisabled`
- Phase gate: `./gradlew test --rerun-tasks` 독립 재검증(Phase 1 lesson)
</verification>

<success_criteria>
- IDEN-01~06 전부 단위 또는 통합 테스트로 관찰 가능한 행위 단언
- SC#1~5 증명: invite/linkIdentity 전이·멱등성/disable+이벤트/필드 소유권/로컬 PK 매칭
- 전체 스위트 GREEN, identity 계층 의존 위반 0, 컨텍스트 부팅(매핑 정합)
- Phase 2 verify-work 진입 가능 상태
</success_criteria>

<output>
Create `.planning/phases/02-identity-context/02-03-SUMMARY.md` when done
</output>
