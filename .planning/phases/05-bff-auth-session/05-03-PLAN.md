---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java
autonomous: true
requirements:
  - AUTH-06
  - AUTH-07
  - AUTH-10

must_haves:
  truths:
    - "비인증 GET /api/auth/session → 200 {authenticated:false} (401 없음) — SC#1"
    - "인증 상태 GET /api/auth/session → 200 {authenticated:true, userId:<id>} — SC#1"
    - "인증 후 POST /api/auth/logout(CSRF) → 204, Redis 세션 삭제, 이후 동일 세션 쿠키로 /api/me 401 — SC#2"
    - "GET /api/auth/login → 302 Location: /oauth2/authorization/* — SC#3"
    - "GET /api/auth/login?returnTo=https://evil.com → Location 에 evil.com 미포함 — SC#3 open-redirect"
    - "비인증 /api/auth/session·/api/auth/login 200/302 AND 비인증 /api/me 여전히 401 — SC#4"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java"
      provides: "SC#1~4 전체 커버 통합 테스트"
      exports:
        - "sessionUnauthenticatedReturns200()"
        - "sessionAuthenticatedReturnsUserId()"
        - "logoutReturns204AndInvalidatesSession()"
        - "loginRedirectsToOAuthEndpoint()"
        - "loginRejectsAbsoluteReturnTo()"
        - "permitAllMatchers()"
  key_links:
    - from: "BffAuthSessionIT.logoutReturns204AndInvalidatesSession()"
      to: "sessionRepository.findById(sessionId)"
      via: "Redis read-back (WireMock 실 토큰 교환 필수)"
      pattern: "sessionRepository.findById"
    - from: "BffAuthSessionIT.loginRedirectsToOAuthEndpoint()"
      to: "Location header"
      via: "noRedirectRestTemplate()"
      pattern: "getLocation.*oauth2/authorization"
---

<objective>
AuthController(Wave 2)와 SecurityConfig(Wave 1) 변경이 SC#1~4 를 실제로 충족하는지
Testcontainers + WireMock + MockMvc 통합 테스트로 단언한다.

목적: Phase 5 Goal("순수 인증 엔드포인트 3종이 행위로 검증됨")을 테스트 GREEN 으로 증명한다.
출력: BffAuthSessionIT.java (신규)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bff-auth-session/05-CONTEXT.md
@.planning/phases/05-bff-auth-session/05-RESEARCH.md
@.planning/phases/05-bff-auth-session/05-PATTERNS.md
@.planning/phases/05-bff-auth-session/05-01-SUMMARY.md
@.planning/phases/05-bff-auth-session/05-02-SUMMARY.md

<interfaces>
<!-- Analog: BffAuthIT.java 핵심 인프라 발췌 (BffAuthSessionIT 가 동일 패턴 계승) -->
<!-- src/test/java/com/anchors/baseline/auth/BffAuthIT.java -->

클래스 선언 패턴 (49~100행):
  @AutoConfigureMockMvc
  class BffAuthIT extends AbstractIntegrationTest {
      private static final MockOidcServer OIDC = new MockOidcServer();
      static { OIDC.start(); }

      @DynamicPropertySource
      static void oidcRegistration(DynamicPropertyRegistry registry) {
          String base = OIDC.baseUrl();
          String reg  = "spring.security.oauth2.client.registration.test-idp.";
          registry.add(reg + "client-id", () -> "test-client-id");
          // ... (63~81행 전체 복사)
      }

      @Autowired MockMvc mvc;
      @Autowired SessionRepository<? extends Session> sessionRepository;
      @LocalServerPort int port;
      private final RestTemplate rest = noRedirectRestTemplate();
  }

performWireMockLogin() 패턴 (244~274행) — SC#2·SC#3 WireMock 흐름에 재사용.
logout() 헬퍼 패턴 (276~293행) — 경로는 "/api/auth/logout" 으로 이미 변경됨(Wave 1).
meStatus() 헬퍼 (295~301행).
noRedirectRestTemplate() (309~331행).
decodeSessionId() (362~365행).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BffAuthSessionIT 신설 — SC#1(session), SC#4(permitAll) 단언</name>
  <files>src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java</files>
  <read_first>
    - Analog: src/test/java/com/anchors/baseline/auth/BffAuthIT.java (전체 — DynamicPropertySource·MockOidcServer·WireMock 흐름·저수준 유틸 패턴)
    - 근거: 05-PATTERNS.md "3. auth/BffAuthSessionIT.java" SC#1 단언 패턴, SC#4 단언 패턴
    - 05-RESEARCH.md "Validation Architecture → SC별 단언 설계 상세 SC#1·SC#4"
    - D-07(CONTEXT.md): Testcontainers + WireMock + MockMvc. AbstractIntegrationTest 상속. 라이브 구동 없음.
    - SC#1 비인증: MockMvc no-auth GET /api/auth/session → 200 {authenticated:false}. userId 키 미존재.
    - SC#1 인증: oidcLogin() post-processor (WireMock 불필요 — 상태 단언만). user_id claim 주입.
    - SC#4: 비인증 /api/auth/session 200, 비인증 /api/auth/login 302(아니면 200이어도 401만 아니면 통과), 비인증 /api/me 401.
  </read_first>
  <behavior>
    - 클래스: @AutoConfigureMockMvc, extends AbstractIntegrationTest
    - MockOidcServer OIDC static 필드 + static { OIDC.start(); }
    - @DynamicPropertySource oidcRegistration() — BffAuthIT.java 63~81행 패턴 그대로 복사 (registration 동일)
    - @Autowired MockMvc mvc, @Autowired SessionRepository<? extends Session> sessionRepository, @LocalServerPort int port
    - private final RestTemplate rest = noRedirectRestTemplate()
    - sessionUnauthenticatedReturns200(): GET /api/auth/session (no auth) → status 200, authenticated==false, userId 키 없음
    - sessionAuthenticatedReturnsUserId(): oidcLogin() post-processor (user_id claim 42L) → status 200, authenticated==true, userId==42
    - permitAllMatchers(): GET /api/auth/session → isOk(), GET /api/auth/login → is3xxRedirection() (not 401), GET /api/me → isUnauthorized()
    - BffAuthIT 와 동일한 저수준 유틸(baseUrl, noRedirectRestTemplate, sessionCookieFrom, cookieValue, decodeSessionId, uniqueEmail, uniqueOid, LoginResult, queryParams) 를 클래스 내부에 복사
  </behavior>
  <action>
    src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java 를 신규 생성한다.

    패키지: com.anchors.baseline.auth.

    클래스 구조: @AutoConfigureMockMvc, extends AbstractIntegrationTest.

    static 인프라: MockOidcServer OIDC + static { OIDC.start(); } + @DynamicPropertySource oidcRegistration() (BffAuthIT.java 63~81행 완전 동일 — registration 명은 "test-idp").

    필드: @Autowired MockMvc mvc, @Autowired SessionRepository<? extends Session> sessionRepository, @LocalServerPort int port, private final RestTemplate rest = noRedirectRestTemplate().

    테스트 메서드 3개 (SC#1·SC#4):

    (1) sessionUnauthenticatedReturns200():
      mvc.perform(get("/api/auth/session")) andExpect status isOk(), authenticated==false, userId doesNotExist.

    (2) sessionAuthenticatedReturnsUserId():
      mvc.perform(get("/api/auth/session").with(oidcLogin().userInfoToken(u -> u.claim("user_id", 42L))))
      andExpect status isOk(), authenticated==true, userId==42.

    (3) permitAllMatchers():
      mvc.perform(get("/api/auth/session")) → isOk() (비인증 200)
      mvc.perform(get("/api/auth/login")) → is3xxRedirection() (비인증 302, 401 아님)
      mvc.perform(get("/api/me")) → isUnauthorized() (기존 보호 API 불변)

    저수준 유틸: BffAuthIT.java 의 baseUrl(), noRedirectRestTemplate(), requireSessionCookie(), sessionCookieFrom(), cookieValue(), decodeSessionId(), queryParams(), uniqueEmail(), uniqueOid(), LoginResult record 를 BffAuthSessionIT 에도 private 메서드/record 로 복사한다.

    임포트: BffAuthIT.java 의 static import + spring/junit 임포트 패턴 계승.
  </action>
  <verify>
    <automated>./gradlew test --tests "*BffAuthSessionIT*" -x javadoc 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - BffAuthSessionIT 전체 GREEN: ./gradlew test --tests "*BffAuthSessionIT*" exits 0
    - sessionUnauthenticatedReturns200() 통과: 비인증 GET /api/auth/session → 200, authenticated==false
    - sessionAuthenticatedReturnsUserId() 통과: oidcLogin() with user_id=42 → 200, authenticated==true, userId==42
    - permitAllMatchers() 통과: /api/auth/session 200, /api/auth/login 302, /api/me 401
    - 파일 존재: ls src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java
  </acceptance_criteria>
  <done>SC#1 과 SC#4 를 단언하는 테스트 3개가 GREEN 이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BffAuthSessionIT — SC#2(logout 204) + SC#3(login 302·returnTo·open-redirect) 단언</name>
  <files>src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java</files>
  <read_first>
    - 수정 대상: src/test/java/com/anchors/baseline/auth/BffAuthSessionIT.java (Task 1 에서 생성)
    - 근거: 05-PATTERNS.md SC#2 단언 패턴(logoutReturns204AndInvalidatesSession), SC#3 단언 패턴(loginRedirectsToOAuthEndpoint, loginRejectsAbsoluteReturnTo)
    - 05-RESEARCH.md "SC별 단언 설계 상세 SC#2·SC#3"
    - SC#2 는 WireMock 실 토큰 교환 필수(서버측 Redis 세션 read-back — Phase 3 lesson: oidcLogin 은 실 세션을 우회함)
    - SC#2 logout 헬퍼: BffAuthIT.logout() 패턴 복사, URL 은 "/api/auth/logout" (Wave 1 에서 이미 갱신됨)
    - SC#3 returnTo: WireMock 전체 흐름 후 최종 Location 이 "/dashboard" 인지 확인
    - SC#3 open-redirect: returnTo=https://evil.com → Location 에 "evil.com" 미포함
  </read_first>
  <behavior>
    - logoutReturns204AndInvalidatesSession():
      WireMock performWireMockLogin → SESSION 쿠키·sessionId 획득
      POST /api/auth/logout (X-XSRF-TOKEN CSRF) → status 204 (302 아님)
      sessionRepository.findById(sessionId) → null (Redis 세션 삭제)
      GET /api/me with old SESSION cookie → 401
    - loginRedirectsToOAuthEndpoint():
      noRedirectRestTemplate GET /api/auth/login → status 302
      Location 헤더 contains "/oauth2/authorization/"
    - loginWithReturnToRedirectsAfterLogin():
      GET /api/auth/login?returnTo=/dashboard (noRedirectRestTemplate)
      WireMock 전체 흐름(performWireMockLogin 변형) → 최종 Location == "/dashboard"
      단, returnTo 세션 저장 후 oauth2Login successHandler 가 복원하는 전체 흐름 검증
    - loginRejectsAbsoluteReturnTo():
      GET /api/auth/login?returnTo=https://evil.com → 302 이고 Location 에 "evil.com" 미포함
      GET /api/auth/login?returnTo=//evil.com → 302 이고 Location 에 "evil.com" 미포함
    - logout 헬퍼(private): BffAuthIT.logout() 패턴 계승, URL 은 "/api/auth/logout"
    - performWireMockLogin(oid, email): BffAuthIT.performWireMockLogin() 패턴 계승 (단, OIDC 를 BffAuthSessionIT.OIDC 참조)
    - WireMock 로그인은 identityService.invite(email) 선행 필요 (BffAuthIT 패턴 참조)
  </behavior>
  <action>
    BffAuthSessionIT.java 에 다음을 추가한다.

    필드 추가:
      @Autowired IdentityApplicationService identityService;

    메서드 추가 — logoutReturns204AndInvalidatesSession():
      email/oid 를 uniqueEmail("sc2")/uniqueOid() 로 생성.
      identityService.invite(email) 호출.
      performWireMockLogin(oid, email) 로 LoginResult 획득.
      sessionId = decodeSessionId(login.sessionCookie()).
      logout(login) 호출 → logoutResp 의 status == 204 단언.
      sessionRepository.findById(sessionId) → null 단언.
      meStatus(login.sessionCookie()) → UNAUTHORIZED 단언.

    메서드 추가 — loginRedirectsToOAuthEndpoint():
      rest.exchange(baseUrl() + "/api/auth/login", GET, EMPTY, Void.class).
      status 302 단언.
      Location.toString() contains "/oauth2/authorization/" 단언.

    메서드 추가 — loginRejectsAbsoluteReturnTo():
      rest.exchange(baseUrl() + "/api/auth/login?returnTo=https://evil.com", GET, EMPTY, Void.class).
      Location.toString() doesNotContain("evil.com") 단언.
      rest.exchange(baseUrl() + "/api/auth/login?returnTo=//evil.com", GET, EMPTY, Void.class).
      Location.toString() doesNotContain("evil.com") 단언.

    메서드 추가 — performWireMockLogin(String oid, String email): BffAuthIT.performWireMockLogin() 완전 동일 구현 복사 (단, OIDC 는 BffAuthSessionIT.OIDC).

    메서드 추가 — logout(LoginResult login): BffAuthIT.logout() 패턴 복사, URL "/api/auth/logout".
      반환 타입 ResponseEntity<Void> 로 변경(status 204 단언을 위해).

    메서드 추가 — meStatus(String sessionCookie): BffAuthIT.meStatus() 완전 동일 복사.

    추가 임포트:
      com.anchors.baseline.identity.application.IdentityApplicationService
      com.anchors.baseline.auth.support.MockOidcServer (이미 있을 수 있음)
      기타 BffAuthIT 임포트 중 아직 없는 것들 (ResponseEntity, URI 등)
  </action>
  <verify>
    <automated>./gradlew test --tests "*BffAuthSessionIT*" --tests "*BffAuthIT*" --tests "*ArchitectureTest*" -x javadoc 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - BffAuthSessionIT 전체 GREEN: ./gradlew test --tests "*BffAuthSessionIT*" exits 0
    - logoutReturns204AndInvalidatesSession() 통과: status 204, sessionRepository.findById == null, meStatus == UNAUTHORIZED
    - loginRedirectsToOAuthEndpoint() 통과: status 302, Location contains "/oauth2/authorization/"
    - loginRejectsAbsoluteReturnTo() 통과: Location 에 "evil.com" 미포함
    - BffAuthIT GREEN 유지: ./gradlew test --tests "*BffAuthIT*" exits 0 (Wave 1·2 회귀 없음)
    - ArchUnit GREEN: ./gradlew test --tests "*ArchitectureTest*" exits 0
    - 전체 테스트 GREEN: ./gradlew test -x javadoc exits 0
  </acceptance_criteria>
  <done>SC#1~4 전체를 커버하는 BffAuthSessionIT 가 GREEN 이고, BffAuthIT 및 ArchUnit 도 GREEN 이다. Phase 5 Goal 행위 증명 완료.</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| BffAuthSessionIT→SecurityConfig | 테스트가 실제 필터 체인을 통과해 단언 — MockMvc 는 Spring Security 필터 체인 전체를 태운다 |
| WireMock OIDC→Redis 세션 | performWireMockLogin 은 실 토큰 교환 경로를 사용 — oidcLogin() 대비 충실도 높음 (SC#2 Redis read-back 필수) |

## STRIDE 위협 레지스터

| 위협 ID | 카테고리 | 컴포넌트 | 처리 방침 | 완화 계획 |
|---------|---------|---------|---------|---------|
| T-05-07 | Repudiation | SC#2 logout Redis 세션 삭제 단언 | mitigate | sessionRepository.findById(sessionId) == null 를 직접 단언 (서버측 상태 read-back — Phase 3 lesson) |
| T-05-08 | Spoofing | SC#3 open-redirect 단언 | mitigate | loginRejectsAbsoluteReturnTo() 에서 https:// 및 // 두 케이스 모두 단언. 프로덕션 코드의 isSafeRelativePath() 검증을 이 테스트가 회귀 방지 |
| T-05-09 | Elevation of Privilege | SC#4 permitAll 과다 노출 회귀 | mitigate | permitAllMatchers() 에서 /api/me 비인증 401 을 명시적으로 단언 — permitAll 범위가 확장되면 이 테스트가 RED 로 즉시 감지 |
| T-05-SC | Tampering | 신규 외부 패키지 없음 | accept | NFR-01 — 테스트도 기존 WireMock/Testcontainers 재사용. slopcheck 불필요 |
</threat_model>

<verification>
Phase 5 완료 조건 — 다음 전체 GREEN:

1. ./gradlew test -x javadoc exits 0 (BffAuthSessionIT + BffAuthIT + ArchitectureTest 모두 포함)
2. SC#1: sessionUnauthenticatedReturns200(), sessionAuthenticatedReturnsUserId() GREEN
3. SC#2: logoutReturns204AndInvalidatesSession() GREEN (Redis sessionRepository.findById == null 포함)
4. SC#3: loginRedirectsToOAuthEndpoint() GREEN, loginRejectsAbsoluteReturnTo() GREEN
5. SC#4: permitAllMatchers() GREEN (/api/me 비인증 401 유지 포함)
6. ArchUnit GREEN (D-06 — 신규 코드가 기존 계층 규칙 자연 통과)
</verification>

<success_criteria>
- BffAuthSessionIT.java 신규 생성
- SC#1~4 단언 전체 GREEN
- Phase 5 ROADMAP SC#1: 비인증 session 200 + 인증 session 200(userId 포함)
- Phase 5 ROADMAP SC#2: logout 204 + HttpSession 무효화 + Redis 세션 삭제 + 이후 401
- Phase 5 ROADMAP SC#3: login 302 + returnTo 복귀 + open-redirect 방지
- Phase 5 ROADMAP SC#4: session·login permitAll AND api/me 401 불변
- BffAuthIT 회귀 없음 (Wave 1 갱신 포함)
- ArchUnit GREEN (D-06)
</success_criteria>

<output>
완료 시 .planning/phases/05-bff-auth-session/05-03-SUMMARY.md 를 생성하라.
</output>
