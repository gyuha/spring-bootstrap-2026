---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java
  - src/test/java/com/anchors/baseline/auth/BffAuthIT.java
autonomous: true
requirements:
  - AUTH-07

must_haves:
  truths:
    - "POST /api/auth/logout 는 302 대신 204 No Content 를 반환한다"
    - "로그아웃 후 HttpSession 무효화·SESSION 쿠키 삭제·인증 클리어가 수행된다"
    - "기존 BffAuthIT.logoutInvalidatesSession() 이 GREEN 을 유지한다"
    - "/api/auth/session 과 /api/auth/login 이 permitAll 매처에 등록되어 비인증 접근 시 401 을 내보내지 않는다"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java"
      provides: "logoutUrl=/api/auth/logout + HttpStatusReturningLogoutSuccessHandler(NO_CONTENT) + permitAll 2개 추가 + oauth2Login successHandler(returnTo 복원)"
      contains: "/api/auth/logout"
    - path: "src/test/java/com/anchors/baseline/auth/BffAuthIT.java"
      provides: "logout() 헬퍼가 /api/auth/logout 을 호출"
      contains: "/api/auth/logout"
  key_links:
    - from: "SecurityConfig.logout DSL"
      to: "HttpStatusReturningLogoutSuccessHandler"
      via: "logoutSuccessHandler() 메서드"
      pattern: "HttpStatusReturningLogoutSuccessHandler.*NO_CONTENT"
    - from: "SecurityConfig.authorizeHttpRequests"
      to: "/api/auth/session, /api/auth/login"
      via: "requestMatchers permitAll"
      pattern: "api/auth/session.*api/auth/login|api/auth/login.*api/auth/session"
---

<objective>
SecurityConfig 를 외과적으로 수정하여 AUTH-07(JSON 로그아웃 204) 와 SC#4(permitAll 매처 순서)를 달성하고,
logoutUrl 변경으로 깨지는 BffAuthIT.logout() 헬퍼 경로를 동반 갱신한다.

목적: Wave 2·3 에서 AuthController 와 통합 테스트가 올라올 수 있는 기반 인프라를 확보한다.
출력: SecurityConfig(3곳 수정) + BffAuthIT(1행 수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bff-auth-session/05-CONTEXT.md
@.planning/phases/05-bff-auth-session/05-RESEARCH.md
@.planning/phases/05-bff-auth-session/05-PATTERNS.md

<interfaces>
<!-- 수정 대상 SecurityConfig.java 현재 상태 — 3곳 외과적 수정 위치 -->
<!-- src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java -->

현재 logout DSL (56~60행):
  .logout(logout -> logout
          .logoutUrl("/logout")            ← 재지정 대상
          .invalidateHttpSession(true)
          .clearAuthentication(true)
          .deleteCookies("SESSION"))       ← logoutSuccessHandler 추가 대상

현재 permitAll 매처 (53~55행):
  .requestMatchers("/actuator/health", "/login/**", "/oauth2/**",
          "/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html").permitAll()
                                           ← "/api/auth/session", "/api/auth/login" 추가 대상

현재 oauth2Login (76~79행):
  http.oauth2Login(oauth -> oauth
          .userInfoEndpoint(ui -> ui.oidcUserService(baselineOidcUserService)));
                                           ← successHandler 추가 대상

<!-- BffAuthIT.java 회귀 수정 위치 -->
<!-- src/test/java/com/anchors/baseline/auth/BffAuthIT.java -->

logout() 헬퍼 292행:
  rest.exchange(baseUrl() + "/logout", ...)    ← "/api/auth/logout" 로 변경
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecurityConfig — logoutUrl 재지정·204 핸들러·permitAll 2개 추가·returnTo successHandler</name>
  <files>src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java</files>
  <read_first>
    - 수정 대상: src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java (현재 56~60행 logout DSL, 53~55행 permitAll 매처, 76~79행 oauth2Login)
    - Analog/근거: 05-PATTERNS.md "수정 1~3" 발췌 코드 + 05-RESEARCH.md Pattern 1·4
    - classpath 실측 근거: 05-RESEARCH.md "핵심 확정 사항" — HttpStatusReturningLogoutSuccessHandler(6.5.1 존재), logoutSuccessHandler() 설정이 invalidateHttpSession/clearAuthentication/deleteCookies 에 영향 없음
  </read_first>
  <behavior>
    - logout DSL: logoutUrl 을 "/logout" 에서 "/api/auth/logout" 으로 재지정 (D-02 per CONTEXT.md)
    - logout DSL: deleteCookies("SESSION") 뒤에 logoutSuccessHandler(new HttpStatusReturningLogoutSuccessHandler(HttpStatus.NO_CONTENT)) 추가 (D-02)
    - permitAll 매처: 기존 requestMatchers 목록 끝에 "/api/auth/session", "/api/auth/login" 추가 (D-05 per CONTEXT.md)
    - oauth2Login successHandler: BaselineOidcUserService 연결 후 successHandler 람다 추가 — 세션에서 "RETURN_TO" attribute 를 꺼내 리다이렉트, 없으면 "/" (D-04 per CONTEXT.md)
    - 신규 import 추가: HttpStatusReturningLogoutSuccessHandler, jakarta.servlet.http.HttpSession (SecurityConfig 상단)
    - "/api/auth/logout" 은 permitAll 에 추가하지 않는다 (D-05)
    - 기존 /api/** 401 엔트리포인트(65~69행) 무변경
    - CSRF CookieCsrfTokenRepository 및 CsrfCookieFilter 무변경
    - fenced code block 사용 금지 — 수정 사항은 이 지시에 따라 파일 직접 편집
  </behavior>
  <action>
    SecurityConfig.java 를 Read 한 뒤 Edit 도구로 다음 3곳을 수정한다.

    수정 1 — logout DSL (56~60행):
    기존 .logoutUrl("/logout") 을 .logoutUrl("/api/auth/logout") 으로 변경.
    .deleteCookies("SESSION") 다음 줄에 .logoutSuccessHandler(new HttpStatusReturningLogoutSuccessHandler(HttpStatus.NO_CONTENT)) 추가.

    수정 2 — permitAll 매처 (53~55행):
    기존 requestMatchers 문자열 목록 마지막("/swagger-ui.html") 뒤에 쉼표로 이어 "/api/auth/session", "/api/auth/login" 을 추가한다. 매처가 길어지므로 기존 줄 바꿈 스타일 유지.

    수정 3 — oauth2Login successHandler (76~79행):
    .userInfoEndpoint(ui -> ui.oidcUserService(baselineOidcUserService)) 뒤에 메서드 체이닝으로
    .successHandler((request, response, authentication) -> { ... }) 를 추가한다.
    successHandler 람다 구현:
      HttpSession session = request.getSession(false);
      String returnTo = (session != null) ? (String) session.getAttribute("RETURN_TO") : null;
      if (session != null) { session.removeAttribute("RETURN_TO"); }
      response.sendRedirect(returnTo != null ? returnTo : "/");

    import 추가:
    기존 import 목록(LogoutFilter import 줄 아래 등 적절한 위치)에 추가:
      import org.springframework.security.web.authentication.logout.HttpStatusReturningLogoutSuccessHandler;
      import jakarta.servlet.http.HttpSession;
  </action>
  <verify>
    <automated>./gradlew compileJava --rerun-tasks 2>&1 | tail -10 && ./gradlew test --tests "*ArchitectureTest*" -x javadoc 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - **BffAuthIT RED 는 의도적** — BffAuthIT.logoutInvalidatesSession() 은 이 Task 이후 RED 가 정상(logout() 헬퍼가 아직 /logout 을 호출하므로). Task 2 에서 GREEN 으로 전환. compileJava EXIT 0 과 ArchitectureTest GREEN 만 확인할 것.
    - 컴파일 에러 없이 빌드 성공: ./gradlew compileJava exits 0
    - SecurityConfig.java 에 "/api/auth/logout" 문자열이 포함됨: grep -c "/api/auth/logout" src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java >= 1
    - SecurityConfig.java 에 "HttpStatusReturningLogoutSuccessHandler" 가 포함됨: grep -c "HttpStatusReturningLogoutSuccessHandler" src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java >= 1
    - SecurityConfig.java 에 "/api/auth/session" 과 "/api/auth/login" 이 포함됨: grep -c "api/auth/session" src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java >= 1
  </acceptance_criteria>
  <done>SecurityConfig 가 컴파일되고 3곳 수정이 반영된다. BffAuthIT.logoutInvalidatesSession() 은 Task 2 수정 전까지 RED 상태일 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: BffAuthIT.logout() 헬퍼 경로 /logout → /api/auth/logout 갱신</name>
  <files>src/test/java/com/anchors/baseline/auth/BffAuthIT.java</files>
  <read_first>
    - 수정 대상: src/test/java/com/anchors/baseline/auth/BffAuthIT.java, 292행
    - 근거: 05-PATTERNS.md "수정 위치: BffAuthIT.java 292행" + 05-RESEARCH.md Pitfall 1 (logoutUrl 변경이 기존 테스트를 무음 파괴)
    - 변경 범위: 단 1행. 나머지 BffAuthIT 코드는 절대 변경하지 않는다.
  </read_first>
  <action>
    BffAuthIT.java 292행에서 "/logout" 을 "/api/auth/logout" 으로 변경한다.
    변경 전: rest.exchange(baseUrl() + "/logout", HttpMethod.POST, ...
    변경 후: rest.exchange(baseUrl() + "/api/auth/logout", HttpMethod.POST, ...
    이 1행 외에 BffAuthIT.java 의 어떤 코드도 변경하지 않는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*BffAuthIT*" --tests "*ArchitectureTest*" -x javadoc 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - BffAuthIT 전체 테스트 GREEN: ./gradlew test --tests "*BffAuthIT*" exits 0
    - BffAuthIT.logoutInvalidatesSession() 통과: 로그아웃 후 sessionRepository.findById == null 및 meStatus == 401 단언 성공
    - ArchitectureTest GREEN 유지: ./gradlew test --tests "*ArchitectureTest*" exits 0
    - BffAuthIT.java 292행 변경 확인: grep -c "api/auth/logout" src/test/java/com/anchors/baseline/auth/BffAuthIT.java >= 1
    - BffAuthIT.java 에 "/logout" 하드코딩(경로 문자열) 미존재: grep -v "api/auth/logout" src/test/java/com/anchors/baseline/auth/BffAuthIT.java | grep -c '"/logout"' == 0
  </acceptance_criteria>
  <done>BffAuthIT 전체 통과. SecurityConfig logoutUrl 변경과 BffAuthIT 경로가 동기화되어 회귀 없음.</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| 클라이언트→SecurityConfig logout DSL | POST /api/auth/logout 는 CSRF 토큰 검증을 통과해야 처리된다 |
| SecurityConfig permitAll 매처→/api/** 엔트리포인트 | 구체 매처(/api/auth/session·/api/auth/login)가 광역 매처(/api/**)보다 먼저 선언되어야 한다 |

## STRIDE 위협 레지스터

| 위협 ID | 카테고리 | 컴포넌트 | 처리 방침 | 완화 계획 |
|---------|---------|---------|---------|---------|
| T-05-01 | Tampering | POST /api/auth/logout | mitigate | 기존 CookieCsrfTokenRepository + X-XSRF-TOKEN 헤더 검증 유지. logoutUrl 변경 후에도 CSRF 처리 경로는 동일 (Spring Security LogoutFilter 가 CSRF 검증 선행) |
| T-05-02 | Elevation of Privilege | authorizeHttpRequests permitAll 과다 노출 | mitigate | /api/auth/session 과 /api/auth/login 만 permitAll 추가. /api/auth/logout 은 추가 안 함. /api/** 401 엔트리포인트 불변 유지. 매처 순서(구체 먼저) 필수 — SC#4 테스트로 검증 |
| T-05-03 | Information Disclosure | oauth2Login successHandler returnTo 노출 | accept | returnTo 는 세션 attribute 에만 저장, 응답 바디에 미포함. 세션은 Redis 서버 측에만 보관(NFR-02). 저위험 |
| T-05-SC | Tampering | SecurityConfig 수정 (npm/pip 아닌 Java) | accept | 신규 외부 패키지 없음(NFR-01). 기존 spring-security-web jar 에서 HttpStatusReturningLogoutSuccessHandler 사용 — Package Legitimacy Audit 불필요 |
</threat_model>

<verification>
Wave 1 완료 후 다음이 모두 GREEN 이어야 다음 Wave 로 진행한다:

1. ./gradlew test --tests "*BffAuthIT*" exits 0 (기존 AUTH-01~05 회귀 없음)
2. ./gradlew test --tests "*ArchitectureTest*" exits 0 (ArchUnit 게이트 무변경)
3. grep -c "/api/auth/logout" src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java >= 1
4. grep -c "api/auth/session" src/main/java/com/anchors/baseline/auth/infrastructure/security/SecurityConfig.java >= 1
</verification>

<success_criteria>
- SecurityConfig.logoutUrl 이 "/api/auth/logout" 으로 재지정됨
- SecurityConfig 에 HttpStatusReturningLogoutSuccessHandler(NO_CONTENT) 가 설정됨
- SecurityConfig permitAll 매처에 "/api/auth/session"·"/api/auth/login" 이 추가됨
- SecurityConfig oauth2Login 에 "RETURN_TO" 세션 attribute 복원 successHandler 가 연결됨
- BffAuthIT.logout() 헬퍼가 "/api/auth/logout" 으로 POST 함
- BffAuthIT 전체 GREEN (기존 AUTH-01~05 테스트 회귀 없음)
- ArchUnit GREEN (D-06 — 무변경 확인)
</success_criteria>

<output>
완료 시 .planning/phases/05-bff-auth-session/05-01-SUMMARY.md 를 생성하라.
</output>
