---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java
autonomous: true
requirements:
  - AUTH-06
  - AUTH-10

must_haves:
  truths:
    - "GET /api/auth/session 은 비인증 시 200 {authenticated:false} 를 반환한다 (401 아님)"
    - "GET /api/auth/session 은 인증 시 200 {authenticated:true, userId:<로컬 User.id>} 를 반환한다"
    - "GET /api/auth/login 은 /oauth2/authorization/{registration} 으로 302 리다이렉트한다"
    - "GET /api/auth/login?returnTo=https://evil.com 은 외부 URL 을 무시하고 / 로 복귀를 설정한다"
    - "AuthController 는 infrastructure 타입(BaselineOidcUser) 을 import/cast 하지 않는다"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java"
      provides: "GET /api/auth/session(AUTH-06), GET /api/auth/login(AUTH-10)"
      exports:
        - "public SessionResponse session(@AuthenticationPrincipal OidcUser principal)"
        - "public void login(@RequestParam String returnTo, HttpSession session, HttpServletResponse response)"
  key_links:
    - from: "AuthController.session()"
      to: "OidcUser.getAttribute(\"user_id\")"
      via: "표준 attribute 읽기 (MeController 동일 패턴)"
      pattern: "getAttribute.*user_id"
    - from: "AuthController.login()"
      to: "/oauth2/authorization/{registrationId}"
      via: "response.sendRedirect()"
      pattern: "sendRedirect.*oauth2/authorization"
---

<objective>
auth/interfaces 계층에 AuthController 를 신설하여 세션 상태 조회(AUTH-06)와 OIDC 로그인 진입 래퍼(AUTH-10)를 구현한다.
MeController 의 OidcUser 표준 읽기 패턴을 계승해 ArchUnit 게이트를 자연 통과한다.

목적: SPA 가 401 노이즈 없이 인증 여부를 판별하고 OIDC 로그인에 진입하는 BFF 표면을 제공한다.
출력: AuthController.java (신규)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bff-auth-session/05-CONTEXT.md
@.planning/phases/05-bff-auth-session/05-RESEARCH.md
@.planning/phases/05-bff-auth-session/05-PATTERNS.md
@.planning/phases/05-bff-auth-session/05-01-SUMMARY.md

<interfaces>
<!-- Analog: MeController.java — AuthController 가 계승하는 패턴 -->
<!-- src/main/java/com/anchors/baseline/auth/interfaces/MeController.java -->

package com.anchors.baseline.auth.interfaces;

import java.util.Collections;
import java.util.Map;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.oauth2.core.oidc.user.OidcUser;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MeController {
    @GetMapping("/api/me")
    public Map<String, Object> me(@AuthenticationPrincipal OidcUser principal) {
        return Collections.singletonMap("userId", principal.getAttribute("user_id"));
    }
}

<!-- Wave 1 수정 완료 후 SecurityConfig permitAll 매처에 등록된 경로 -->
<!-- /api/auth/session → permitAll (비인증 접근 가능) -->
<!-- /api/auth/login  → permitAll (비인증 접근 가능) -->

<!-- open-redirect 방지 검증 로직 (RESEARCH.md Pattern 3, classpath 실측) -->
private static boolean isSafeRelativePath(String url) {
    return url != null
        && url.startsWith("/")
        && !url.startsWith("//")
        && !url.contains("://");
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthController 신설 — session 엔드포인트 (AUTH-06, SC#1)</name>
  <files>src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java</files>
  <read_first>
    - Analog: src/main/java/com/anchors/baseline/auth/interfaces/MeController.java (전체 — OidcUser.getAttribute 패턴, 임포트 패턴)
    - 근거: 05-PATTERNS.md "1. auth/interfaces/AuthController.java" 발췌 코드, Pattern 2
    - ArchUnit 제약 (D-06): auth/interfaces 는 Application·Domain 계층만 의존. BaselineOidcUser import/cast 금지
    - Map.of 금지 (lesson 03): null 값 NPE → record DTO + @JsonInclude(NON_NULL) 사용
  </read_first>
  <behavior>
    - 패키지: com.anchors.baseline.auth.interfaces
    - 클래스: @RestController @RequestMapping("/api/auth") public class AuthController
    - GET /api/auth/session: @GetMapping("/session"), 파라미터 @AuthenticationPrincipal OidcUser principal (nullable)
      - principal == null → new SessionResponse(false, null)
      - principal != null → new SessionResponse(true, principal.getAttribute("user_id"))
    - SessionResponse: record DTO. boolean authenticated, @JsonInclude(JsonInclude.Include.NON_NULL) Object userId
    - infrastructure 타입(BaselineOidcUser) import 없음 — OidcUser.getAttribute("user_id") 표준 읽기만 사용
    - Map.of 사용 금지
  </behavior>
  <action>
    src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java 를 신규 생성한다.

    클래스 선언: com.anchors.baseline.auth.interfaces 패키지, @RestController, @RequestMapping("/api/auth").

    session() 메서드: @GetMapping("/session"), @AuthenticationPrincipal OidcUser principal 를 nullable 로 수신.
    principal 이 null 이면 new SessionResponse(false, null) 반환.
    null 이 아니면 new SessionResponse(true, principal.getAttribute("user_id")) 반환.

    SessionResponse 내부 record: boolean authenticated, @JsonInclude(JsonInclude.Include.NON_NULL) Object userId.
    com.fasterxml.jackson.annotation.JsonInclude 임포트 필요.

    임포트 목록 (MeController.java 임포트 패턴 계승):
      import com.fasterxml.jackson.annotation.JsonInclude;
      import org.springframework.security.core.annotation.AuthenticationPrincipal;
      import org.springframework.security.oauth2.core.oidc.user.OidcUser;
      import org.springframework.web.bind.annotation.GetMapping;
      import org.springframework.web.bind.annotation.RequestMapping;
      import org.springframework.web.bind.annotation.RestController;
    (login 메서드는 Task 2 에서 같은 파일에 추가)
  </action>
  <verify>
    <automated>./gradlew compileJava --rerun-tasks 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: ls src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java
    - 컴파일 성공: ./gradlew compileJava exits 0
    - BaselineOidcUser 미참조: grep -c "BaselineOidcUser" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java == 0
    - session 메서드 존재: grep -c "GetMapping.*session\|session.*GetMapping" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 1
    - SessionResponse record 존재: grep -c "record SessionResponse" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 1
  </acceptance_criteria>
  <done>AuthController.java 가 생성되고, session() 엔드포인트가 컴파일된다. infrastructure 타입 미참조로 ArchUnit 자연 통과 구조.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuthController — login 엔드포인트 추가 (AUTH-10, SC#3)</name>
  <files>src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java</files>
  <read_first>
    - 수정 대상: src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java (Task 1 에서 생성한 파일)
    - 근거: 05-PATTERNS.md "login 래퍼 핵심 패턴" + 05-RESEARCH.md Pattern 3 + Pitfall 3(open-redirect), Pitfall 4(registration 부재)
    - open-redirect 방지 필수 (D-04, 보안 위협 HIGH): isSafeRelativePath() — startsWith("/") && !startsWith("//") && !contains("://")
    - ObjectProvider 가드 필수 (D-04): ClientRegistrationRepository 빈이 없으면 503 반환 (main 프로파일 부재 방어)
    - registrationId 하드코딩: "test-idp" (D-04 — 단일 registration 전제, A3 가정)
    - ArchUnit: ClientRegistrationRepository 는 외부 라이브러리 타입이라 ..infrastructure.. 패턴 미매칭 → ObjectProvider 방식으로 주입
  </read_first>
  <behavior>
    - GET /api/auth/login: @GetMapping("/login"), @RequestParam(required=false) String returnTo, HttpSession session, HttpServletResponse response, throws IOException
    - isSafeRelativePath(returnTo) 가 true 이면 session.setAttribute("RETURN_TO", returnTo)
    - clientRegistrationRepository.getIfAvailable() == null 이면 response.sendError(503) 후 return
    - 그 외에는 response.sendRedirect("/oauth2/authorization/test-idp")
    - isSafeRelativePath(): returnTo != null && startsWith("/") && !startsWith("//") && !contains("://")
    - 생성자: @Autowired 또는 생성자 주입으로 ObjectProvider<ClientRegistrationRepository> clientRegistrationRepository 를 주입
    - returnTo 가 null 이거나 isSafeRelativePath 실패 시 세션 attribute 미설정 (그대로 "/" 로 복귀)
  </behavior>
  <action>
    AuthController.java 에 login() 메서드를 추가한다.

    생성자 주입 필드 추가: private final ObjectProvider<ClientRegistrationRepository> clientRegistrationRepository;
    생성자: public AuthController(ObjectProvider<ClientRegistrationRepository> clientRegistrationRepository)

    login() 메서드: @GetMapping("/login"), public void login(@RequestParam(required = false) String returnTo, HttpSession session, HttpServletResponse response) throws IOException.

    구현 순서:
    1. isSafeRelativePath(returnTo) true 이면 session.setAttribute("RETURN_TO", returnTo)
    2. clientRegistrationRepository.getIfAvailable() == null 이면 response.sendError(HttpServletResponse.SC_SERVICE_UNAVAILABLE, "OIDC registration not configured") 후 return
    3. response.sendRedirect("/oauth2/authorization/test-idp")

    isSafeRelativePath() private static 메서드: url != null && url.startsWith("/") && !url.startsWith("//") && !url.contains("://")

    추가 임포트:
      import java.io.IOException;
      import jakarta.servlet.http.HttpServletResponse;
      import jakarta.servlet.http.HttpSession;
      import org.springframework.beans.factory.ObjectProvider;
      import org.springframework.security.oauth2.client.registration.ClientRegistrationRepository;
      import org.springframework.web.bind.annotation.RequestParam;
  </action>
  <verify>
    <automated>./gradlew test --tests "*ArchitectureTest*" -x javadoc 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 컴파일 성공: ./gradlew compileJava exits 0
    - ArchUnit GREEN: ./gradlew test --tests "*ArchitectureTest*" exits 0 (D-06 — interfaces→infrastructure 위반 없음)
    - isSafeRelativePath 존재: grep -c "isSafeRelativePath" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 2 (선언 + 호출)
    - ObjectProvider 주입: grep -c "ObjectProvider" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 1
    - open-redirect 방지 로직: grep -c "startsWith.*//\|contains.*://" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 1
    - BaselineOidcUser 미참조: grep -c "BaselineOidcUser" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java == 0
  </acceptance_criteria>
  <done>AuthController.java 가 session·login 두 엔드포인트를 모두 포함하며 컴파일되고, ArchUnit 게이트를 통과한다.</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| 클라이언트→AuthController.login() | returnTo 쿼리 파라미터는 신뢰 불가 — 상대경로 검증 후 세션에 저장 |
| AuthController.session()→OidcUser | principal 은 인증 성공 시에만 non-null — nullable 분기 필수 |

## STRIDE 위협 레지스터

| 위협 ID | 카테고리 | 컴포넌트 | 처리 방침 | 완화 계획 |
|---------|---------|---------|---------|---------|
| T-05-04 | Spoofing | GET /api/auth/login returnTo 파라미터 | mitigate | isSafeRelativePath() — startsWith("/") && !startsWith("//") && !contains("://") 검증. 무효 returnTo 는 무시하고 "/" 로 폴백. open-redirect 방지 필수 (HIGH) |
| T-05-05 | Information Disclosure | GET /api/auth/session 응답 | accept | authenticated·userId 만 반환. 토큰·세션ID·개인정보 미포함. userId 는 로컬 DB PK(민감도 낮음). SC#1 명세 설계 의도 |
| T-05-06 | Elevation of Privilege | AuthController.login() registration 부재 시 | accept | ObjectProvider 가드로 503 반환. 공격자가 이를 악용해 인증 우회 불가 — 오히려 안전 실패(fail-closed) |
| T-05-SC | Tampering | npm/pip/cargo 없음 — Java 신규 파일 | accept | 신규 외부 패키지 없음(NFR-01). spring-security-oauth2-client 기존 의존성만 참조 |
</threat_model>

<verification>
Wave 2 완료 후 다음이 모두 GREEN 이어야 Wave 3 으로 진행한다:

1. ./gradlew test --tests "*ArchitectureTest*" exits 0 (AuthController 가 ArchUnit 게이트 통과)
2. ./gradlew test --tests "*BffAuthIT*" exits 0 (Wave 1 회귀 없음)
3. grep -c "BaselineOidcUser" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java == 0
4. grep -c "isSafeRelativePath" src/main/java/com/anchors/baseline/auth/interfaces/AuthController.java >= 2
</verification>

<success_criteria>
- AuthController.java 신설 (auth/interfaces 패키지)
- GET /api/auth/session: 비인증 → 200 {authenticated:false}, 인증 → 200 {authenticated:true, userId}
- GET /api/auth/login: /oauth2/authorization/test-idp 302 리다이렉트 + returnTo 세션 저장 + open-redirect 방지
- BaselineOidcUser infrastructure 타입 미참조 (ArchUnit 자연 통과)
- ArchUnit GREEN (D-06)
- BffAuthIT GREEN (Wave 1 회귀 없음)
</success_criteria>

<output>
완료 시 .planning/phases/05-bff-auth-session/05-02-SUMMARY.md 를 생성하라.
</output>
