---
phase: 01-platform-skeleton
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-04
files_modified:
  - src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java
  - src/main/resources/application.yml
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06

must_haves:
  truths:
    - "애플리케이션이 기동하고 가상 스레드 활성화 로그가 확인된다 (SC#1)"
    - "platform/{domain,application,infrastructure,interfaces} 패키지 구조가 존재하고 ArchUnit 의존 방향 위반이 없다 (SC#2)"
    - "Flyway 마이그레이션이 실행되고 flyway_schema_history에 success=true 레코드가 남는다 (SC#3)"
    - "JPA 쓰기와 MyBatis 조회가 동일 DataSource·단일 트랜잭션에서 동작한다 (SC#4)"
    - "/actuator/health가 app·DB(PostgreSQL)·Redis 상태를 모두 UP으로 반환한다 (SC#5)"
    - "`./gradlew test` 전체 스위트가 GREEN으로 완료된다"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java"
      provides: "D-04 JPA+MyBatis 단일 트랜잭션 검증 — @Disabled 제거, em.flush() 포함"
      contains: "em.flush"
    - path: "src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java"
      provides: "PLAT-06 /actuator/health db+redis UP 검증"
      contains: "actuator/health"
    - path: "src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java"
      provides: "PLAT-01 isVirtual() assert"
      contains: "isVirtual"
    - path: "src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java"
      provides: "PLAT-03 flyway_schema_history 쿼리 검증"
      contains: "flyway_schema_history"
  key_links:
    - from: "PersistenceIntegrationTest"
      to: "SampleEntity + SampleMapper (Plan 04)"
      via: "EntityManager.persist() + em.flush() + SampleMapper.findAll()"
      pattern: "em\\.flush\\(\\)"
    - from: "ActuatorHealthTest"
      to: "/actuator/health 엔드포인트"
      via: "TestRestTemplate GET"
      pattern: "actuator/health"
---

<objective>
Plan 02에서 @Disabled 상태로 생성된 통합 테스트 파일을 Plan 04의 구현체(SampleEntity, SampleMapper)를 대상으로 활성화하고 완성한다. 5개 성공 기준(SC#1~5)이 모두 자동화된 테스트로 검증된다.

Purpose: Phase 1의 최종 검증 게이트. 전체 테스트 스위트가 GREEN이면 Phase 1이 완료된다.
Output: 활성화된 통합 테스트 4종 + `./gradlew test` GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-skeleton/01-VALIDATION.md
@.planning/phases/01-platform-skeleton/01-RESEARCH.md
@.planning/phases/01-platform-skeleton/01-CONTEXT.md
</context>

<interfaces>
<!-- Plan 04에서 생성된 타입 — 이 플랜의 테스트가 참조한다 -->

SampleEntity (com.anchors.baseline.platform.domain.model):
  - 생성자: SampleEntity(String value)
  - getId(): Long
  - getValue(): String

SampleMapper (com.anchors.baseline.platform.infrastructure.mybatis):
  - @Mapper 인터페이스
  - findAll(): List&lt;SampleDto&gt;

SampleDto (com.anchors.baseline.platform.infrastructure.mybatis):
  - getValue(): String

AbstractIntegrationTest (com.anchors.baseline):
  - @Testcontainers + @SpringBootTest(RANDOM_PORT) + @ActiveProfiles("test")
  - static PostgreSQLContainer postgres (@ServiceConnection)
  - static GenericContainer redis (@ServiceConnection(name="redis"))
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>태스크 1: PersistenceIntegrationTest + FlywayMigrationTest 완성</name>
  <files>
    src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java
  </files>
  <read_first>
    - src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java (Plan 02에서 생성된 현재 내용 전체)
    - src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java (Plan 02에서 생성된 현재 내용 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 2: JPA+MyBatis 단일 트랜잭션 — em.flush() 필수 이유)
    - src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java (Plan 04 생성, 실제 생성자/필드 확인)
    - src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleMapper.java (Plan 04 생성, 실제 메서드 시그니처 확인)
    - src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleDto.java (Plan 04 생성, getValue() 확인)
  </read_first>
  <action>
    1. `PersistenceIntegrationTest.java` 수정:
       - @Disabled 애노테이션 제거 (Plan 02에서 임시 추가된 것)
       - import 추가: SampleEntity, SampleMapper, SampleDto (Plan 04 패키지 경로로)
       - @Autowired SampleMapper sampleMapper 필드 주입 활성화
       - jpaWriteAndMyBatisReadInSingleTransaction() 테스트 활성화:
         SampleEntity entity = new SampleEntity("test-value-" + System.currentTimeMillis())  // 유일한 값
         em.persist(entity)
         em.flush()  // ← 필수: 이 줄이 없으면 MyBatis가 빈 결과를 반환한다
         List&lt;SampleDto&gt; results = sampleMapper.findAll()
         assertThat(results).extracting(SampleDto::getValue).anyMatch(v -> v.startsWith("test-value-"))
       - 추가 테스트: redisConnectionAlive() — @Autowired StringRedisTemplate redisTemplate 으로
         redisTemplate.opsForValue().set("test-key", "test-value")
         assertThat(redisTemplate.opsForValue().get("test-key")).isEqualTo("test-value")
         이 테스트는 PLAT-05 Redis 연결 확인을 담당한다

    2. `FlywayMigrationTest.java` 수정:
       - @Disabled 제거 (있는 경우)
       - flywaySchemaHistoryExists() 완성:
         SELECT count(*) FROM flyway_schema_history WHERE success = true
         count가 1 이상임을 assert
       - flywayPlatformSampleTableExists() 추가:
         SELECT count(*) FROM information_schema.tables WHERE table_name = 'platform_sample'
         count가 1임을 assert

    주의: @Transactional이 붙은 테스트 메서드는 기본적으로 롤백되므로 다른 테스트에 영향을 주지 않는다.
    em.persist() + em.flush() 패턴은 @Transactional 메서드 안에서만 유효하다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 &amp;&amp; ./gradlew test --tests "*PersistenceIntegrationTest*" --tests "*FlywayMigrationTest*" 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PersistenceIntegrationTest에 "@Disabled" 가 없다
    - PersistenceIntegrationTest에 "em.flush()" 가 포함되어 있다
    - `./gradlew test --tests "*PersistenceIntegrationTest*"` 가 BUILD SUCCESSFUL로 종료된다 (테스트 GREEN)
    - `./gradlew test --tests "*FlywayMigrationTest*"` 가 BUILD SUCCESSFUL로 종료된다
    - FlywayMigrationTest에 "flyway_schema_history" SQL이 포함되어 있다
  </acceptance_criteria>
  <done>D-04 JPA+MyBatis 단일 트랜잭션 검증 테스트와 Flyway 마이그레이션 검증 테스트가 GREEN 상태로 실행된다</done>
</task>

<task type="auto" tdd="false">
  <name>태스크 2: VirtualThreadTest + ActuatorHealthTest 완성 및 전체 테스트 스위트 GREEN</name>
  <files>
    src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java,
    src/main/resources/application.yml
  </files>
  <read_first>
    - src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java (Plan 02에서 생성된 현재 내용)
    - src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java (Plan 02에서 생성된 현재 내용)
    - src/main/resources/application.yml (Plan 03에서 생성된 현재 내용 — spring.threads.virtual.enabled 확인)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 5: Actuator 헬스체크 — 응답 구조 JSON)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 1: 가상 스레드 확인 방법)
  </read_first>
  <action>
    1. `VirtualThreadTest.java` 완성:
       - @Disabled 제거
       - virtualThreadEnabled() 테스트: Thread.ofVirtual().start()로 생성한 스레드에서 Thread.currentThread().isVirtual() == true 검증
       - springVirtualThreadsEnabled() 테스트: @Value("${spring.threads.virtual.enabled:false}") boolean virtualEnabled 주입하여 assertTrue(virtualEnabled)
       - application.yml의 spring.threads.virtual.enabled=true 설정이 테스트 환경(application-test.yml)에도 적용되는지 확인 — application-test.yml이 이 속성을 오버라이드하지 않아야 한다

    2. `ActuatorHealthTest.java` 완성:
       - @Disabled 제거
       - @Autowired TestRestTemplate restTemplate (또는 @Autowired WebTestClient)
       - actuatorHealthReturnsAllUp() 테스트:
         GET /actuator/health 호출
         HTTP 200 assert
         body에 "\"status\":\"UP\"" 포함 assert
         body에 "\"db\"" 포함 assert (PostgreSQL 헬스 인디케이터)
         body에 "\"redis\"" 포함 assert (Redis 헬스 인디케이터)
       - ActuatorHealthTest는 @SpringBootTest(WebEnvironment.RANDOM_PORT) 환경에서 실행되므로 TestRestTemplate이 기본으로 제공된다
       - application-test.yml에 management.endpoints.web.exposure.include=health,info 가 설정되어 있어야 한다 (Plan 03에서 설정됨)

    3. application.yml 확인 및 필요 시 보정:
       - spring.threads.virtual.enabled: true 가 있는지 확인
       - 없으면 추가
       - mybatis.configuration.map-underscore-to-camel-case: true 추가 (SampleDto의 created_at → createdAt 매핑)

    4. 전체 테스트 스위트 실행:
       `./gradlew test` 실행 후 ArchitectureTest, PersistenceIntegrationTest, ActuatorHealthTest, VirtualThreadTest, FlywayMigrationTest 모두 GREEN 확인
       실패하는 테스트가 있으면 원인을 분석하여 수정한다
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 &amp;&amp; ./gradlew test 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - VirtualThreadTest에 "isVirtual()" 이 포함되어 있고 "@Disabled" 가 없다
    - ActuatorHealthTest에 "/actuator/health" 가 포함되어 있고 "@Disabled" 가 없다
    - `./gradlew test --tests "*ArchitectureTest*"` 가 BUILD SUCCESSFUL로 종료된다 (의존 방향 위반 없음)
    - `./gradlew test --tests "*ActuatorHealthTest*"` 가 BUILD SUCCESSFUL로 종료된다
    - `./gradlew test --tests "*VirtualThreadTest*"` 가 BUILD SUCCESSFUL로 종료된다
    - `./gradlew test` 전체 스위트가 BUILD SUCCESSFUL로 종료된다 (테스트 실패 0건)
    - application.yml에 "map-underscore-to-camel-case: true" 가 포함되어 있다 (MyBatis 결과 매핑)
  </acceptance_criteria>
  <done>Phase 1의 모든 성공 기준(SC#1~5)이 자동화된 테스트로 검증되고 `./gradlew test` 전체 스위트가 GREEN이다</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| TestRestTemplate → /actuator/health | 테스트 클라이언트가 내부 헬스 엔드포인트에 접근 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-05-01 | Information Disclosure | /actuator/health show-details: always | accept (dev) | 테스트 환경 전용. application-test.yml에서 개발 전용으로 명시됨. 운영 배포 시 T-03-01과 동일한 완화 적용 필요. |
| T-05-SC | Tampering | Testcontainers 컨테이너 기동 | accept | postgres:16, redis:7 공식 이미지 사용. 테스트 환경 한정. |
</threat_model>

<verification>
1. `./gradlew test --tests "*ArchitectureTest*"` → BUILD SUCCESSFUL, 0 failures
2. `./gradlew test --tests "*PersistenceIntegrationTest*"` → BUILD SUCCESSFUL, 0 failures
3. `./gradlew test --tests "*ActuatorHealthTest*"` → BUILD SUCCESSFUL, 0 failures
4. `./gradlew test --tests "*VirtualThreadTest*"` → BUILD SUCCESSFUL, 0 failures
5. `./gradlew test --tests "*FlywayMigrationTest*"` → BUILD SUCCESSFUL, 0 failures
6. `./gradlew test` → BUILD SUCCESSFUL, 전체 스위트 GREEN

Phase 1 성공 기준 체크:
- SC#1: VirtualThreadTest — isVirtual() = true
- SC#2: ArchitectureTest — layeredArchitecture() 위반 없음
- SC#3: FlywayMigrationTest — flyway_schema_history count > 0
- SC#4: PersistenceIntegrationTest — em.flush() + MyBatis 단일 트랜잭션
- SC#5: ActuatorHealthTest — /actuator/health db+redis UP
</verification>

<success_criteria>
- `./gradlew test` 전체 스위트가 GREEN으로 완료된다
- PLAT-01 ~ PLAT-06 모든 요구사항이 자동화 테스트로 검증된다
- Phase 1의 5개 성공 기준(SC#1~5)이 모두 충족된다
- ArchUnit CI 게이트가 GREEN이어서 이후 Phase에서 계층 위반이 발생하면 자동으로 감지된다
</success_criteria>

<output>
완료 시 `.planning/phases/01-platform-skeleton/01-05-SUMMARY.md` 생성
</output>
