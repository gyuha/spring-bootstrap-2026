---
phase: 01-platform-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/test/java/com/anchors/baseline/AbstractIntegrationTest.java
  - src/test/java/com/anchors/baseline/architecture/ArchitectureTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java
  - src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java
  - src/test/resources/application-test.yml
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06

must_haves:
  truths:
    - "ArchitectureTest.java가 존재하고 layeredArchitecture() 규칙이 선언되어 있다 (초기에는 RED 가능)"
    - "PersistenceIntegrationTest.java에 em.flush() 가 포함된 JPA+MyBatis 단일 트랜잭션 테스트가 있다"
    - "ActuatorHealthTest.java가 /actuator/health 엔드포인트를 검증한다"
    - "AbstractIntegrationTest.java에 Testcontainers PostgreSQL + Redis @ServiceConnection(name='redis')가 공유 컨테이너로 선언되어 있다"
    - "application-test.yml이 존재한다"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/AbstractIntegrationTest.java"
      provides: "공유 Testcontainers base class — PostgreSQLContainer + GenericContainer(redis)"
      contains: "@ServiceConnection"
    - path: "src/test/java/com/anchors/baseline/architecture/ArchitectureTest.java"
      provides: "ArchUnit layeredArchitecture() CI 게이트"
      contains: "layeredArchitecture"
    - path: "src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java"
      provides: "D-04 JPA+MyBatis 단일 트랜잭션 검증"
      contains: "em.flush"
  key_links:
    - from: "PersistenceIntegrationTest.java"
      to: "AbstractIntegrationTest.java"
      via: "extends AbstractIntegrationTest"
      pattern: "extends AbstractIntegrationTest"
    - from: "ArchitectureTest.java"
      to: "com.anchors.baseline 패키지"
      via: "ClassFileImporter().importPackages"
      pattern: "com\\.anchors\\.baseline"
---

<objective>
VALIDATION.md의 Wave 0 요구사항을 모두 충족하는 테스트 파일 골격을 생성한다. 이 플랜은 01-01(build.gradle.kts — Testcontainers/ArchUnit classpath) 완료 후 실행된다(Wave 2). 테스트가 먼저 존재해야 Wave 3 구현 완료 후 즉시 검증이 가능하다.

Purpose: PLAT-01~06 각각에 대한 자동화된 검증 게이트를 선언한다. Wave 0 테스트는 초기 실행 시 RED(컴파일 실패 또는 실패)여도 되지만, 파일과 기본 구조는 존재해야 한다.
Output: 6개 테스트 파일 + AbstractIntegrationTest base class + application-test.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-skeleton/01-VALIDATION.md
@.planning/phases/01-platform-skeleton/01-RESEARCH.md
@.planning/phases/01-platform-skeleton/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>태스크 1: 공통 Testcontainers base class 및 테스트 설정 생성</name>
  <files>
    src/test/java/com/anchors/baseline/AbstractIntegrationTest.java,
    src/test/resources/application-test.yml
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 7: Testcontainers 통합 테스트 — GenericContainer + @ServiceConnection(name="redis") 패턴 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§함정 5: @ServiceConnection 없는 GenericContainer Redis)
    - .planning/phases/01-platform-skeleton/01-VALIDATION.md (§Wave 0 Requirements 목록)
  </read_first>
  <action>
    1. `src/test/java/com/anchors/baseline/AbstractIntegrationTest.java` 생성:
       - 패키지: com.anchors.baseline
       - 클래스 수준 애노테이션: @Testcontainers, @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT), @ActiveProfiles("test")
       - PostgreSQL 공유 컨테이너: @Container @ServiceConnection static PostgreSQLContainer&lt;?&gt; postgres = new PostgreSQLContainer&lt;&gt;("postgres:16")
       - Redis 공유 컨테이너: @Container @ServiceConnection(name = "redis") static GenericContainer&lt;?&gt; redis = new GenericContainer&lt;&gt;("redis:7").withExposedPorts(6379)
       - 두 컨테이너 모두 static 필드로 선언 (테스트 클래스 간 컨테이너 재사용을 위해)
       - import: org.springframework.boot.testcontainers.service.connection.ServiceConnection, org.testcontainers.containers.GenericContainer, org.testcontainers.containers.PostgreSQLContainer, org.testcontainers.junit.jupiter.Container, org.testcontainers.junit.jupiter.Testcontainers
       - 주의: GenericContainer에는 반드시 @ServiceConnection(name = "redis") — name 속성 누락 시 Spring Boot가 Redis 연결 정보를 주입하지 못한다 (RESEARCH 함정 5)

    2. `src/test/resources/application-test.yml` 생성:
       - spring.jpa.hibernate.ddl-auto: validate (Flyway가 스키마를 관리하므로 create/update 금지)
       - spring.flyway.enabled: true
       - logging.level.org.flywaydb: DEBUG (마이그레이션 실행 로그 확인용)
       - management.endpoints.web.exposure.include: health,info
       - management.endpoint.health.show-details: always
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AbstractIntegrationTest.java 파일이 src/test/java/com/anchors/baseline/에 존재한다
    - 파일 내에 "@ServiceConnection(name = \"redis\")" 문자열이 포함되어 있다
    - 파일 내에 "PostgreSQLContainer" 문자열이 포함되어 있다
    - application-test.yml 파일이 src/test/resources/에 존재한다
    - application-test.yml에 "validate" 문자열이 포함되어 있다 (ddl-auto)
    - ./gradlew compileTestJava 가 BUILD SUCCESSFUL (컴파일 오류 없음)
  </acceptance_criteria>
  <done>AbstractIntegrationTest.java와 application-test.yml이 올바른 위치에 생성되고 컴파일이 통과된다</done>
</task>

<task type="auto" tdd="false">
  <name>태스크 2: ArchUnit 아키텍처 테스트 + 통합 테스트 스텁 생성</name>
  <files>
    src/test/java/com/anchors/baseline/architecture/ArchitectureTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java,
    src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 6: ArchUnit 의존 방향 강제 — layeredArchitecture() 코드 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 7: Testcontainers 통합 테스트 — jpaWriteAndMyBatisReadInSingleTransaction 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§함정 4: ArchUnit layeredArchitecture + 순환 의존)
    - .planning/phases/01-platform-skeleton/01-VALIDATION.md (§Per-Task Verification Map — 각 PLAT-XX별 자동화 명령)
  </read_first>
  <action>
    1. `src/test/java/com/anchors/baseline/architecture/ArchitectureTest.java` 생성 (PLAT-02, D-05):
       - 패키지: com.anchors.baseline.architecture
       - ROOT 상수: "com.anchors.baseline"
       - ClassFileImporter().importPackages(ROOT) 로 classes 로드
       - hexagonalLayerDependencies() 테스트 메서드:
         layeredArchitecture().consideringAllDependencies()
         .layer("Domain").definedBy(ROOT + "..domain..")
         .layer("Application").definedBy(ROOT + "..application..")
         .layer("Infrastructure").definedBy(ROOT + "..infrastructure..")
         .layer("Interfaces").definedBy(ROOT + "..interfaces..")
         .whereLayer("Domain").mayNotAccessAnyLayer()
         .whereLayer("Application").mayOnlyAccessLayers("Domain")
         .whereLayer("Interfaces").mayOnlyAccessLayers("Application", "Domain")
         .whereLayer("Infrastructure").mayOnlyAccessLayers("Domain", "Application")
         .check(classes)
       - 주의: 패키지 패턴은 ".." (두 점) 방식 사용 — ROOT + "..domain.." 패턴이 com.anchors.baseline.*.domain.* 전체를 포괄한다 (RESEARCH 안티패턴 참고)

    2. `src/test/java/com/anchors/baseline/platform/infrastructure/PersistenceIntegrationTest.java` 생성 (PLAT-04, PLAT-05, D-04):
       - extends AbstractIntegrationTest
       - @Autowired EntityManager em
       - @Autowired SampleMapper sampleMapper (Wave 2에서 생성될 예정 — 현재는 컴파일 실패 허용, @Disabled 애노테이션으로 임시 비활성화)
       - @Test @Transactional jpaWriteAndMyBatisReadInSingleTransaction() 메서드:
         SampleEntity entity = new SampleEntity("test-value")
         em.persist(entity)
         em.flush()  // ← 필수 (RESEARCH 패턴 2, 함정 2)
         List&lt;SampleDto&gt; results = sampleMapper.findAll()
         assertThat(results).extracting(SampleDto::getValue).contains("test-value")
       - @Disabled("Wave 2에서 SampleEntity/SampleMapper 생성 후 활성화") 를 클래스 또는 메서드에 추가해 Wave 1에서 컴파일 오류 없이 유지
       - import: jakarta.persistence.EntityManager, org.springframework.transaction.annotation.Transactional

    3. `src/test/java/com/anchors/baseline/platform/infrastructure/ActuatorHealthTest.java` 생성 (PLAT-06):
       - extends AbstractIntegrationTest
       - @Autowired TestRestTemplate restTemplate (또는 @LocalServerPort + RestTemplate)
       - actuatorHealthReturnsUp() 테스트:
         ResponseEntity&lt;String&gt; response = restTemplate.getForEntity("/actuator/health", String.class)
         assertThat(response.getStatusCode().value()).isEqualTo(200)
         assertThat(response.getBody()).contains("\"status\":\"UP\"")
         assertThat(response.getBody()).contains("\"db\"")
         assertThat(response.getBody()).contains("\"redis\"")

    4. `src/test/java/com/anchors/baseline/platform/infrastructure/VirtualThreadTest.java` 생성 (PLAT-01):
       - extends AbstractIntegrationTest
       - @Autowired Environment env (또는 @Value로 spring.threads.virtual.enabled 확인)
       - virtualThreadEnabled() 테스트:
         boolean[] result = new boolean[1]
         Thread vt = Thread.ofVirtual().start(() -> result[0] = Thread.currentThread().isVirtual())
         vt.join()
         assertThat(result[0]).isTrue()
       - virtualThreadPropertyEnabled() 테스트:
         assertThat(env.getProperty("spring.threads.virtual.enabled")).isEqualTo("true")

    5. `src/test/java/com/anchors/baseline/platform/infrastructure/FlywayMigrationTest.java` 생성 (PLAT-03):
       - extends AbstractIntegrationTest
       - @Autowired DataSource dataSource
       - flywaySchemaHistoryExists() 테스트:
         try (Connection conn = dataSource.getConnection(); Statement stmt = conn.createStatement()) {
           ResultSet rs = stmt.executeQuery("SELECT count(*) FROM flyway_schema_history WHERE success = true")
           rs.next()
           assertThat(rs.getInt(1)).isGreaterThan(0)
         }
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ArchitectureTest.java 파일이 src/test/java/com/anchors/baseline/architecture/ 에 존재한다
    - ArchitectureTest.java에 "layeredArchitecture" 문자열이 포함되어 있다
    - ArchitectureTest.java에 "mayNotAccessAnyLayer" 문자열이 포함되어 있다
    - PersistenceIntegrationTest.java에 "em.flush()" 문자열이 포함되어 있다
    - PersistenceIntegrationTest.java에 "extends AbstractIntegrationTest" 가 포함되어 있다
    - ActuatorHealthTest.java에 "/actuator/health" 문자열이 포함되어 있다
    - VirtualThreadTest.java에 "isVirtual()" 문자열이 포함되어 있다
    - FlywayMigrationTest.java에 "flyway_schema_history" 문자열이 포함되어 있다
    - 5개 테스트 파일 모두 존재한다
    - ./gradlew compileTestJava 가 BUILD SUCCESSFUL (컴파일 오류 없음)
  </acceptance_criteria>
  <done>모든 Wave 0 테스트 파일이 올바른 패키지 위치에 생성되고, ArchitectureTest는 즉시 실행 가능한 상태이며 컴파일이 통과된다</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| Testcontainers → Docker daemon | 테스트 실행 시 컨테이너 이미지 다운로드 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-02-01 | Information Disclosure | application-test.yml | accept | 테스트 전용 설정, 운영 자격증명 포함 안 함. |
| T-02-SC | Tampering | Testcontainers 이미지 다운로드 | accept | postgres:16, redis:7은 공식 Docker Hub 이미지. 테스트 환경 한정, 운영 비노출. |
</threat_model>

<verification>
1. `find src/test -name "*.java" | sort` — 6개 테스트 파일 + AbstractIntegrationTest 확인
2. `grep -l "layeredArchitecture" src/test/**/*.java` — ArchitectureTest 확인
3. `grep -l "em.flush" src/test/**/*.java` — PersistenceIntegrationTest 확인
4. `grep -l "ServiceConnection" src/test/**/*.java` — AbstractIntegrationTest 확인
5. `./gradlew compileTestJava` — BUILD SUCCESSFUL 확인
</verification>

<success_criteria>
- VALIDATION.md의 Wave 0 Requirements 체크리스트가 모두 충족된다
- AbstractIntegrationTest.java에 PostgreSQL + Redis 공유 컨테이너가 @ServiceConnection과 함께 선언된다
- ArchitectureTest.java가 layeredArchitecture() 규칙을 포함하고 Wave 2 이후 즉시 CI 게이트로 동작한다
- PersistenceIntegrationTest.java에 em.flush() 가 포함된 D-04 검증 메서드가 존재한다
- 모든 테스트 파일이 컴파일 오류 없이 빌드된다
</success_criteria>

<output>
완료 시 `.planning/phases/01-platform-skeleton/01-02-SUMMARY.md` 생성
</output>
