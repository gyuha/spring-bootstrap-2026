---
phase: 01-platform-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/resources/application.yml
  - src/main/resources/db/migration/V1__init_schema.sql
  - compose.yaml
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
  - PLAT-05
  - PLAT-06

must_haves:
  truths:
    - "application.yml에 spring.threads.virtual.enabled=true가 선언되어 있다"
    - "application.yml에 flyway 설정이 있고 flyway-core + flyway-database-postgresql이 로드된다"
    - "application.yml에 Redis host/port가 선언되어 있다"
    - "application.yml에 /actuator/health show-details=always (개발 환경)가 설정되어 있다"
    - "V1__init_schema.sql Flyway 마이그레이션 파일이 존재하고 platform_sample 테이블을 생성한다"
    - "compose.yaml에 postgres:16 + redis:7 서비스가 로컬 개발용으로 선언되어 있다"
  artifacts:
    - path: "src/main/resources/application.yml"
      provides: "모든 인프라 설정 — 가상 스레드, JPA, Flyway, Redis, Actuator"
      contains: "spring.threads.virtual.enabled: true"
    - path: "src/main/resources/db/migration/V1__init_schema.sql"
      provides: "Flyway 초기 마이그레이션 — platform_sample 테이블 DDL"
      contains: "platform_sample"
    - path: "compose.yaml"
      provides: "로컬 개발용 PostgreSQL + Redis Docker Compose 정의"
      contains: "postgres:16"
  key_links:
    - from: "application.yml spring.flyway.locations"
      to: "src/main/resources/db/migration/"
      via: "classpath:db/migration"
      pattern: "classpath:db/migration"
    - from: "application.yml spring.data.redis"
      to: "compose.yaml redis 서비스"
      via: "localhost:6379"
      pattern: "6379"
---

<objective>
애플리케이션 설정 파일, Flyway 초기 마이그레이션, Docker Compose 로컬 개발 환경을 구성한다. 가상 스레드 활성화, Flyway 위치, Redis 연결, Actuator 헬스체크 노출 범위를 올바르게 선언한다.

Purpose: 애플리케이션이 기동할 수 있는 최소 설정을 확보한다. Flyway 마이그레이션이 초기 스키마를 생성해 이후 JPA 엔티티 매핑이 가능해진다.
Output: application.yml, V1__init_schema.sql, compose.yaml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-platform-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>태스크 1: application.yml 생성</name>
  <files>
    src/main/resources/application.yml
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 1: 가상 스레드 활성화 — application.yml 설정)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 3: Flyway 설정 — application.yml 내용)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 4: Redis 연결 설정)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 5: Actuator 헬스체크 — application.yml 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§보안 도메인 — T-A2: show-details: always는 개발 환경 전용)
  </read_first>
  <action>
    `src/main/resources/application.yml` 생성. 다음 설정 블록을 포함한다:

    spring.application.name: baseline

    spring.threads.virtual.enabled: true  (PLAT-01, RESEARCH 패턴 1)

    spring.datasource:
      url: jdbc:postgresql://localhost:5432/baseline
      username: baseline
      password: baseline
      driver-class-name: org.postgresql.Driver

    spring.jpa:
      hibernate.ddl-auto: validate  (Flyway가 스키마 관리 — create/update 금지)
      show-sql: false
      properties.hibernate.format_sql: false
      open-in-view: false  (OSIV 비활성화 — 계층 경계 준수)

    spring.flyway:
      locations: classpath:db/migration
      baseline-on-migrate: false  (greenfield 프로젝트)
      validate-on-migrate: true
      (flyway-database-postgresql이 classpath에 있으므로 별도 url 설정 불필요)

    spring.data.redis:
      host: localhost
      port: 6379
      (Phase 1: 비밀번호 없음, Lettuce 기본 클라이언트)

    management:
      endpoints.web.exposure.include: health,info
      endpoint.health:
        show-details: always      # 개발 환경 전용 — 운영 배포 시 when_authorized로 변경 필요 (T-01 보안 주의)
        show-components: always
      health:
        db.enabled: true
        redis.enabled: true
        diskspace.enabled: false

    logging.level:
      com.anchors.baseline: DEBUG
      org.flywaydb: INFO
  </action>
  <verify>
    <automated>grep -c "spring.threads.virtual.enabled" /Users/gyuha/workspace/spring-bootstrap-2026/src/main/resources/application.yml</automated>
  </verify>
  <acceptance_criteria>
    - 파일이 src/main/resources/application.yml 경로에 존재한다
    - "spring.threads.virtual.enabled: true" 또는 해당 YAML 계층 구조가 포함되어 있다
    - "classpath:db/migration" 이 포함되어 있다
    - "actuator" 관련 설정에 "health" 가 포함되어 있다
    - "redis" 연결 설정이 포함되어 있다
    - "open-in-view: false" 가 포함되어 있다 (OSIV 비활성화)
    - "show-details: always" 와 함께 개발 환경 전용임을 나타내는 주석이 존재한다
  </acceptance_criteria>
  <done>application.yml이 완성되어 가상 스레드, Flyway, Redis, Actuator 헬스체크 설정이 모두 포함된다</done>
</task>

<task type="auto" tdd="false">
  <name>태스크 2: Flyway 초기 마이그레이션 + Docker Compose 생성</name>
  <files>
    src/main/resources/db/migration/V1__init_schema.sql,
    compose.yaml
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 3: Flyway 설정 — 마이그레이션 파일 네이밍 규약)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 9: Docker Compose — 로컬 개발)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§보안 도메인 — compose.yaml 패스워드 로컬 개발 전용 주석)
    - .planning/phases/01-platform-skeleton/01-CONTEXT.md (D-06: Docker Compose = PostgreSQL + Redis)
  </read_first>
  <action>
    1. `src/main/resources/db/migration/V1__init_schema.sql` 생성:
       - 파일명 규약: V{버전}__{설명}.sql (언더스코어 두 개 필수)
       - platform_sample 테이블 DDL:
         CREATE TABLE IF NOT EXISTS platform_sample (
           id BIGSERIAL PRIMARY KEY,
           value VARCHAR(255) NOT NULL,
           created_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL
         );
       - 이 테이블은 D-04 JPA+MyBatis 단일 트랜잭션 검증(Plan 04의 SampleEntity)용이다
       - COMMENT ON TABLE platform_sample IS 'Phase 1 JPA+MyBatis 통합 검증용 샘플 테이블 — 업무 도메인과 무관';

    2. `compose.yaml` 생성 (RESEARCH 패턴 9 기반, D-06 per):
       - services.postgres:
         image: postgres:16
         environment: POSTGRES_DB=baseline, POSTGRES_USER=baseline, POSTGRES_PASSWORD=baseline
         # 로컬 개발 전용 자격증명 — 운영 환경에 사용 금지
         ports: "5432:5432"
         healthcheck: pg_isready -U baseline -d baseline (interval:10s, timeout:5s, retries:5, start_period:30s)
       - services.redis:
         image: redis:7
         ports: "6379:6379"
         healthcheck: redis-cli ping (interval:10s, timeout:5s, retries:3)
       - 상단에 파일 레벨 주석: "# 로컬 개발 전용 Docker Compose — 운영 환경 사용 금지"
  </action>
  <verify>
    <automated>find /Users/gyuha/workspace/spring-bootstrap-2026/src/main/resources/db/migration -name "V1__*.sql" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - V1__init_schema.sql 파일이 src/main/resources/db/migration/에 존재한다
    - V1__init_schema.sql에 "platform_sample" 테이블 DDL이 포함되어 있다
    - V1__init_schema.sql에 "BIGSERIAL PRIMARY KEY" 가 포함되어 있다
    - compose.yaml에 "postgres:16" 이 포함되어 있다
    - compose.yaml에 "redis:7" 이 포함되어 있다
    - compose.yaml에 "로컬 개발 전용" 또는 동일 의미의 주석이 포함되어 있다
    - compose.yaml에 healthcheck 설정이 두 서비스 모두에 포함되어 있다
  </acceptance_criteria>
  <done>Flyway 마이그레이션 파일과 Docker Compose 설정이 완성되어 로컬 개발 환경과 Testcontainers 통합 테스트 모두에서 사용 가능하다</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| compose.yaml → Docker Hub | 컨테이너 이미지 다운로드 |
| application.yml → 로컬 PostgreSQL | 자격증명 포함 설정 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-03-01 | Information Disclosure | management.endpoint.health.show-details: always | accept (dev) | 개발 환경 전용. 주석으로 명시. 운영 배포 시 when_authorized로 변경 필요 — 이 제약은 RESEARCH.md §보안 도메인 A2에 문서화됨. |
| T-03-02 | Information Disclosure | compose.yaml POSTGRES_PASSWORD=baseline | accept (local) | 로컬 개발 전용 자격증명. 파일 내 주석으로 명시. Git에 체크인되나 운영 시크릿 아님 — 운영 배포는 별도 시크릿 관리 레이어 필요. |
| T-03-SC | Tampering | Docker Hub 이미지 다운로드 | accept | postgres:16, redis:7은 공식 Docker Hub 이미지. |
</threat_model>

<verification>
1. `grep "virtual.enabled" src/main/resources/application.yml` → "true" 출력 확인
2. `grep "flyway-database-postgresql\|flyway.locations" src/main/resources/application.yml` → 관련 설정 확인
3. `find src/main/resources/db/migration -name "*.sql"` → V1__init_schema.sql 확인
4. `grep "platform_sample" src/main/resources/db/migration/V1__init_schema.sql` → 테이블명 확인
5. `grep "postgres:16\|redis:7" compose.yaml` → 두 서비스 이미지 확인
</verification>

<success_criteria>
- application.yml이 완성되어 가상 스레드(PLAT-01), Flyway, Redis, Actuator가 올바르게 설정된다
- V1__init_schema.sql이 platform_sample 테이블을 생성하는 DDL을 포함한다
- compose.yaml이 healthcheck를 포함한 PostgreSQL + Redis 로컬 개발 환경을 정의한다
- 보안 주의사항(Actuator show-details, 로컬 자격증명)이 주석으로 문서화된다
</success_criteria>

<output>
완료 시 `.planning/phases/01-platform-skeleton/01-03-SUMMARY.md` 생성
</output>
