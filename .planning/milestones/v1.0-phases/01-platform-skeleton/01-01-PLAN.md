---
phase: 01-platform-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle/libs.versions.toml
  - settings.gradle.kts
  - src/main/java/com/anchors/baseline/BaselineApplication.java
autonomous: true
requirements:
  - PLAT-01
  - PLAT-04

must_haves:
  truths:
    - "Gradle 빌드가 성공하고 `./gradlew dependencies`가 오류 없이 실행된다"
    - "flyway-database-postgresql 의존성이 flyway-core와 함께 선언되어 있다"
    - "mybatis-spring-boot-starter:3.0.4 (Spring Boot 4.0 전용인 4.0.0이 아님)가 선언되어 있다"
    - "Java 21 toolchain이 지정되어 있고 `./gradlew compileJava`가 성공한다"
    - "BaselineApplication.java가 루트 패키지 com.anchors.baseline에 위치한다"
  artifacts:
    - path: "build.gradle.kts"
      provides: "전체 의존성 선언 + Java 21 toolchain + Lombok annotationProcessor"
      contains: "flyway-database-postgresql"
    - path: "gradle/libs.versions.toml"
      provides: "버전 카탈로그 — spring-boot=3.5.3, mybatis-spring-boot=3.0.4, archunit=1.4.1, testcontainers=1.21.3"
    - path: "src/main/java/com/anchors/baseline/BaselineApplication.java"
      provides: "@SpringBootApplication 진입점"
  key_links:
    - from: "build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "alias(libs.xxx) 참조"
      pattern: "libs\\.mybatis\\.spring\\.boot\\.starter"
    - from: "build.gradle.kts"
      to: "flyway-database-postgresql"
      via: "implementation 선언"
      pattern: "flyway-database-postgresql"
---

<objective>
Gradle (Kotlin DSL) 단일 모듈 프로젝트 기반을 세운다. 버전 카탈로그(`libs.versions.toml`)에 모든 외부 의존성 버전을 고정하고, `build.gradle.kts`에서 Spring Boot BOM과 함께 선언한다. Flyway PostgreSQL 분리 모듈 누락 함정과 MyBatis 버전 오선택 함정을 의존성 선언 단계에서 미연에 방지한다.

Purpose: 이후 모든 Plan이 올바른 의존성 기반 위에서 작성된다. 빌드 실패가 없으면 Wave 2 구현 플랜이 안전하게 진행된다.
Output: build.gradle.kts, gradle/libs.versions.toml, settings.gradle.kts, BaselineApplication.java
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-platform-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>태스크 1: Gradle 버전 카탈로그 및 빌드 스크립트 생성</name>
  <files>
    gradle/libs.versions.toml,
    settings.gradle.kts,
    build.gradle.kts
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 8: Gradle Kotlin DSL + Version Catalog — 전체 TOML 및 build.gradle.kts 내용)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§표준 스택 — 버전 표 전체)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§함정 1, 3 — flyway-database-postgresql 누락 방지, mybatis 버전 오선택 방지)
  </read_first>
  <action>
    1. `gradle/libs.versions.toml` 생성. [versions] 섹션: spring-boot="3.5.3", mybatis-spring-boot="3.0.4", archunit="1.4.1", testcontainers="1.21.3", lombok="1.18.38". [libraries] 섹션: mybatis-spring-boot-starter (module="org.mybatis.spring.boot:mybatis-spring-boot-starter", version.ref="mybatis-spring-boot"), archunit-junit5 (module="com.tngtech.archunit:archunit-junit5", version.ref="archunit"), testcontainers-postgresql (module="org.testcontainers:postgresql", 버전은 BOM 관리), testcontainers-junit-jupiter (module="org.testcontainers:junit-jupiter", 버전은 BOM 관리). [plugins] 섹션: spring-boot (id="org.springframework.boot", version.ref="spring-boot"), spring-dependency-management (id="io.spring.dependency-management", version="1.1.7").

    2. `settings.gradle.kts` 생성. rootProject.name = "baseline". `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")` 포함.

    3. `build.gradle.kts` 생성. 핵심 내용:
       - plugins 블록: java, alias(libs.plugins.spring.boot), alias(libs.plugins.spring.dependency.management)
       - group = "com.anchors", version = "0.0.1-SNAPSHOT"
       - java.toolchain.languageVersion = JavaLanguageVersion.of(21)
       - configurations 블록: compileOnly가 annotationProcessor를 extendsFrom
       - dependencies에 반드시 포함할 항목 (per D-01, RESEARCH 패턴 8):
         * implementation("org.springframework.boot:spring-boot-starter-web")
         * implementation("org.springframework.boot:spring-boot-starter-data-jpa")
         * implementation("org.springframework.boot:spring-boot-starter-data-redis")
         * implementation("org.springframework.boot:spring-boot-starter-actuator")
         * implementation(libs.mybatis.spring.boot.starter)  ← 3.0.4, 4.0.0 아님
         * implementation("org.flywaydb:flyway-core")
         * implementation("org.flywaydb:flyway-database-postgresql")  ← 누락 시 기동 실패
         * compileOnly("org.projectlombok:lombok")
         * annotationProcessor("org.projectlombok:lombok")
         * runtimeOnly("org.postgresql:postgresql")
         * testImplementation("org.springframework.boot:spring-boot-starter-test")
         * testImplementation("org.springframework.boot:spring-boot-testcontainers")
         * testImplementation(libs.testcontainers.postgresql)
         * testImplementation(libs.testcontainers.junit.jupiter)
         * testImplementation(libs.archunit.junit5)
         * testCompileOnly("org.projectlombok:lombok")
         * testAnnotationProcessor("org.projectlombok:lombok")
       - tasks.withType&lt;Test&gt; { useJUnitPlatform() }

    주의: mybatis-spring-boot-starter 버전은 반드시 libs.versions.toml에서 참조하여 3.0.4가 고정되도록 한다. 직접 문자열로 버전을 선언하면 실수가 생긴다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 &amp;&amp; ./gradlew dependencies --configuration compileClasspath 2&gt;&amp;1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts에 "flyway-database-postgresql" 문자열이 포함되어 있다 (grep 확인)
    - gradle/libs.versions.toml에 mybatis-spring-boot = "3.0.4" 가 있다
    - gradle/libs.versions.toml에 spring-boot = "3.5.3" 가 있다
    - `./gradlew dependencies --configuration compileClasspath` 가 BUILD SUCCESSFUL로 종료된다
    - `./gradlew dependencies` 출력에 "mybatis-spring-boot-starter:3.0.4" 가 포함된다
    - "4.0.0" 문자열이 mybatis 관련 의존성에 나타나지 않는다
  </acceptance_criteria>
  <done>Gradle 빌드가 오류 없이 완료되고, 모든 의존성이 올바른 버전으로 해석된다</done>
</task>

<task type="auto" tdd="false">
  <name>태스크 2: BaselineApplication.java 생성</name>
  <files>
    src/main/java/com/anchors/baseline/BaselineApplication.java
  </files>
  <read_first>
    - .planning/phases/01-platform-skeleton/01-CONTEXT.md (D-02: 루트 패키지 = com.anchors.baseline)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 1: 가상 스레드 활성화 — CommandLineRunner verifyVirtualThreads 패턴)
    - spring-backend-ddd-baseline.md (§2 기술 스택, §3 A-1)
  </read_first>
  <action>
    `src/main/java/com/anchors/baseline/BaselineApplication.java` 생성.
    - 패키지: com.anchors.baseline (D-02 per)
    - 애노테이션: @SpringBootApplication
    - main 메서드: SpringApplication.run(BaselineApplication.class, args)
    - 가상 스레드 검증용 @Bean CommandLineRunner verifyVirtualThreads() 포함: Thread.ofVirtual().start() 내부에서 log.info("Virtual thread active: {}", Thread.currentThread().isVirtual()) 출력 후 join()
    - @Slf4j (Lombok) 사용으로 Logger 선언 생략

    파일 경로 주의: src/main/java/com/anchors/baseline/ 디렉터리가 없으면 생성 필요.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/spring-bootstrap-2026 &amp;&amp; ./gradlew compileJava 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 파일이 src/main/java/com/anchors/baseline/BaselineApplication.java 경로에 존재한다
    - 파일 첫 줄이 "package com.anchors.baseline;" 이다
    - @SpringBootApplication 애노테이션이 포함되어 있다
    - @Slf4j 와 CommandLineRunner가 포함되어 있다
    - `./gradlew compileJava` 가 BUILD SUCCESSFUL로 종료된다
  </acceptance_criteria>
  <done>BaselineApplication.java 컴파일이 성공하고 가상 스레드 검증 CommandLineRunner가 포함된다</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| build.gradle.kts → Maven Central | 의존성 다운로드 시 외부 레지스트리 접근 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-01-01 | Tampering | 의존성 다운로드 (Maven Central) | accept | 모든 패키지가 RESEARCH.md Package Legitimacy Audit에서 [VERIFIED: Maven Central]로 승인됨. Java 생태계 — slopcheck N/A. |
| T-01-SC | Tampering | npm/pip/cargo 설치 | accept | 이 플랜은 Java/Gradle 의존성만 사용. Package Legitimacy Audit 완료 (RESEARCH.md). |
</threat_model>

<verification>
1. `./gradlew dependencies --configuration compileClasspath` → BUILD SUCCESSFUL, flyway-database-postgresql + mybatis-spring-boot-starter:3.0.4 포함 확인
2. `./gradlew compileJava` → BUILD SUCCESSFUL
3. grep으로 build.gradle.kts에 "flyway-database-postgresql", "mybatis-spring-boot-starter" 확인
4. gradle/libs.versions.toml에 spring-boot = "3.5.3", mybatis-spring-boot = "3.0.4" 확인
</verification>

<success_criteria>
- Gradle 빌드가 오류 없이 완료된다
- flyway-database-postgresql 의존성이 flyway-core와 함께 선언되어 있다
- mybatis-spring-boot-starter 버전이 3.0.4이다 (4.0.0 아님)
- Java 21 toolchain이 설정되어 있다
- BaselineApplication.java가 com.anchors.baseline 패키지에 존재한다
</success_criteria>

<output>
완료 시 `.planning/phases/01-platform-skeleton/01-01-SUMMARY.md` 생성
</output>
