---
phase: 01-platform-skeleton
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java
  - src/main/java/com/anchors/baseline/platform/domain/repository/SampleRepository.java
  - src/main/java/com/anchors/baseline/platform/infrastructure/jpa/SampleJpaRepository.java
  - src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleMapper.java
  - src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleDto.java
  - src/main/java/com/anchors/baseline/platform/application/SampleApplicationService.java
  - src/main/java/com/anchors/baseline/platform/interfaces/rest/SampleController.java
  - src/main/java/com/anchors/baseline/common/domain/package-info.java
  - src/main/java/com/anchors/baseline/common/application/package-info.java
  - src/main/java/com/anchors/baseline/common/infrastructure/package-info.java
  - src/main/java/com/anchors/baseline/common/interfaces/package-info.java
autonomous: true
requirements:
  - PLAT-02
  - PLAT-04

must_haves:
  truths:
    - "platform 컨텍스트 하위에 domain/application/infrastructure/interfaces 계층이 모두 존재한다"
    - "common 컨텍스트 하위에 domain/application/infrastructure/interfaces 계층이 모두 존재한다"
    - "SampleEntity가 @Entity + @Table(name='platform_sample') 로 선언되어 있다"
    - "SampleMapper가 @Mapper 로 선언되고 findAll() 메서드가 있다"
    - "SampleApplicationService가 application 계층에 있고 포트 인터페이스(SampleRepository)만 의존한다"
    - "SampleController가 interfaces 계층에 있고 application만 의존한다"
    - "SampleRepository 포트 인터페이스가 domain.repository 패키지에 존재하고 SampleJpaRepository가 이를 구현한다"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java"
      provides: "JPA @Entity — platform_sample 테이블 매핑, D-04 검증용"
      contains: "@Table(name = \"platform_sample\")"
    - path: "src/main/java/com/anchors/baseline/platform/domain/repository/SampleRepository.java"
      provides: "도메인 포트 인터페이스 — application이 의존하는 리포지토리 계약 (baseline §4.2)"
      contains: "SampleRepository"
    - path: "src/main/java/com/anchors/baseline/platform/infrastructure/jpa/SampleJpaRepository.java"
      provides: "포트 구현체 — JpaRepository + SampleRepository 구현"
      contains: "implements SampleRepository"
    - path: "src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleMapper.java"
      provides: "MyBatis @Mapper — platform_sample 읽기 모델"
      contains: "@Mapper"
    - path: "src/main/java/com/anchors/baseline/platform/application/SampleApplicationService.java"
      provides: "application 계층 유스케이스 조율자 — @Transactional 경계 보유"
      contains: "@Transactional"
    - path: "src/main/java/com/anchors/baseline/platform/interfaces/rest/SampleController.java"
      provides: "interfaces 계층 REST 어댑터"
      contains: "@RestController"
  key_links:
    - from: "SampleApplicationService (application)"
      to: "SampleRepository (domain.repository)"
      via: "포트 인터페이스 의존성 주입 — infrastructure 직접 의존 없음"
      pattern: "SampleRepository"
    - from: "SampleJpaRepository (infrastructure)"
      to: "SampleRepository (domain.repository)"
      via: "implements SampleRepository"
      pattern: "implements SampleRepository"
    - from: "SampleController (interfaces)"
      to: "SampleApplicationService (application)"
      via: "의존성 주입"
      pattern: "SampleApplicationService"
    - from: "SampleJpaRepository (infrastructure)"
      to: "SampleEntity (domain)"
      via: "JpaRepository<SampleEntity, Long>"
      pattern: "SampleEntity"
---

<objective>
후속 컨텍스트(Identity, Authorization)의 형판(template)이 되는 platform 바운디드 컨텍스트의 전체 계층 골격을 생성한다. 헥사고날+DDD 패키지 구조(`<context>/{domain,application,infrastructure,interfaces}`)를 platform과 common 양쪽에 완성하고, D-04 검증용 SampleEntity + SampleMapper를 실제로 동작하는 상태로 구현한다.

Purpose: ArchUnit CI 게이트가 실제 코드를 대상으로 GREEN이 되려면 올바른 계층 구조가 존재해야 한다. SampleRepository 포트 인터페이스가 domain.repository에 위치함으로써 application이 infrastructure를 직접 의존하지 않아 layeredArchitecture() 규칙을 준수한다. SampleEntity/Mapper는 Plan 05의 PersistenceIntegrationTest가 @Disabled를 해제하고 실행할 수 있게 한다.
Output: platform/{domain,application,infrastructure,interfaces} 계층 파일, common 패키지 형판
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-platform-skeleton/01-RESEARCH.md
@spring-backend-ddd-baseline.md
</context>

<interfaces>
<!-- Plan 04가 생성하는 타입 계약 — Plan 05(통합 테스트)가 이 타입들을 사용한다 -->

SampleEntity (platform.domain.model):
  - @Entity @Table(name = "platform_sample")
  - 필드: id (Long, @GeneratedValue IDENTITY), value (String), createdAt (Instant)
  - 생성자: SampleEntity(String value) — ID는 DB가 생성
  - getter: getValue(), getId()

SampleRepository (platform.domain.repository) — 포트 인터페이스:
  - SampleEntity save(SampleEntity entity)
  - List&lt;SampleEntity&gt; findAll() (선택적 — 서비스 필요에 따라)

SampleJpaRepository (platform.infrastructure.jpa) — 포트 구현체:
  - interface SampleJpaRepository extends JpaRepository&lt;SampleEntity, Long&gt;, SampleRepository

SampleDto (platform.infrastructure.mybatis):
  - @Data @NoArgsConstructor @AllArgsConstructor 클래스 (Lombok)
  - 필드: Long id, String value, Instant createdAt
  - getter: getValue() — Lombok @Data가 생성
  - 주의: record 사용 불가 — MyBatis 결과 매핑이 no-args 생성자를 요구하며 Java record는 no-args 생성자가 없어 런타임 매핑 실패함

SampleMapper (platform.infrastructure.mybatis):
  - @Mapper 인터페이스
  - List&lt;SampleDto&gt; findAll() — SELECT id, value, created_at FROM platform_sample
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>태스크 1: platform 도메인 계층 + 인프라 계층 생성</name>
  <files>
    src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java,
    src/main/java/com/anchors/baseline/platform/domain/repository/SampleRepository.java,
    src/main/java/com/anchors/baseline/platform/infrastructure/jpa/SampleJpaRepository.java,
    src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleMapper.java,
    src/main/java/com/anchors/baseline/platform/infrastructure/mybatis/SampleDto.java
  </files>
  <read_first>
    - spring-backend-ddd-baseline.md (§4.2 패키지/계층 구조 — 리포지토리 인터페이스(포트)는 domain/repository, 구현은 infrastructure)
    - spring-backend-ddd-baseline.md (§4.4 JPA 방식 A — 엔티티=애그리거트)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§패턴 2: JPA+MyBatis 단일 트랜잭션 — SampleEntity 예시)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§안티패턴 — @MapperScan or @Mapper 필수)
    - .planning/phases/01-platform-skeleton/01-CONTEXT.md (D-03: platform/common 골격, D-04: JPA+MyBatis 검증용 엔티티, D-05: ArchUnit CI 게이트)
  </read_first>
  <action>
    1. `SampleEntity.java` (패키지: com.anchors.baseline.platform.domain.model):
       - @Entity, @Table(name = "platform_sample")
       - @Getter (Lombok)
       - id: @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id
       - value: @Column(nullable = false) private String value
       - createdAt: @Column(name = "created_at", nullable = false, updatable = false) private Instant createdAt = Instant.now()
       - protected 기본 생성자 (JPA 요구사항): protected SampleEntity() {}
       - 공개 생성자: public SampleEntity(String value) { this.value = value; }
       - 클래스 레벨 주석: "Phase 1 JPA+MyBatis 통합 검증용 샘플 엔티티 — 업무 도메인과 무관"

    2. `SampleRepository.java` (패키지: com.anchors.baseline.platform.domain.repository) — 포트 인터페이스:
       - baseline §4.2 규약: 리포지토리 인터페이스(포트)는 domain 계층에 위치, infrastructure가 구현
       - public interface SampleRepository:
         SampleEntity save(SampleEntity entity)
       - 이 인터페이스를 통해 application 계층은 domain만 의존하고 infrastructure를 직접 참조하지 않아 ArchUnit layeredArchitecture() 규칙 준수 (D-05 per)

    3. `SampleJpaRepository.java` (패키지: com.anchors.baseline.platform.infrastructure.jpa) — 포트 구현체:
       - public interface SampleJpaRepository extends JpaRepository&lt;SampleEntity, Long&gt;, SampleRepository
       - JpaRepository가 save(SampleEntity) 를 이미 구현하므로 추가 메서드 불필요
       - infrastructure 계층에 위치하여 domain 포트(SampleRepository)를 구현 — 의존성 역전 원칙 준수

    4. `SampleDto.java` (패키지: com.anchors.baseline.platform.infrastructure.mybatis):
       - @Data @NoArgsConstructor @AllArgsConstructor (Lombok) 클래스로 구현
       - 필드: private Long id; private String value; private Instant createdAt;
       - record 사용 금지: MyBatis 결과 매핑은 no-args 생성자를 요구하나 Java record는 no-args 생성자가 없어 런타임 매핑 실패함
       - createdAt 필드명은 MyBatis 결과 매핑에서 created_at → createdAt 변환 필요 (mapUnderscoreToCamelCase 설정 또는 @Results 사용)

    5. `SampleMapper.java` (패키지: com.anchors.baseline.platform.infrastructure.mybatis):
       - @Mapper 인터페이스 (com.anchors.baseline.platform.infrastructure.mybatis 패키지에 위치)
       - @Select("SELECT id, value, created_at as createdAt FROM platform_sample") List&lt;SampleDto&gt; findAll()
       - @MapperScan 없이 @Mapper 애노테이션만으로 auto-scan 가능 (mybatis-spring-boot-starter 자동설정)
  </action>
  <verify>
    <automated>./gradlew compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SampleEntity.java 에 "@Table(name = \"platform_sample\")" 이 포함되어 있다
    - SampleEntity.java 에 "@GeneratedValue" 이 포함되어 있다
    - SampleRepository.java 가 com.anchors.baseline.platform.domain.repository 패키지에 존재한다
    - SampleRepository.java 에 "interface SampleRepository" 가 포함되어 있다
    - SampleJpaRepository.java 에 "implements SampleRepository" 가 포함되어 있다
    - SampleJpaRepository.java 에 "JpaRepository" 이 포함되어 있다
    - SampleMapper.java 에 "@Mapper" 이 포함되어 있다
    - SampleMapper.java 에 "findAll" 메서드가 포함되어 있다
    - SampleDto.java 에 "@NoArgsConstructor" 이 포함되어 있다 (record 아님)
    - SampleDto.java 에 "value" 필드가 포함되어 있다
    - ./gradlew compileJava 가 BUILD SUCCESSFUL (컴파일 오류 없음)
  </acceptance_criteria>
  <done>platform 도메인 + 인프라 계층 파일이 생성되어 SampleRepository 포트 패턴이 적용되고 D-04 검증 테스트가 @Disabled 없이 컴파일 가능하다</done>
</task>

<task type="auto" tdd="false">
  <name>태스크 2: platform application/interfaces 계층 + common 패키지 형판 생성</name>
  <files>
    src/main/java/com/anchors/baseline/platform/application/SampleApplicationService.java,
    src/main/java/com/anchors/baseline/platform/interfaces/rest/SampleController.java,
    src/main/java/com/anchors/baseline/common/domain/package-info.java,
    src/main/java/com/anchors/baseline/common/application/package-info.java,
    src/main/java/com/anchors/baseline/common/infrastructure/package-info.java,
    src/main/java/com/anchors/baseline/common/interfaces/package-info.java
  </files>
  <read_first>
    - spring-backend-ddd-baseline.md (§4.2 패키지/계층 구조 — 의존 방향 안쪽, interfaces→application→domain; infrastructure는 domain 포트 구현)
    - .planning/phases/01-platform-skeleton/01-RESEARCH.md (§아키텍처 책임 맵 — 각 계층 역할)
    - .planning/phases/01-platform-skeleton/01-CONTEXT.md (D-03: platform/common 골격 형판, D-05: ArchUnit CI 게이트)
  </read_first>
  <action>
    1. `SampleApplicationService.java` (패키지: com.anchors.baseline.platform.application):
       - @Service, @RequiredArgsConstructor (Lombok)
       - @Transactional 클래스 또는 메서드 레벨
       - 생성자 주입: SampleRepository sampleRepository (domain 포트 — per D-05), SampleMapper sampleMapper
       - save(String value) → Long: new SampleEntity(value)를 sampleRepository.save()로 저장 후 ID 반환
       - findAll() → List&lt;SampleDto&gt;: sampleMapper.findAll() 호출
       - 중요: SampleJpaRepository(infrastructure)를 직접 주입하지 않는다. 반드시 SampleRepository(domain.repository 포트)를 주입해야 ArchUnit layeredArchitecture() 규칙(Application은 Domain만 접근 허용)을 준수한다 (D-05 per).

    2. `SampleController.java` (패키지: com.anchors.baseline.platform.interfaces.rest):
       - @RestController, @RequiredArgsConstructor
       - @RequestMapping("/api/v1/samples")
       - POST /: {"value": "..."} 요청 받아 sampleApplicationService.save() 호출 → 201 응답
       - GET /: sampleApplicationService.findAll() 호출 → 200 응답
       - interfaces 계층은 application만 의존한다 (domain 직접 의존 금지)

    3. common 컨텍스트 패키지 형판 — package-info.java 4개 파일:
       - com.anchors.baseline.common.domain: "공통 도메인 빌딩 블록 (공유 VO, 기반 인터페이스)"
       - com.anchors.baseline.common.application: "공통 애플리케이션 계층 유틸리티"
       - com.anchors.baseline.common.infrastructure: "공통 인프라 설정 (DataSource, Redis 등)"
       - com.anchors.baseline.common.interfaces: "공통 인터페이스 계층 (공유 DTO, 응답 래퍼 등)"
       각 파일은 단순 package 선언문만 포함하면 된다. 목적: D-03의 "빈 디렉터리 선점유 금지" 원칙에 따라 package-info.java로 디렉터리 존재를 선언.
  </action>
  <verify>
    <automated>./gradlew compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SampleApplicationService.java 에 "@Service" 와 "@Transactional" 이 포함되어 있다
    - SampleApplicationService.java 에 "SampleRepository sampleRepository" 주입이 포함되어 있다 (SampleJpaRepository 직접 주입 없음)
    - SampleController.java 에 "@RestController" 이 포함되어 있다
    - SampleController.java 에 "/api/v1/samples" 이 포함되어 있다
    - common 컨텍스트 하위 4개 계층(domain, application, infrastructure, interfaces) 디렉터리가 존재한다
    - platform 컨텍스트 하위 4개 계층(domain, application, infrastructure, interfaces) 디렉터리가 존재한다
    - ./gradlew compileJava 가 BUILD SUCCESSFUL (컴파일 오류 없음)
  </acceptance_criteria>
  <done>platform과 common 컨텍스트의 전체 계층 골격이 완성되어 ArchUnit 의존 방향 테스트가 GREEN 상태로 실행된다</done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| interfaces (REST) → application | 외부 HTTP 요청이 애플리케이션 계층으로 진입하는 경계 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-04-01 | Elevation of Privilege | SampleController REST API | accept (dev) | Phase 1 골격 전용. 인증/인가는 Phase 3·4에서 추가됨. 현재는 로컬 개발 환경 한정. |
| T-04-02 | Tampering | SampleEntity 입력값 (value 필드) | accept | Phase 1 검증 목적 엔티티. 운영 비노출. 입력 검증은 Phase 2+ 업무 도메인에서 적용. |
| T-04-SC | Tampering | 의존성 | accept | 기존 Plan 01에서 검증된 의존성만 사용. 신규 패키지 없음. |
</threat_model>

<verification>
1. `find src/main/java/com/anchors/baseline -type d | sort` — platform/{domain,application,infrastructure,interfaces}, common/{domain,application,infrastructure,interfaces} 디렉터리 확인
2. `grep -r "@Table(name" src/main/java` — platform_sample 매핑 확인
3. `grep -r "@Mapper" src/main/java` — SampleMapper 확인
4. `grep -r "@RestController" src/main/java` — SampleController 확인
5. `grep -r "implements SampleRepository" src/main/java` — 포트 구현 확인
6. `grep -r "SampleRepository sampleRepository" src/main/java` — application이 포트를 주입하는지 확인
7. Plan 05에서: `./gradlew test --tests "*ArchitectureTest*"` → BUILD SUCCESSFUL, 의존 방향 위반 없음
</verification>

<success_criteria>
- platform 바운디드 컨텍스트가 domain/application/infrastructure/interfaces 4계층으로 완성된다
- common 바운디드 컨텍스트의 4계층 패키지 형판이 존재한다
- SampleRepository 포트 인터페이스가 domain.repository에 위치하고 SampleJpaRepository가 이를 구현한다
- SampleApplicationService가 SampleRepository(포트)를 주입하여 infrastructure 직접 의존이 없다
- SampleDto가 @NoArgsConstructor를 가진 JavaBean 클래스로 MyBatis 결과 매핑과 호환된다
- SampleEntity가 platform_sample 테이블과 매핑된다
- SampleMapper가 @Mapper로 선언되어 findAll()을 제공한다
- 계층 간 의존 방향이 interfaces→application→domain을 따른다 (ArchUnit 규칙 준수)
- Plan 05의 PersistenceIntegrationTest가 @Disabled 없이 컴파일된다
</success_criteria>

<output>
완료 시 `.planning/phases/01-platform-skeleton/01-04-SUMMARY.md` 생성
</output>
