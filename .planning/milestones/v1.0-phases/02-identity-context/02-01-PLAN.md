---
phase: 02-identity-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/anchors/baseline/identity/domain/model/User.java
  - src/main/java/com/anchors/baseline/identity/domain/model/Email.java
  - src/main/java/com/anchors/baseline/identity/domain/model/UserStatus.java
  - src/main/java/com/anchors/baseline/identity/domain/event/UserDisabled.java
  - src/main/java/com/anchors/baseline/identity/domain/exception/InvalidStateTransition.java
  - src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java
  - src/main/resources/db/migration/V2__create_identity_users.sql
autonomous: true
requirements:
  - IDEN-01
  - IDEN-02
  - IDEN-03
  - IDEN-04
  - IDEN-05
  - IDEN-06

must_haves:
  truths:
    - "User.invite(Email) 정적 팩토리가 status=INVITED 인 애그리거트를 생성한다"
    - "User.linkIdentity(externalId, idpDisplayName) 가 INVITED→ACTIVE 전이를 수행하고 status!=INVITED 면 InvalidStateTransition 을 던진다 (IDEN-03 멱등성)"
    - "linkIdentity 는 관리자 입력 필드(internalNote)를 인자로 받지 않아 구조적으로 갱신 불가하다 (IDEN-06)"
    - "User.disable() 이 DISABLED 로 전이하고 registerEvent(new UserDisabled(this.id)) 를 누적하며, 이미 DISABLED 면 무연산·이벤트 미발행이다 (Open Q1 결정)"
    - "User 가 AbstractAggregateRoot<User> 를 상속하고 public setter 가 없다 (D-01)"
    - "Email 은 @Embeddable VO 로 생성 시점 형식 검증을 수행한다 (D-02)"
    - "externalId 는 nullable String 컬럼으로 직접 매핑되어 all-null embeddable 함정을 회피한다 (RESEARCH Pitfall 1)"
    - "V2 마이그레이션이 users 테이블(email NOT NULL UNIQUE, external_id nullable UNIQUE, status NOT NULL, display_name, internal_note, created_at)을 생성한다"
    - "UserRepository 포트가 domain.repository 에 존재하고 save/findById/existsByEmail/findByExternalId 를 선언한다"
  artifacts:
    - path: "src/main/java/com/anchors/baseline/identity/domain/model/User.java"
      provides: "User 애그리거트 루트 — invite/linkIdentity/disable 불변식, 이벤트 누적"
      contains: "extends AbstractAggregateRoot<User>"
    - path: "src/main/java/com/anchors/baseline/identity/domain/model/Email.java"
      provides: "Email 값 객체 — @Embeddable, 형식 검증"
      contains: "@Embeddable"
    - path: "src/main/java/com/anchors/baseline/identity/domain/model/UserStatus.java"
      provides: "상태 enum — INVITED/ACTIVE/DISABLED"
      contains: "INVITED"
    - path: "src/main/java/com/anchors/baseline/identity/domain/event/UserDisabled.java"
      provides: "도메인 이벤트 — userId(Long) 페이로드만 (§4.1 ID 참조)"
      contains: "record UserDisabled"
    - path: "src/main/java/com/anchors/baseline/identity/domain/exception/InvalidStateTransition.java"
      provides: "도메인 예외 — 잘못된 상태 전이·멱등성 위반 (D-05)"
      contains: "class InvalidStateTransition"
    - path: "src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java"
      provides: "영속화 포트 (도메인) — Phase 1 SampleRepository 형판"
      contains: "interface UserRepository"
    - path: "src/main/resources/db/migration/V2__create_identity_users.sql"
      provides: "users 테이블 스키마 — Hibernate 매핑과 정합 (ddl-auto: validate)"
      contains: "CREATE TABLE"
  key_links:
    - from: "src/main/java/com/anchors/baseline/identity/domain/model/User.java"
      to: "UserDisabled"
      via: "disable() 내 registerEvent"
      pattern: "registerEvent\\(new UserDisabled"
    - from: "src/main/resources/db/migration/V2__create_identity_users.sql"
      to: "User @Entity 매핑"
      via: "컬럼명·nullable·unique 정합 (ddl-auto: validate)"
      pattern: "external_id"
---

<objective>
Identity 바운디드 컨텍스트의 **도메인 계층 전체**와 이를 뒷받침할 Flyway V2 스키마를 생성한다. `User` 애그리거트 루트(invite/linkIdentity/disable 불변식), `Email` VO, `UserStatus` enum, `UserDisabled` 도메인 이벤트, 도메인 예외, `UserRepository` 포트, 그리고 `users` 테이블 마이그레이션이 산출물이다.

Purpose: 후속 wave(인프라 어댑터·애플리케이션 서비스·테스트)와 후속 Phase(3 BFF 인증의 linkIdentity 호출, 4 Authorization 의 UserDisabled 구독)가 의존할 **공개 도메인 계약**을 먼저 확정한다. 전방 참조 컴파일 위험(Phase 1 lesson)을 wave 순서로 제거한다 — 타입이 먼저 존재하므로 후속 wave 테스트가 @Disabled+주석 우회 없이 바로 컴파일된다.

Output: `com.anchors.baseline.identity.domain.*` 7개 파일 + `V2__create_identity_users.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-context/02-CONTEXT.md
@.planning/phases/02-identity-context/02-RESEARCH.md
@spring-backend-ddd-baseline.md

<interfaces>
<!-- Phase 1 형판 — 이 도메인이 그대로 계승하는 패턴. 코드베이스 재탐색 불필요. -->

From src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java (JPA 방식 A 스타일):
- @Entity @Table(name=...) @Getter(Lombok) 를 domain/model 에 직접 선언
- @Id @GeneratedValue(strategy = GenerationType.IDENTITY) → BIGSERIAL
- protected 무인자 생성자 (JPA 전용), public setter 없음
- @Column(name="created_at", nullable=false, updatable=false) private Instant createdAt = Instant.now();

From src/main/java/com/anchors/baseline/platform/domain/repository/SampleRepository.java (포트):
- domain.repository 패키지의 순수 interface. infrastructure 가 구현.

From src/main/resources/db/migration/V1__init_schema.sql (Flyway 규약):
- BIGSERIAL PRIMARY KEY, TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL, COMMENT ON TABLE

From src/main/resources/application.yml (확인됨 — A5 해소):
- spring.jpa.hibernate.ddl-auto: validate  → Flyway 가 스키마 권위. V2 DDL 이 Hibernate 매핑과 정확히 정합해야 함 (불일치 시 부팅 실패).
- AbstractAggregateRoot 는 spring-data-commons(이미 의존)의 org.springframework.data.domain.AbstractAggregateRoot.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: User 애그리거트 + VO + enum + 이벤트 + 예외 작성</name>
  <files>
    src/main/java/com/anchors/baseline/identity/domain/model/User.java,
    src/main/java/com/anchors/baseline/identity/domain/model/Email.java,
    src/main/java/com/anchors/baseline/identity/domain/model/UserStatus.java,
    src/main/java/com/anchors/baseline/identity/domain/event/UserDisabled.java,
    src/main/java/com/anchors/baseline/identity/domain/exception/InvalidStateTransition.java
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/platform/domain/model/SampleEntity.java (JPA 방식 A 스타일 형판)
    - .planning/phases/02-identity-context/02-RESEARCH.md §"Pattern 1" (User 코드 형판), §"Common Pitfalls" Pitfall 1·2 (nullable embeddable / AbstractAggregateRoot @Transient)
    - spring-backend-ddd-baseline.md §5.2 (Identity worked example), §4.4 (방식 A)
  </read_first>
  <behavior>
    - invite: User.invite(email) → status==INVITED, id/externalId==null, internalNote==null
    - linkIdentity 정상: INVITED 상태에서 linkIdentity("oid-1","홍길동") → status==ACTIVE, externalId=="oid-1", displayName=="홍길동"
    - linkIdentity 멱등성(IDEN-03): ACTIVE 상태(또는 status!=INVITED)에서 linkIdentity 재호출 → InvalidStateTransition
    - 필드 소유권(IDEN-06): linkIdentity 시그니처가 internalNote 를 인자로 받지 않음 → 컴파일 수준 구조적 강제. linkIdentity 호출 후 기존 internalNote 값 불변, displayName 만 갱신
    - disable 정상(IDEN-04): 임의 비-DISABLED 상태에서 disable() → status==DISABLED, UserDisabled 이벤트 1건 누적
    - disable 멱등성(Open Q1 결정): 이미 DISABLED 에서 disable() 재호출 → 무연산, 추가 이벤트 미발행
    - Email 검증(D-02): 형식 위반 문자열로 Email 생성 시 예외, 정상 형식은 통과
  </behavior>
  <action>
    `com.anchors.baseline.identity.domain.model` 에 다음을 작성한다.

    `UserStatus` enum: INVITED, ACTIVE, DISABLED 3개 값.

    `Email`: @Embeddable 값 객체. 단일 필드 value(String, NOT NULL). 정적 팩토리 또는 생성자에서 형식 검증(간단한 정규식 또는 jakarta @Email 수준 — 공백·@ 부재 거부 정도면 충분, 과도한 RFC 검증 금지). equals/hashCode 는 value 기준(record 불가 — JPA @Embeddable 제약 시 명시 구현 또는 Hibernate 6 record-as-embeddable 사용은 재량이나 nullable 함정 없는 NOT NULL 이므로 단순 클래스+@Getter 권장). getValue() 노출, public setter 금지.

    `User`: @Entity @Table(name="users") @Getter, `extends AbstractAggregateRoot<User>` (org.springframework.data.domain.AbstractAggregateRoot). 필드:
    - @Id @GeneratedValue(IDENTITY) Long id
    - @Embedded @AttributeOverride(name="value", column=@Column(name="email", nullable=false, unique=true)) Email email
    - @Column(name="external_id", unique=true) String externalId  ← **nullable String 직접 매핑** (RESEARCH Pitfall 1: nullable @Embeddable all-null=null NPE 회피)
    - @Enumerated(EnumType.STRING) @Column(name="status", nullable=false) UserStatus status
    - @Column(name="display_name") String displayName  ← IdP 출처 필드(linkIdentity 가 갱신)
    - @Column(name="internal_note") String internalNote  ← **관리자 입력 필드(Open Q2 결정: SC#4 행위 단언용 최소 placeholder)**. linkIdentity 가 인자로 받지 않아 구조적으로 보존됨
    - @Column(name="created_at", nullable=false, updatable=false) Instant createdAt = Instant.now()

    메서드:
    - protected User() {} (JPA 전용)
    - private User(Email email) { this.email=email; this.status=INVITED; }
    - public static User invite(Email email) → IDEN-01
    - public void linkIdentity(String externalId, String idpDisplayName) → IDEN-02/03/06. 가드: status != INVITED → throw InvalidStateTransition (externalId null 여부가 아니라 **status 기준** — Pitfall 1 의존 회피). 통과 시 this.externalId=externalId, this.displayName=idpDisplayName, this.status=ACTIVE. internalNote 는 미변경.
    - public void disable() → IDEN-04 + Open Q1. 이미 DISABLED 면 즉시 return(무연산). 아니면 this.status=DISABLED; registerEvent(new UserDisabled(this.id));
    - (선택) 관리자 필드 설정용 메서드(예: setInternalNote 대신 의미 메서드 updateInternalNote) — SC#4 테스트가 internalNote 를 세팅할 수 있도록 최소 도메인 메서드 1개 제공. public setter 형태 금지(D-01), 의미 있는 메서드명 사용.

    `com.anchors.baseline.identity.domain.event.UserDisabled`: `public record UserDisabled(Long userId) {}` (§4.1 컨텍스트 간 참조는 ID로만).

    `com.anchors.baseline.identity.domain.exception.InvalidStateTransition`: RuntimeException 확장 도메인 예외(D-05). 메시지 생성자. 멱등성 위반·잘못된 전이를 표현. (EmailAlreadyExists 는 Plan 02 의 application 선검사에서 도입 — 이 plan 범위 아님.)

    **금지:** ApplicationEventPublisher 를 User 에 주입하지 말 것(도메인 오염, RESEARCH Anti-Pattern). external_id 를 NOT NULL 로 두지 말 것. 애그리거트 메서드 밖(application)에 규칙 배치 금지(D-04).
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - identity/domain/model 에 User.java, Email.java, UserStatus.java 존재, identity/domain/event/UserDisabled.java, identity/domain/exception/InvalidStateTransition.java 존재
    - User 가 AbstractAggregateRoot<User> 상속, public setter 없음, linkIdentity 시그니처에 internalNote 인자 없음(grep 확인)
    - externalId 가 String 타입 @Column(nullable 허용, unique)
    - compileJava GREEN (단위 테스트는 Plan 03 에서 작성·실행)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: UserRepository 포트 + Flyway V2 마이그레이션 작성</name>
  <files>
    src/main/java/com/anchors/baseline/identity/domain/repository/UserRepository.java,
    src/main/resources/db/migration/V2__create_identity_users.sql
  </files>
  <read_first>
    - src/main/java/com/anchors/baseline/platform/domain/repository/SampleRepository.java (포트 형판)
    - src/main/resources/db/migration/V1__init_schema.sql (Flyway 규약)
    - src/main/resources/application.yml (ddl-auto: validate 확인 — DDL↔매핑 정합 필수)
    - .planning/phases/02-identity-context/02-RESEARCH.md §"Flyway V2 Migration", §"Pattern 3"(포트 시그니처)
  </read_first>
  <action>
    `com.anchors.baseline.identity.domain.repository.UserRepository` 포트 인터페이스 작성(Phase 1 SampleRepository 형판). 선언 메서드:
    - User save(User user)
    - Optional<User> findById(Long id)
    - boolean existsByEmail(Email email)  ← email 선검사용(D-06). @Embeddable 파라미터 — Plan 02 어댑터에서 파생 쿼리 매핑 검증
    - Optional<User> findByExternalId(String externalId)  ← 연결 중복 선검사용(선택)

    `src/main/resources/db/migration/V2__create_identity_users.sql` 작성. Phase 1 규약(BIGSERIAL PK, TIMESTAMP WITH TIME ZONE DEFAULT now(), COMMENT) 그대로. **Task 1 의 User @Entity 매핑과 컬럼명·nullable·unique 가 정확히 정합**해야 한다(ddl-auto: validate — 불일치 시 통합 테스트 부팅 실패):
    - id BIGSERIAL PRIMARY KEY
    - email VARCHAR(255) NOT NULL UNIQUE  (IDEN-05)
    - external_id VARCHAR(255) UNIQUE  (nullable + UNIQUE — A-6, D-06)
    - status VARCHAR(20) NOT NULL  (@Enumerated STRING)
    - display_name VARCHAR(255)  (IdP 출처)
    - internal_note VARCHAR(255)  (관리자 입력 — Open Q2)
    - created_at TIMESTAMP WITH TIME ZONE DEFAULT now() NOT NULL
    - COMMENT ON TABLE users IS 'Identity 컨텍스트 — 사용자 식별 진실 공급원 (Phase 2)'

    `users` 는 PostgreSQL 예약어 아님 — 따옴표 불필요(소문자 unquoted).
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - identity/domain/repository/UserRepository.java 존재, 4개 메서드 선언, infrastructure import 없음(domain 순수)
    - V2__create_identity_users.sql 존재, 7개 컬럼 + UNIQUE(email)·UNIQUE(external_id) + COMMENT
    - external_id 가 NULL 허용(NOT NULL 아님), email 만 NOT NULL UNIQUE
    - compileJava GREEN (Flyway 실행·매핑 정합 검증은 Plan 03 통합 테스트)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| application → domain (User 메서드) | linkIdentity/disable 호출 시 도메인 불변식이 강제되는 경계 |
| infrastructure (JPA) → DB (users 테이블) | 영속화 시 UNIQUE 제약이 최종 방어선이 되는 경계 |

## STRIDE 위협 등록부

| Threat ID | 카테고리 | 컴포넌트 | 처리 방향 | 완화 계획 |
|-----------|---------|---------|----------|---------|
| T-02-01 | Tampering | linkIdentity — 관리자 입력 필드 덮어쓰기 시도 | mitigate | IDEN-06: linkIdentity 시그니처가 internalNote 를 인자로 받지 않아 구조적으로 덮어쓰기 불가. Task 1 에서 강제. |
| T-02-02 | Elevation of Privilege | linkIdentity 멱등성 우회(동일 신원 재연결) | mitigate | IDEN-03: status != INVITED 가드로 재연결 차단(InvalidStateTransition). 도메인 메서드 내부 강제(D-04). |
| T-02-03 | Spoofing | 중복 email 로 다중 신원 생성 | mitigate | IDEN-05: DB UNIQUE(email) 최종 방어 + Plan 02 application 선검사. external_id 도 UNIQUE. |
| T-02-04 | Information Disclosure | UserDisabled 이벤트 페이로드 과다 노출 | mitigate | 페이로드 = userId(Long) 만(§4.1). PII 미포함. |
| T-02-SC | Tampering | 의존성(공급망) | accept | 신규 외부 패키지 0건 — Phase 1 BOM 재사용(RESEARCH Package Legitimacy Audit). slopcheck 대상 없음. |
</threat_model>

<verification>
- `./gradlew compileJava` GREEN — 도메인 타입 전부 컴파일
- grep: `User extends AbstractAggregateRoot`, `registerEvent(new UserDisabled`, `linkIdentity(String externalId, String idpDisplayName)` (internalNote 인자 부재)
- grep: V2 SQL 에 `email VARCHAR(255) NOT NULL UNIQUE`, `external_id VARCHAR(255) UNIQUE`(NOT NULL 부재)
- ArchUnit(기존 게이트)은 Plan 03 전체 스위트에서 검증 — 이 plan 은 domain 만 생성하므로 위반 불가
</verification>

<success_criteria>
- identity.domain 패키지에 User/Email/UserStatus/UserDisabled/InvalidStateTransition/UserRepository 6개 타입 존재
- V2 마이그레이션이 Phase 1 규약을 따르고 User 매핑과 정합
- compileJava GREEN, infrastructure 의존 0건(domain 순수)
- 후속 Plan 02·03 이 의존할 도메인 계약 확정(전방 참조 위험 제거)
</success_criteria>

<output>
Create `.planning/phases/02-identity-context/02-01-SUMMARY.md` when done
</output>
