---
phase: 04-authorization
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/test/java/com/anchors/baseline/authorization/PermissionEvaluatorTest.java
  - src/test/java/com/anchors/baseline/authorization/PermissionEvaluationIT.java
  - src/test/java/com/anchors/baseline/authorization/ListObjectsIT.java
  - src/test/java/com/anchors/baseline/authorization/ResourceHierarchyCycleIT.java
  - src/test/java/com/anchors/baseline/authorization/InvitedGrantSurfacingIT.java
  - src/test/java/com/anchors/baseline/authorization/AuthorizationPortWiringIT.java
autonomous: true
requirements: [AUTHZ-01, AUTHZ-05, AUTHZ-06, AUTHZ-07, AUTHZ-08, AUTHZ-09]

must_haves:
  truths:
    - "grant→evaluate=true, revoke→evaluate=false가 연속 단언된다(SC#1 즉시 반영)"
    - "evaluate 진리표(직접/그룹/상속/조합/없음 + 역할 함의)가 정확한 boolean을 낸다(SC#2)"
    - "상위 리소스 부여가 하위로 상속되어 ListObjects 결과에 포함된다(SC#3)"
    - "resource_hierarchy 사이클을 실 Postgres에 입력해도 무한 루프 없이 종료된다"
    - "INVITED userId grant 저장 → linkIdentity ACTIVE 전이 → 동일 userId evaluate 반영(SC#4)"
    - "AuthorizationPort가 Postgres 어댑터로 주입되어 교체 가능 구조임이 단언된다(SC#5)"
  artifacts:
    - path: "src/test/java/com/anchors/baseline/authorization/PermissionEvaluationIT.java"
      provides: "SC#1·#2 행위 단언(즉시성 + 진리표)"
      contains: "AbstractIntegrationTest"
    - path: "src/test/java/com/anchors/baseline/authorization/InvitedGrantSurfacingIT.java"
      provides: "SC#4 — 실제 IdentityApplicationService.linkIdentity 호출 엮음"
      contains: "linkIdentity"
    - path: "src/test/java/com/anchors/baseline/authorization/ResourceHierarchyCycleIT.java"
      provides: "사이클 가드 — 실 Postgres에 사이클 입력"
      contains: "AbstractIntegrationTest"
  key_links:
    - from: "PermissionEvaluationIT.java"
      to: "AuthorizationApplicationService + PermissionEvaluator"
      via: "grant 커밋 후 evaluate 호출"
      pattern: "evaluate"
    - from: "InvitedGrantSurfacingIT.java"
      to: "IdentityApplicationService.linkIdentity"
      via: "실제 호출(placeholder 금지)"
      pattern: "linkIdentity"
---

<objective>
SC#1~5와 사이클 가드를 **실 Postgres(Testcontainers) 행위 단언**으로 검증하는 Wave 0 테스트를 작성하고 전체 스위트를 GREEN으로 만든다. 재귀 CTE 문법·MyBatis 매핑은 여기서 실측되어 추정이 아님이 증명된다(lesson 03 P1).

Purpose: VALIDATION.md의 SC별 주장이 실제 통합 테스트 코드로 구현됐는지를 닫는다(lesson 02 P1: 주장 ↔ 테스트 존재). SC#4는 실제 linkIdentity 호출, 사이클 가드는 실 Postgres 사이클 입력으로 충실도를 강제한다.

Output: 단위 1종(PermissionEvaluatorTest) + 통합 5종(PermissionEvaluationIT, ListObjectsIT, ResourceHierarchyCycleIT, InvitedGrantSurfacingIT, AuthorizationPortWiringIT). 전체 GREEN + ArchitectureTest GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-authorization/04-VALIDATION.md
@.planning/phases/04-authorization/04-01-SUMMARY.md
@.planning/phases/04-authorization/04-02-SUMMARY.md

<read_first>
- src/test/java/com/anchors/baseline/AbstractIntegrationTest.java — 싱글턴 Testcontainers(postgres:16 + redis:7), @SpringBootTest(RANDOM_PORT), @ActiveProfiles("test"). 모든 IT가 상속(D-10). 재귀 CTE는 실 Postgres 필수 — H2 금지.
- src/main/java/com/anchors/baseline/identity/application/IdentityApplicationService.java — invite(email)→userId, linkIdentity(userId, externalId, displayName) INVITED→ACTIVE. SC#4가 호출.
- src/test/resources/application-test.yml — ddl-auto: validate. 엔티티↔V3 DDL 불일치 시 컨텍스트 부팅 실패(테스트 RED로 표면화).
- 04-RESEARCH.md §Validation Architecture — SC별 Test Map. §Pitfall 3 — grant 커밋 후 evaluate(같은 tx 내 미flush 미가시).
- (Phase 2/3 IT 형판) src/test/java/com/anchors/baseline/identity/ 및 auth/ 하위 통합 테스트 — @Autowired 서비스 주입·행위 단언 스타일 참조.
</read_first>

<interfaces>
Wave 1·2가 제공(이 Plan이 호출):
- AuthorizationApplicationService: grantGlobalRole/revokeGlobalRole, grantResourceToUser/Group, createGroup→groupId, addMember, defineHierarchy(resourceId, parentResourceId) 등
- PermissionEvaluator.evaluate(long userId, ResourceId, Action) (application)
- AuthorizationPort.evaluate(...) / listObjects(long, Action) — PostgresAuthorizationAdapter 주입
- PermissionReadPort (fake로 단위 테스트에서 대체 가능)
- IdentityApplicationService.invite(String)→Long, linkIdentity(Long, String, String)

주체 polymorphic(user_id XOR group_id). resource_hierarchy(resource_id, parent_resource_id). 역할 함의 EDITOR ⊇ VIEWER.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 단위 — PermissionEvaluatorTest (역할 함의)</name>
  <files>src/test/java/com/anchors/baseline/authorization/PermissionEvaluatorTest.java</files>
  <behavior>
    - fake PermissionReadPort가 빈 리스트 반환 → evaluate=false (default-deny, T-04-01).
    - fake가 EDITOR role grant 반환 + VIEW action 요청 → true (EDITOR ⊇ VIEWER).
    - fake가 VIEWER role grant 반환 + EDIT action 요청 → false.
  </behavior>
  <action>
in-memory fake PermissionReadPort(테스트 내 익명 구현 또는 람다)를 PermissionEvaluator에 주입해 역할 함의 규칙을 Testcontainers 없이 단위 검증한다(@SpringBootTest 불요 — 순수 POJO 단위 테스트, 빠른 피드백). default-deny·EDITOR⊇VIEWER·VIEWER∌EDIT 케이스를 단언한다. @ParameterizedTest로 함의 매트릭스를 표현해도 좋다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*PermissionEvaluatorTest*"</automated>
  </verify>
  <done>PermissionEvaluatorTest가 default-deny + 역할 함의(EDITOR⊇VIEW, VIEWER∌EDIT)를 fake 읽기 포트로 단위 검증하고 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 통합 — SC#1·#2·#3 (즉시성·진리표·ListObjects)</name>
  <files>src/test/java/com/anchors/baseline/authorization/PermissionEvaluationIT.java, src/test/java/com/anchors/baseline/authorization/ListObjectsIT.java</files>
  <behavior>
    - SC#1: grantGlobalRole(또는 resource grant) 커밋 → evaluate=true. revoke 커밋 → evaluate=false. 연속 단언(캐시 없음, 즉시).
    - SC#2: evaluate 진리표 — 직접만/그룹만(addMember 후 그룹 부여)/상속만(상위 리소스 부여)/조합/없음(default-deny) 각 케이스가 정확한 boolean. 역할 함의(EDITOR→VIEW) 케이스 포함. @ParameterizedTest 권장.
    - SC#3: defineHierarchy(child, parent) + parent에 부여 → listObjects 결과에 child 포함(재귀 CTE 하향 전개).
  </behavior>
  <action>
AbstractIntegrationTest를 상속하고 AuthorizationApplicationService·PermissionEvaluator(또는 AuthorizationPort)·IdentityApplicationservice를 @Autowired 주입한다. PermissionEvaluationIT: SC#1 즉시성(grant→evaluate true, revoke→evaluate false 연속) + SC#2 진리표(직접/그룹/상속/조합/없음 + 역할 함의). Pitfall 3 준수 — grant는 서비스 호출(트랜잭션 커밋)로 영속화 후 별도 evaluate 호출(같은 tx 내 read-after-write 회피). ListObjectsIT: SC#3 — 리소스 계층 정의 + 상위 부여 후 listObjects에 하위 resource_id 포함 단언(재귀 CTE 경로 실측). 테스트 데이터는 각 테스트가 자기 userId/resourceId를 생성해 격리(@Transactional 롤백 또는 고유 키).
  </action>
  <verify>
    <automated>./gradlew test --tests "*PermissionEvaluationIT*" --tests "*ListObjectsIT*"</automated>
  </verify>
  <done>PermissionEvaluationIT가 SC#1 즉시성 + SC#2 진리표(직접∪그룹∪상속 + default-deny + 역할 함의)를 실 Postgres로 단언하고, ListObjectsIT가 SC#3 계층 상속을 단언하며 둘 다 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 통합 — 사이클 가드·SC#4·SC#5 + 전체 스위트 GREEN</name>
  <files>src/test/java/com/anchors/baseline/authorization/ResourceHierarchyCycleIT.java, src/test/java/com/anchors/baseline/authorization/InvitedGrantSurfacingIT.java, src/test/java/com/anchors/baseline/authorization/AuthorizationPortWiringIT.java</files>
  <behavior>
    - 사이클 가드: resource_hierarchy에 A→B→A 사이클을 실 Postgres에 입력 후 evaluate/listObjects 호출 → assertTimeoutPreemptively 내 종료(무한 루프 없음, CYCLE/UNION 검증).
    - SC#4: identity.invite(email)→INVITED userId → authorization grant(해당 userId) → grant DB 저장 단언 → identity.linkIdentity(userId,...) ACTIVE 전이 → 동일 userId evaluate=true 단언.
    - SC#5: AuthorizationPort 빈이 PostgresAuthorizationAdapter 타입으로 주입되고 evaluate/listObjects가 동작(포트만 의존, 어댑터 교체 가능 구조).
  </behavior>
  <action>
ResourceHierarchyCycleIT: defineHierarchy로 사이클(A의 부모=B, B의 부모=A)을 일부러 만들고 evaluate/listObjects를 assertTimeoutPreemptively(Duration.ofSeconds(5), ...)로 감싸 무한 루프 부재를 증명한다(lesson 03: 틀리면 깨지는 테스트 — H2면 재현 불가하므로 실 Postgres 필수). InvitedGrantSurfacingIT: **실제 IdentityApplicationService.linkIdentity를 호출**(placeholder 금지 — VALIDATION 명시). invite→userId(INVITED) → authorization grant → 쓰기 포트/조회로 DB 저장 단언 → linkIdentity로 ACTIVE 전이 → 동일 userId evaluate=true(soft ref라 추가 코드 없이 반영됨을 행위로 증명, D-07). AuthorizationPortWiringIT: @Autowired AuthorizationPort가 non-null이고 구현 타입이 PostgresAuthorizationAdapter임(또는 evaluate 위임 동작)을 단언해 SC#5 교체 가능 구조 확인(ArchitectureTest가 구조 정합을 별도 보장). 마지막으로 전체 스위트를 독립 재검증(--rerun-tasks)한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*ResourceHierarchyCycleIT*" --tests "*InvitedGrantSurfacingIT*" --tests "*AuthorizationPortWiringIT*" --tests "*ArchitectureTest*" && ./gradlew test --rerun-tasks</automated>
  </verify>
  <done>사이클 가드 IT가 실 Postgres 사이클 입력에 타임아웃 없이 종료, InvitedGrantSurfacingIT가 실제 linkIdentity 호출로 SC#4를 단언, AuthorizationPortWiringIT가 SC#5 포트 주입을 단언하며, `./gradlew test --rerun-tasks` 전체 스위트가 GREEN(ArchitectureTest 포함).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 입력 → 실 Postgres CTE | 사이클·진리표 입력이 실 DB로 전달(추정 아닌 실측) |
| identity ↔ authorization | linkIdentity 후 soft ref userId로 권한 표면화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | default-deny | mitigate | 진리표 "없음" 케이스 + 단위 default-deny가 false 단언 |
| T-04-04 | Elevation of Privilege | 상속 오버리치 | mitigate | 진리표 상속 케이스가 조상 방향만 반영됨을 단언(역방향 grant는 미반영 확인) |
| T-04-06 | Denial of Service | 재귀 CTE 사이클 | mitigate | ResourceHierarchyCycleIT가 실 Postgres 사이클에 assertTimeoutPreemptively로 종료 증명 |
| T-04-07 | Elevation of Privilege | revoke stale | mitigate | SC#1 연속 단언(revoke 커밋 후 즉시 false) |
| T-04-09 | Spoofing/Integrity | SC#4 placeholder 우회 | mitigate | InvitedGrantSurfacingIT가 실제 linkIdentity 호출(VALIDATION 강제, lesson 02 P1) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 없음(D-11) |
</threat_model>

<verification>
- `./gradlew test --rerun-tasks` 전체 GREEN (독립 재검증 — lesson 01/02 Continue)
- `*ArchitectureTest*` GREEN (신규 authorization 4계층 정합 — Phase 게이트)
- SC#1~5 + 사이클 가드 각 IT가 실 Postgres(Testcontainers)로 GREEN — VALIDATION.md Per-Task Map 전 항목이 ✅ green으로 전환
- InvitedGrantSurfacingIT에 `linkIdentity` 실제 호출 존재(placeholder 0건)
</verification>

<success_criteria>
- SC#1: grant→true, revoke→false 연속 단언 GREEN
- SC#2: evaluate 진리표(직접∪그룹∪상속 + default-deny + 역할 함의) GREEN
- SC#3: 계층 상속 → ListObjects 포함 GREEN
- SC#4: INVITED grant → linkIdentity ACTIVE → evaluate 반영(실제 linkIdentity) GREEN
- SC#5: AuthorizationPort 주입 + 어댑터 교체 가능 구조 GREEN
- 사이클 가드 GREEN, 전체 스위트 + ArchitectureTest GREEN
</success_criteria>

<output>
Create `.planning/phases/04-authorization/04-03-SUMMARY.md` when done
</output>
